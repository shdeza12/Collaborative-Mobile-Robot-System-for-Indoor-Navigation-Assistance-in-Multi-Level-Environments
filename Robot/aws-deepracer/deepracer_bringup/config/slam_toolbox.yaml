slam_toolbox:
  ros__parameters:

    # Plugin params
    solver_plugin: solver_plugins::CeresSolver
    ceres_linear_solver: SPARSE_NORMAL_CHOLESKY
    ceres_preconditioner: SCHUR_JACOBI
    ceres_trust_strategy: LEVENBERG_MARQUARDT
    ceres_dogleg_type: TRADITIONAL_DOGLEG
    ceres_loss_function: None

    # ROS Parameters
    odom_frame: odom
    map_frame: map
    base_frame: base_link
    scan_topic: /scan

    debug_logging: false
    throttle_scans: 1
    transform_publish_period: 0.25 #if 0 never publishes odometry
    map_update_interval: 0.5
    resolution: 0.05
    # DEBE quedar por debajo del alcance fisico del LiDAR, que es 10.0 m
    # (deepracer.xacro: lidar_360_degree_max_range). Con el valor anterior de
    # 12.0 los rayos sin retorno volvian como 10.0 m, quedaban por debajo del
    # umbral y se rasterizaban como paredes fantasma, dibujando arcos a 10 m
    # de radio en los extremos abiertos del pasillo.
    max_laser_range: 9.5
    minimum_time_interval: 0.05
    transform_timeout: 0.5
    tf_buffer_duration: 10.0
    stack_size_to_use: 40000000 #// program needs a larger stack size to serialize large maps
    enable_interactive_mode: false

    # General Parameters
    use_scan_matching: true
    use_scan_barycenter: true
    # Ajustados para pasillo largo (mundo de 58,2 m x 6,7 m).
    # Los valores previos eran los del upstream de AWS, dimensionados para una
    # pista de carreras pequena, e impedian el enlace de nodos y el cierre de
    # bucle a lo largo del corredor.
    minimum_travel_distance: 0.15
    minimum_travel_heading: 0.3
    scan_buffer_size: 20
    scan_buffer_maximum_scan_distance: 10.0
    link_match_minimum_response_fine: 0.1
    link_scan_maximum_distance: 10.0
    loop_search_maximum_distance: 8.0
    do_loop_closing: true
    loop_match_minimum_chain_size: 10
    loop_match_maximum_variance_coarse: 3.0
    loop_match_minimum_response_coarse: 0.35
    loop_match_minimum_response_fine: 0.45

    # Correlation Parameters - Correlation Parameters
    correlation_search_space_dimension: 0.5
    correlation_search_space_resolution: 0.01
    correlation_search_space_smear_deviation: 0.1

    # Correlation Parameters - Loop Closure Parameters
    loop_search_space_dimension: 8.0
    loop_search_space_resolution: 0.05
    loop_search_space_smear_deviation: 0.03

    # Scan Matcher Parameters
    distance_variance_penalty: 0.5
    angle_variance_penalty: 1.0

    fine_search_angle_offset: 0.00349
    coarse_search_angle_offset: 0.349
    coarse_angle_resolution: 0.0349
    minimum_angle_penalty: 0.9
    minimum_distance_penalty: 0.5
    use_response_expansion: true

    # En simulacion debe ser true. El launch tambien lo inyecta, pero dejarlo
    # explicito evita que una invocacion directa del nodo lo deje en false.
    use_sim_time: true
    mode: mapping #localization