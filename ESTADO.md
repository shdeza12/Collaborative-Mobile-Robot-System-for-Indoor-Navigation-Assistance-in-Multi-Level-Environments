# Estado del proyecto — tablero de trazabilidad

> Fuente única de verdad del avance. **El corte formal es el viernes en la noche** (regla adoptada
> el 2026-08-18): ese día se actualiza este tablero, se cierra el entregable de la semana y se
> commitea. Entre semana entra lo que no puede esperar —un hallazgo que invalida una decisión se
> registra el día que aparece—, pero la contabilidad semanal no se dispersa en cinco commits.
> Documento de referencia: `Documentos/Anteproyecto_Jonny_Santi.pdf` (cronograma en Cap. 8).

| | |
|---|---|
| **Semana del cronograma** | S21 de 32 — **abierta**, con lunes, martes y miércoles cumplidos el lunes, y el jueves cumplido a medias el martes |
| **Fecha de corte** | 2026-09-01 (trabajo de martes, fuera del corte formal del viernes) |
| **Fases activas** | Fase 5 — Integración (S18–S23) |
| **Semanas restantes** | **3 de desarrollo** (S21, S22, S23) — la congelación de código es S23, del 14 al 20 de septiembre. Después no entra funcionalidad nueva: solo campaña, análisis y escritura. Sustentación **S28 (19–25 oct)**, documento final S29–S32 |
| **Último entregable formal** | Semana 20 |
| **Entregables pendientes** | Ninguno. El atraso de dos cortes seguidos —S19 se cerró 4 de 4 el 25-ago sin documento y S20 cerró el 29-ago— quedó saldado en el corte del 29-ago: `Entregable_semana_19.pdf` y `Entregable_semana_20.pdf` emitidos |
| **Acta de directores** | ✅ *Actividad 1 – Corte 1* revisada y firmada por **Armando Mateus** el 2026-08-14 ([PDF](Documentos/Entregables/Actividad_1_Corte_1_Cronograma_2026-2_firmado.pdf)) |
| **Planificación vigente** | [`Documentos/CRONOGRAMA_S17_S32.md`](Documentos/CRONOGRAMA_S17_S32.md) |

---

## 1. Avance por objetivo específico

| ID | Objetivo específico (anteproyecto §4.2) | Avance | Evidencia verificable | Responsable |
|----|------------------------------------------|--------|------------------------|-------------|
| **OE1** | Modelar arquitectura funcional: requisitos, escenarios multipiso, asignación de tareas, esquema de comunicación | 🟡 80 % | `Documentos/Evidencia/arquitectura.png` (diagrama completo con protocolo de relevo); [`Documentos/REQUISITOS.md`](Documentos/REQUISITOS.md) — matriz RF ↔ OE ↔ prueba con 34 requisitos (RF-01…RF-27, RNF-01…RNF-07), ningún OE huérfano; [`CONTRATO_INTERFACES.md`](Documentos/CONTRATO_INTERFACES.md) verificado en simulación; `puntos_interes.yaml` con **15 destinos del piso 1** derivados de los vanos sobre el mapa (2026-08-20) más el **punto de transferencia del piso 2** (2026-08-23); **`coordinacion_msgs` existe y funciona** (2026-08-24): las **cinco** definiciones del §5 del contrato, compiladas en Humble y con **round-trip real por DDS** —`test/prueba_round_trip.py`, 18 de 18 sobre los 16 puntos reales del YAML—, no solo compilación. El **nodo de coordinación** existe desde ese mismo día (`Robot/aws-deepracer/coordinacion/`), con el relevo escrito como función pura y **992 planes verificados sin simulador**. **Los destinos del piso 2 ya no son un pendiente** (2026-08-26): el catálogo pasó de un solo punto —la escalera— a **16 destinos de `nivel: 2`**, y eso es lo que permitió la prueba del 29-ago. **La asignación por nivel está ejecutada de extremo a extremo** (2026-08-29, [`S20_asignacion_por_nivel.md`](Documentos/Evidencia/S20_asignacion_por_nivel.md)): dos solicitudes con destino en niveles distintos produjeron **dos agentes distintos**, que es lo que distingue «eligió bien» de «siempre responde lo mismo». **El relevo con los dos robots vivos está EJECUTADO** (2026-08-30, [`S21_relevo_ejecutado.md`](Documentos/Evidencia/S21_relevo_ejecutado.md)): misión de dos niveles con un relevo, `exito: true`, llegadas a 0,128 y 0,077 m contra `/odom`. El bloqueo de dominios DDS que lo impedía se levantó el mismo día. **Falta, y es lo que retiene el 20 % restante:** compilar `coordinacion_msgs` en la tarjeta Jazzy del carro, y los nombres definitivos de las salas del piso 2, que hacen falta en S24 y no antes | — |
| **OE2** | Plataforma robótica con **dos** vehículos: locomoción, sensado, procesamiento y **comunicación** | 🟡 55 % | Un robot en Gazebo, `deepracer_sim.launch.py`, stack de sensores validado (S14), SLAM 2D (S15). **Hardware (2026-08-05):** `deepracer-custom-car` instalado en Raspberry Pi 4 y en la tarjeta original del DeepRacer, **ambas sobre Ubuntu Server 24.04 con ROS 2 Jazzy** (confirmado 2026-08-14); acceso por red a ambas; locomoción verificada desde la interfaz web. **Sensado y control medidos sobre el vehículo, no declarados (2026-08-19 y 2026-08-21):** el LiDAR real repite una pared con 2,8 mm de dispersión ([`S19_spike_p1_p2_hardware.md`](Documentos/Evidencia/S19_spike_p1_p2_hardware.md)) y el LiDAR de fábrica publica `/scan` a 6,80 Hz con 360 muestras sobre 360° ([`S19_lidar_original_evo.md`](Documentos/Evidencia/S19_lidar_original_evo.md)); `/ctrl_pkg/servo_msg` responde de forma proporcional en dirección y tracción. **La simulación es Humble: el desajuste de distribución es un hecho, no una hipótesis — ver R8.** **Dos pendientes cerrados el 2026-08-28** ([`S20_frente_b_hardware.md`](Documentos/Evidencia/S20_frente_b_hardware.md)): el **LiDAR ya convive con la pila** del vehículo —enlace simbólico `rplidar_node` → `rplidar_composition`, elegido sobre editar `/opt/aws/deepracer/` para que una actualización de AWS no lo revierta en silencio—, a un coste medido de **−6,6 % de frecuencia de barrido** (6,80 → 6,35 Hz); y hay **control desde ROS 2 en vez de la web**, teleoperando por `/ctrl_pkg/servo_msg` con hombre muerto de 0,6 s. Es un **puente**, no un arreglo: la cadena `/cmd_vel` sigue con sus dos defectos. **Falta:** namespaces, servidor de coordinación, comunicación inter-robot, el segundo vehículo (R11), y comprobar que las cámaras publiquen en ROS | — |
| **OE3** | Interfaz HRI web responsiva (selección origen–destino) | 🔴 0 % | Ninguna | — |
| **OE4** | Evaluación con métricas: tiempo de respuesta, tiempo de asignación, tasa de éxito, continuidad entre niveles | 🟡 55 % | [`PROTOCOLO_EXPERIMENTAL.md`](Documentos/PROTOCOLO_EXPERIMENTAL.md) (2026-08-22): las cuatro métricas con definiciones operativas medibles por máquina, criterio de éxito contra `/odom`, N = 30 en simulación con sorteo por semilla, y una **lista cerrada** de causas de descarte con techo del 20 %. Escrito **antes** de instrumentar, a propósito. **Instrumentación construida (2026-08-27):** esquema de RF-25 versionado y comprobable, `herramientas/grabar_mision.sh` sella el bag con tiempo de simulación, `herramientas/componer_registro.py` compone un registro validado contra el esquema, y `herramientas/diagnosticar_llegada.py` dictamina la llegada sin usar el `SUCCEEDED` de Nav2. **Dos misiones de dos tramos grabadas y aprobadas** (`S20_rutas_03` y `_04`, cuatro llegadas entre 0,117 y 0,204 m contra un criterio de 0,25 m). De los **siete prerrequisitos del §10** —eran seis hasta el 27-ago, cuando el bloqueo de dominios DDS entró como séptimo—, **seis están cerrados**: el de dominios cayó el 30-ago con el relevo ejecutado. **Cadena ejercitada de extremo a extremo el 2026-08-29** ([`S20_asignacion_por_nivel.md`](Documentos/Evidencia/S20_asignacion_por_nivel.md)): dos solicitudes con destinos en **niveles distintos** produjeron dos agentes distintos y dos registros validados, `S20_A_M1` con éxito a **0,036 m** contra `/odom` y `S20_A_M2` como control negativo. Las dos marcadas `es_piloto` (§7 del protocolo). **El piloto destapó lo que no se ve leyendo código, y una de las dos cosas ya está corregida:** el **tiempo de asignación —una de las cuatro métricas— valía cero por construcción**, porque `_marcar()` publicaba etapa y agente en el mismo mensaje; **corregido el 2026-08-29** (`8e870ac`) con una etapa `RECIBIDA` publicada antes de planificar, y **verificado sobre bag el mismo día**: donde había un mensaje ahora hay dos ([`registros/S20_RECIBIDA_01.json`](Documentos/Evidencia/registros/S20_RECIBIDA_01.json)). **Lo que la corrección destapó a su vez es un límite de instrumento, no de código:** `/clock` de Gazebo late a **10 Hz**, así que todo sello de bag está cuantizado a **100 ms** y la asignación tarda ~0,14 ms — tres órdenes de magnitud por debajo de un tic. El §3.2 del protocolo quedó enmendado el 29-ago (`9cfc52b`): esta métrica **se reporta como cota superior por misión más una caracterización en banco**, no como un valor por misión. El **bag tampoco puede dar el RTF** con `--use-sim-time`, ya corregido en `grabar_mision.sh`. **El banco de asignación existe y dio cifra** (2026-08-30, [`S21_banco_tiempo_asignacion.md`](Documentos/Evidencia/S21_banco_tiempo_asignacion.md)): mediana 154–175 µs y máximo 306,4 µs en cuatro corridas de 30 misiones, o sea 326 veces por debajo de un tic de `/clock`. Cierra el séptimo ítem del §9 y es la primera de las cuatro métricas con número propio. **La condición B ya no está bloqueada** —el relevo corrió el 30-ago— pero **sigue sin campaña**: hay n = 1 y sin sortear, contra los N = 30 que pide el protocolo. **La instrumentación queda completa el 2026-08-31:** `herramientas/sortear_misiones.py` (§9.5) y `herramientas/analizar_campana.py` (§9.6, 43 comprobaciones sin simulador) cierran los **dos últimos pendientes del §9**, y con ellos la campaña pasa de *definida* a *ejecutable*. **Escribir el analizador destapó dos defectos que ninguna corrida habría revelado:** (1) **RF-24 —«la variable de respuesta principal» según el propio §2— no tenía campo en el registro**, así que las 30 misiones de S24 se habrían corrido sin medir la métrica que justifica el experimento; corregido con el esquema **1.1.0** y `continuidad_de()` en el compositor. (2) El intervalo de confianza que el §6.1 y el §11 prometían **no se reproducía**: 27/30 por Wilson da **74,4–96,5 %**, no el «80 % al 97 %» escrito, y el §11 había fundado una regla de defendibilidad sobre la cota mala. Los dos corregidos en documento y código. **Falta, y es lo que retiene el 45 % restante:** ejecutar la campaña —**cero** de las 30 misiones corridas—, y los cuatro huecos del registrador medidos el 27-ago | — |

**Lectura al 2026-08-31.** A **S21 de 32 (65,6 % del calendario)** el avance técnico agregado sin ponderar es del **47,5 %** (80 + 55 + 0 + 55, sobre cuatro). La brecha con el calendario es de **18 puntos** —eran 25 el 29-ago— y **no se cierra alargando semanas**: la congelación de código es S23, o sea que quedan **tres semanas de desarrollo** para lo que falta. *La lectura anterior arrastraba cifras que ya no coincidían con sus propias filas (65 y 30 para OE1 y OE4); se recalcula aquí contra la tabla.*

Dos matices que la cifra sola no dice, y que importan más que la cifra:

- **El aporte declarado ya está ejecutado, y lo que falta ahora es reportarlo.** El relevo entre pisos corrió el 30-ago con los dos robots vivos (`exito: true`, llegadas a 0,128 y 0,077 m contra `/odom`), así que el riesgo de quedarse sin resultado central **se cerró**. El riesgo que lo sustituye es distinto y menor: hay **n = 1 y sin sortear** contra los N = 30 que pide el protocolo, o sea que existe el resultado pero no la **evidencia estadística** para afirmarlo. La instrumentación para producirla quedó completa el 31-ago; ejecutar la campaña es S24.
- **OE3 sigue en 0 % y es la barra más larga que queda entera.** Es una semana completa (S22) que no admite compresión, porque de ella dependen **RF-17 a RF-20** y la demostración ante el jurado. *Corregido el 2026-08-31: esta línea decía «RF-01 a RF-05», que son de **OE1** y de los cuales tres ya están verificados. El §9 de [`REQUISITOS.md`](Documentos/REQUISITOS.md) es la trazabilidad buena: OE3 = RF-17 a RF-20, los cuatro pendientes.* **Se reparte el 2026-08-31:** Jonny toma la HRI en paralelo, que es la paralelización más segura disponible porque **RF-19 obliga a que hable sólo con `/coordinacion`, nunca con los agentes** — la frontera ya está declarada y no se cruza con la campaña de OE4.

---

## 2. Camino crítico

Ordenado por dependencia. Nada de lo que sigue puede saltarse. La columna *Semana* remite al [cronograma vigente](Documentos/CRONOGRAMA_S17_S32.md).

| # | Hito | Bloquea a | Semana | Estado |
|---|------|-----------|--------|--------|
| 1 | **Contrato de interfaces ROS 2** (namespaces, acciones, tópicos) | Todo el desarrollo posterior | S17 | 🟢 Verificado en simulación el 2026-08-14: dos robots navegan bajo namespace con mando aislado ([`S17_nav2_namespaces.md`](Documentos/Evidencia/S17_nav2_namespaces.md), resultado 4) |
| 2 | **Entorno con dos regiones navegables** en Gazebo | Mapas, navegación, relevo | S18 | 🟢 Cerrado 2026-08-14 con `primer_piso_dos_niveles.world` ([`S18_entorno_dos_niveles.md`](Documentos/Evidencia/S18_entorno_dos_niveles.md)) y **sustituido el 2026-08-20 por `mundo_definitivo.world`**, que modela el pasillo real de la USTA. Siguen siendo el piso 1 y el piso 2, pero **dibujados lado a lado en Y** en vez de apilados a 3,0 m. Ver D4 y la entrada del 20-ago en §7 |
| 3 | **Navegación autónoma punto a punto** con restricción Ackermann | Guiado, relevo, métricas | S19 | 🟢 Validada también en el nivel 2: 4,79 m sobre un objetivo de 5,0 m, con z constante a 1,9 µm |
| 4 | **Segundo agente + servidor de coordinación** | OE2, protocolo de relevo | S19–S20 | 🟢 **Cerrado 2026-08-25.** El segundo agente navega en su nivel (H3, 0,143 m contra `/odom`) y el servidor existe desde el 24-ago (`Robot/aws-deepracer/coordinacion/`), con **992 planes** origen–destino verificados sin simulador y el relevo escrito como función pura |
| 5 | **Protocolo de relevo** (máquina de estados) | OE4, núcleo del aporte | S21 | 🟢 **EJECUTADO el 2026-08-30** ([`S21_relevo_ejecutado.md`](Documentos/Evidencia/S21_relevo_ejecutado.md)). `piso1_representacion` → `piso2_lab_313` con los dos robots vivos: `exito: true`, **1 relevo**, 47,6 s, etapas `RECIBIDA`→`TRAMO_1`→`TRAMO_1`→`TRANSFERENCIA`→`TRAMO_2`→`COMPLETADA`. Llegadas contra `/odom` y no contra el `SUCCEEDED`: **0,128 m** y **0,077 m** contra un criterio de 0,25. Que los dos robots fueran *conducidos* y no solo *notificados* lo dice el bag: **373 y 580 mensajes** en sus `cmd_vel`. El bloqueo de dominios que lo impedía se levantó el mismo día ([`S21_bloqueo_dominios.md`](Documentos/Evidencia/S21_bloqueo_dominios.md)): un solo dominio, dos `gzserver`, separación por nombres. **Lo que este hito NO cierra:** la condición B sigue abierta, porque es **n = 1 y sin sortear** y el protocolo pide N = 30 |
| 6 | **Interfaz HRI** web sobre `rosbridge_suite` | OE3 | S22 | 🔴 No iniciado |
| 7 | **Instrumentación de métricas** | OE4, Fase 6 | S20–S21 | 🟡 **Registrador de misión operativo 2026-08-27** (RF-25). Esquema JSON versionado y comprobable, `herramientas/componer_registro.py` compone un registro validado desde un bag, y el veredicto se calcula contra `/odom`. **Ejercitado de extremo a extremo el 2026-08-29** con tres registros validados. **El banco de caracterización del tiempo de asignación está hecho y ejecutado** (2026-08-30): mediana 154–175 µs, máximo 306,4 µs. **La cadena queda cerrada el 2026-08-31**: `sortear_misiones.py` (§9.5) y `analizar_campana.py` (§9.6) son los **dos últimos ítems del §9** del protocolo, y con ellos existe el camino completo sorteo → corrida → bag → registro → métricas de OE4 con intervalo de confianza. El esquema del registro sube a **1.1.0** para dar campo a RF-24, que no lo tenía. **Falta** cerrar cuatro huecos medidos (ver la bitácora del 27-ago) y, sobre todo, **ejecutar** la campaña: el hito pasa a 🟢 en cuanto haya corridas sorteadas, no antes |
| 8 | **Bring-up de los dos DeepRacers** y despliegue del stack | Demostración física de OE2 | S21–S22 | 🟡 Custom car y ROS 2 Jazzy instalados; el LiDAR de fábrica **mide y publica** (2026-08-21) y **ya convive con la pila del vehículo desde el 2026-08-28** —enlace simbólico `rplidar_node` → `rplidar_composition`, a un coste medido de −6,6 % de frecuencia—, con teleoperación desde ROS 2 por `/ctrl_pkg/servo_msg`. **Falta:** `coordinacion_msgs` compilado en la tarjeta Jazzy, la cadena `/cmd_vel` real (sigue con sus dos defectos) y el **segundo** vehículo (R11) |

El hito 2 decía «réplica del laboratorio GED, dividida en dos zonas». [`ENTORNO_DE_EVALUACION.md`](Documentos/ENTORNO_DE_EVALUACION.md) §5 descartó el laboratorio como entorno de evaluación —16 m² en un solo nivel, sin discontinuidad vertical: la pregunta de investigación no se puede plantear ahí— y adoptó `primer_piso` replicado en dos niveles. El laboratorio sigue en el escalamiento como **etapa 2** (validación de hardware), no como entorno de evaluación.

---

## 3. Decisión de alcance vigente

**Estrategia adoptada (2026-08-05): la simulación produce la evidencia estadística; el hardware produce la demostración.** Sustituye a la versión del 2026-08-03. Desarrollo completo en [`Documentos/CRONOGRAMA_S17_S32.md`](Documentos/CRONOGRAMA_S17_S32.md) §4.

| ID | Decisión |
|----|----------|
| D1 | Las cuatro métricas de OE4 se miden en **simulación con N = 30**. Los vehículos físicos ejecutan el protocolo con N de 5 a 10, como demostración funcional. *Razón:* una tasa de éxito exige N. Con 5 corridas y 4 aciertos el IC del 95 % va de 38 % a 96 %; con 30 y 27 aciertos va de 80 % a 97 % |
| D2 | **Un piso es una región navegable con mapa y agente propios; la transición es un evento lógico.** En el esquema de relevo ningún robot cruza entre niveles, luego la frontera no necesita ser vertical. Se declara como simplificación explícita en el documento final |
| D3 | **Escalamiento en tres etapas.** (1) Simulación sobre `primer_piso_dos_niveles.world`: aporta la evidencia estadística. (2) **Laboratorio GED**: puerta de validación del hardware —los vehículos deben andar ahí antes de salir—, no entorno de evaluación. (3) **Pasillo real del primer piso USTA**: produce la evidencia física, porque es la geometría que el mundo simulado replica. *Revisado 2026-08-14* |
| D4 | El mundo simulado replica el **pasillo real del edificio**, no el laboratorio. Es lo que hace comparables los dos conjuntos de datos. **Revisado 2026-08-20:** el mundo vigente es `mundo_definitivo.world`. **Los dos pasillos siguen siendo el piso 1 y el piso 2 del edificio, uno encima del otro en la realidad**; lo que cambia es **cómo se dibujan**: van lado a lado en Y, ambos a z = 0, en vez de apilados a 3,0 m. Es una decisión de representación, no de alcance, y la autoriza **D2**: si un piso es una región navegable con mapa y agente propios y ningún robot cruza entre niveles, la separación vertical no cumple ninguna función. **Corregido el 2026-08-23:** la frase «ni siquiera comparten escena» era falsa y se midió. `ROS_DOMAIN_ID` aísla el **grafo de ROS**, no la **escena de Gazebo**: las 59 paredes estaban en un solo `.world`, el LiDAR simulado alcanza 10,0 m y entre los dos pasillos hay 5,44 m, de modo que desde 61 posiciones libres del piso 1 los rayos alcanzaban geometría del piso 2 —159 rayos, el peor caso a 6,48 m—, concentrados en el tramo norte-sur donde nace `robot1` y donde está el punto de transferencia. Apilarlos a z = 3,0 protegía de esto sin que nadie lo hubiera anotado como función. **La decisión se mantiene —los pisos siguen lado a lado, no apilados— pero ahora cada uno tiene su propio `.world`**, por extracción pura. La asignación está escrita en `herramientas/robot.sh`: `robot1` → piso 1 (tramo norte-sur), `robot2` → piso 2 (canal oeste) |
| D5 | La interfaz HRI es **web responsiva** sobre `rosbridge_suite`, accedida desde un teléfono. No es un recorte: §7.2 del anteproyecto lista "JavaScript, PHP, HTML" y "dispositivos móviles" |
| D6 | El código se escribe **una vez contra un contrato de interfaces ROS 2** y se despliega en dos destinos. Coordinación, relevo, HRI e instrumentación operan contra `/robot1/...` y `/robot2/...` sin conocer el backend. **Precisada el 2026-08-18 (R8 caracterizado):** vale como **portabilidad del código fuente** —se escribe una vez y compila en las dos distribuciones—, **no** como interoperabilidad en un mismo grafo ROS. Un coordinador Humble y un robot Jazzy no se hablan por `navigate_to_pose`, porque el tipo difiere. El despliegue son **dos mundos cerrados** —simulación Humble, hardware Jazzy—, que es como D1 ya los tenía repartidos: la campaña de N=30 en simulación, la demostración en hardware. Lo nuevo es que no pueden mezclarse, no solo que no hace falta |

> ✅ **Presentadas y en acta.** El entregable *Actividad 1 – Corte 1* —cronograma S17–S32, matriz
> objetivo–evidencia, hitos y desviaciones— lleva **constancia de revisión firmada por Armando
> Mateus con fecha 2026-08-14**, dentro del plazo (antes de S19). Su observación literal:
> *«Se ha desarrolla un buen avance. Uno de los DeepRacers está esta semana en intervención
> técnica.»* Archivo:
> [`Actividad_1_Corte_1_Cronograma_2026-2_firmado.pdf`](Documentos/Entregables/Actividad_1_Corte_1_Cronograma_2026-2_firmado.pdf).
>
> **Alcance exacto de lo firmado:** la constancia recoge **un solo director**. Ospina y Gélvez no
> firman este documento, y las decisiones D1–D6 no aparecen enumeradas en la constancia: lo que
> está avalado es el cronograma replanificado y la matriz objetivo–evidencia, que son el vehículo
> de esas decisiones. No se debe citar como aprobación de los tres directores.
>
> ⚠️ **El acta se firmó sobre una versión anterior del entorno.** Su fecha de corte es el 13 de
> agosto y el entorno de dos niveles se adoptó el 14. En consecuencia la actividad 12 del
> cronograma firmado dice «medir el laboratorio GED y modelar su planta en SDF», y la matriz
> objetivo–evidencia de OE1 cita `laboratorio_ged.world`. **Eso quedó superado por D3 y D4
> revisadas**: el entorno de evaluación es `primer_piso_dos_niveles.world`. No es un
> incumplimiento —el producto verificable existe y es mejor que el prometido— pero **debe
> comunicarse a los directores en el próximo corte**, no darse por sabido.

---

## 4. Riesgos abiertos

| ID | Riesgo | Impacto | Mitigación | Estado |
|----|--------|---------|------------|--------|
| R1 | **Ningún mapa obtenido es utilizable.** Mejor resultado: 41,9 % de cobertura en X y una extensión en Y de 16,5 m contra los 5,3 m reales del pasillo | **Degradado el 2026-08-05:** ya no bloquea. Afecta solo al pasillo USTA, que pasó a escenario secundario | **Causa raíz confirmada:** `max_laser_range` de slam_toolbox estaba en 12,0 m, por encima del alcance físico del LiDAR (10,0 m). Los rayos sin retorno volvían como 10,0 m, quedaban bajo el umbral y se rasterizaban como paredes fantasma en arcos de 10 m de radio. Corregido a 9,5 m el 2026-08-03 | 🟢 Fuera del camino crítico |
| R7 | **El código que se ejecuta no es el que está versionado.** `~/deepracer_sim_ws/src/aws-deepracer` era una copia independiente del repositorio, no un enlace | Las correcciones no surten efecto; hay trabajo que nunca llega al historial | **Se dio por cerrado el 2026-08-03 sin estarlo.** La verificación de entonces comprobó que `install/` resolvía a `src/` —cierto— pero no que `src/` fuera a su vez una copia. Lo era: un clon de `aws-deepracer/aws-deepracer.git` con cambios locales. Cerrado de verdad el 2026-08-05 sustituyendo la copia por un enlace simbólico al repositorio, con `colcon build` limpio (7/7) y comprobando que `readlink -f` de un archivo instalado apunta a la raíz del repositorio | ✅ Cerrado (2026-08-05) |
| R2 | **Ackermann vs. Nav2.** El controlador por defecto (DWB) asume tracción diferencial; el DeepRacer no gira sobre su eje | Trayectorias inejecutables | Resuelto en el stack publicado en `integracion-nav2`: Smac Hybrid-A\*, árboles de comportamiento propios sin `<Spin>`, `use_rotate_to_heading: false`, footprint real 0,28 × 0,19 m. **`REEDS_SHEPP` + `allow_reversing` verificado el 2026-08-18** ([`S19_conversion_cmdvel_ackermann.md`](Documentos/Evidencia/S19_conversion_cmdvel_ackermann.md)): el planificador *propone* la maniobra de volteo con su cúspide y el vehículo la *ejecuta*, las dos cosas, dibujadas desde el bag con `herramientas/graficar_plan.py`. En el camino apareció y se corrigió el defecto de conversión de `angular.z` (ver bitácora del 18-ago): RMS del giro real contra el comandado 0,298 → 0,111 rad/s | 🟢 Mitigado y verificado (2026-08-18) |
| R3 | **Ambigüedad de localización.** Pasillo largo de paredes lisas y repetitivas → AMCL propenso a divergir. **Precisado el 2026-08-26: no es «AMCL diverge», es que la posición longitudinal no es observable.** En un tubo uniforme de 2,10 m la posición lateral está perfectamente restringida y la longitudinal no lo está, y eso afecta por igual a las dos capas del sistema | **Falsos negativos en las métricas de OE4** —hoy se mediría mal la *tasa de éxito*— y, sobre todo, **falla la navegación**: tres corridas a ETM1 pararon a 0,767, 1,198 y 0,559 m contra una tolerancia de 0,25. **Y no es un problema solo de simulación:** el DeepRacer no tiene encoders de rueda, así que su única odometría es `rf2o_laser_odometry` (scan matching), que falla en el mismo eje que AMCL y sin ninguna medida independiente que lo delate | **Medido el 2026-08-26, ya no es una sospecha.** *(a) Capa AMCL:* con `/odom` publicado desde `model_->WorldPose()` la TF `map → odom` debería ser constante y derivó **1,977 m**, con dy nunca por encima de 0,026 m y σx = 1,325 m contra σy = 0,213 m. *(b) Capa odometría:* rf2o corrido fuera de línea sobre el bag `mision3` registra **5,7 %** y **1,3 %** del desplazamiento real en los dos tramos del pasillo — se congela, no deriva; el `yaw` sigue bien (2,8° final), o sea que el nodo funciona y lo que falta es información. **Mitigación deliberadamente aplazada:** el pasillo simulado es más pobre que el real, luego 5,7 % es un **piso** y no una estimación, y construir contra una magnitud desconocida sería adivinar. Se decide con la corrida recta de **≥ 20 m** del frente B de S20 (umbrales M1 y M2 del GO / NO-GO de S21). *(Anotado el 2026-09-01: **ese par de umbrales, tal como está escrito, no decide nada.** M1 ≥ 0,90 tolera un 10 % y el peor error jamás medido por el proyecto es el 5,7 % de esta misma celda; además está escrito a un solo lado, así que el **+2,9 % largo** del carro pasa por definición —y pasaría igual a +50 %—. Y M2 ≤ 0,50 m ya tiene resultado calculable antes de correrlo: 0,029 × 20 m = **0,58 m**, y por este mismo R3 AMCL no lo corrige en un pasillo uniforme. Corrección propuesta en [`S21_preparacion_G2.md`](Documentos/Evidencia/S21_preparacion_G2.md) §4: **\|registrado ÷ real − 1\| ≤ 0,10**, de dos lados. Y correr M2 igual, pero como **medida** y no como puerta: la pregunta abierta de verdad es cuánto recupera AMCL con la geometría real del pasillo, que tiene marcos de puerta y mobiliario que el simulado no tiene.)* Vías candidatas si M1 no pasa: IMU, odometría visual con las dos cámaras RGB, o aproximación final relativa a la hornacina de la puerta. **Mitigado EN SIMULACIÓN el 2026-08-27, y por una causa distinta de la que se creía:** los cinco `alpha` de AMCL estaban en `0.2`, o sea declarándole al filtro un 20 % de ruido de odometría sobre una fuente que en simulación **no es odometría** —`/odom` sale de `model_->WorldPose()`, exacta al milímetro—. Sin observabilidad longitudinal que lo corrigiera, ese ruido inventado se acumulaba como paseo aleatorio en X. Con `alpha 0.01` la tasa de deriva cae de **0,0343 a 0,0049 y 0,0044 m por metro recorrido** (~7×) y el peor error de AMCL de 1,473 m a 0,407 y 0,374 m, medido sobre dos misiones completas de dos tramos (`S20_rutas_03` y `_04`). **Esto no cierra el riesgo:** el arreglo consiste en decirle a AMCL la verdad sobre una fuente que **solo en simulación** es exacta; en el carro físico `/odom` sí es una estimación de `rf2o` y los `alpha` habrá que medirlos allí. La degeneración del pasillo sigue intacta — lo que se quitó es el ruido que la explotaba | 🟡 **Mitigado en simulación (2026-08-27)**, tras estar 🔴 confirmado con medida el 26-ago. **Sigue 🔴 para el vehículo físico**, pendiente de la corrida de ≥ 20 m |
| R4 | **Pared sur abierta en el SDF** deja celdas desconocidas | Afecta planificación, no solo el mapa | Cerrar la geometría en `primer_piso_v2.world` | 🟡 Identificado (desde S14) |
| R5 | **Fases 2 y 3 sin artefacto verificable.** No existía documento de requisitos | Imposibilita §7.4 del anteproyecto ("comparación con requisitos") | Emitido [`Documentos/REQUISITOS.md`](Documentos/REQUISITOS.md) el 2026-08-18: 34 requisitos numerados, cada uno con fuente citable —anteproyecto §4.2, §4.3 o `CONTRATO_INTERFACES.md`—, prueba y semana de construcción. **Se declara reconstrucción, no recuperación:** el anteproyecto nunca enumeró requisitos, y eso es precisamente lo que este riesgo registraba. 11 verificados, 4 parciales, 19 pendientes | ✅ Cerrado (2026-08-18) |
| R6 | **Discrepancias informe ↔ repositorio** (ver §5) | Credibilidad de la evidencia ante el jurado | Corregir en el entregable de cierre de fase | 🔴 Abierto |
| R8 | **Desajuste de distribución.** El stack de simulación es ROS 2 Humble; `deepracer-custom-car` sobre Ubuntu 24.04 es Jazzy. Los parámetros de Nav2 y varias API cambian entre ambas | Puede invalidar el supuesto de D6: que el mismo código se despliega en los dos destinos sin cambios | **Caracterizado el 2026-08-18** en [`S19_spike_p4_humble_jazzy.md`](Documentos/Evidencia/S19_spike_p4_humble_jazzy.md). Tamaño acotado: **6 diferencias, 37 líneas de YAML y 2 árboles de comportamiento** que hay que convertir de BT.CPP v3 a v4. Tres de las seis son **silenciosas** (no dan error). Se mantiene una variante de configuración por distribución, derivada de la de Humble. **Lo que no es de configuración:** `nav2_msgs/NavigateToPose` —la única vía de mando del contrato— tiene **distinta definición** en las dos distros, así que un coordinador Humble no habla con un robot Jazzy. La simulación **no puede** migrar a Jazzy: `gazebo_ros2_control` no está liberado ahí | 🟡 **Caracterizado (2026-08-18)**, mitigación pendiente de aplicar |
| R10 | **El mapa por defecto de Nav2 no pasa la verificación del propio proyecto.** `herramientas/verificar_mapa.py` **rechaza** los dos mapas de `primer_piso` contra su `.world`: cobertura en X del 35,5 % (mínimo 85 %), extensión en Y de 15,0 m contra los 5,3 m reales y 62,8 % de celdas desconocidas | La instalación queda verificada y compila, pero cualquier métrica de navegación obtenida sobre este mapa es discutible ante el jurado. No bloquea el bring-up ni la capa de coordinación | Resuelto por otra vía que la prevista: en vez de rehacer el SLAM, `herramientas/generar_mapa_desde_mundo.py` deriva el mapa de la geometría del `.world` (commit `35c47da`). El mapa vigente es `primer_piso_definitivo`, y `python3 herramientas/verificar_mapa.py Robot/aws-deepracer/deepracer_bringup/maps/primer_piso_definitivo.yaml primer_piso_v2.world` devuelve **0**: cobertura 99,7 % en X y 100,8 % en Y, 14,5 % de celdas desconocidas y **0 de 5149 obstáculos sin pared real** a menos de 0,30 m. El entorno que importa para OE4 es ahora `primer_piso_dos_niveles.world` (hito 2), y **no necesita un mapa propio**: los dos niveles son la misma planta en las mismas x-y, así que `primer_piso_definitivo.yaml` sirve para ambos, y así se validó el 2026-08-14 | 🟢 Cerrado 2026-08-14 (detectado 2026-08-11) |
| R11 | **Uno de los dos DeepRacers está en intervención técnica.** Consta por escrito en la observación del director en el acta del 2026-08-14: *«Uno de los DeepRacers está esta semana en intervención técnica»*. No se sabe cuál vehículo, qué falla ni cuánto dura | Bloquea la **pregunta 3 del spike** (latencia de ida y vuelta entre los dos vehículos, exige los dos encendidos en la misma red) y la actividad de **paridad entre vehículos** de S19. No bloquea las preguntas 1, 2 y 4, que se responden con **un solo** vehículo | Reordenar el spike: correr primero las preguntas 1, 2 y 4 sobre el vehículo disponible y dejar la 3 supeditada a la reparación. Jonny debe declarar por escrito qué vehículo es, qué se intervino y en qué fecha vuelve a estar operativo | 🔴 **Abierto (2026-08-14)**, sin caracterizar |
| R12 | **El verificador de meta de Nav2 cuenta como fallo llegadas que fueron buenas.** `SimpleGoalChecker` corre en `stateful: True`: cumplida la tolerancia de posición deja de comprobarla y solo exige el **rumbo**, que un Ackermann no puede cerrar girando sobre su eje | **Bloquea la campaña de OE4**, que es donde se mide *tasa de éxito*: dos corridas del 18-ago terminaron en `ABORTED` y `CANCELED` maniobrando en el sitio, con **80 cúspides ejecutadas contra 2 planificadas**, tras haber llegado a la posición pedida | Detectado el 2026-08-18 con evidencia gráfica ([`S19_llegada_abortada_rumbo.png`](Documentos/Evidencia/S19_llegada_abortada_rumbo.png), [`S19_llegada_cancelada_rumbo.png`](Documentos/Evidencia/S19_llegada_cancelada_rumbo.png)). **Decidido el 2026-08-22, antes de instrumentar** ([`PROTOCOLO_EXPERIMENTAL.md`](Documentos/PROTOCOLO_EXPERIMENTAL.md) §4): el rumbo **no** decide el éxito —el criterio es posición contra `/odom`— y `stateful` pasa a `False`. **Decidido no es aplicado:** el YAML sigue en `True` y el cambio no vale hasta repetir los tres destinos del 21-ago comparando cúspides contra aquel registro (§9.1). Cambiarlo sin medir sería un arreglo declarado sin evidencia. **No es determinista (2026-08-21):** el objetivo `piso1_escalera`, elegido justamente porque obliga a la maniobra de volteo, cerró el rumbo a **5,65°** y devolvió `SUCCEEDED`. Que no se dispare siempre lo hace **peor** de cara a OE4, no mejor: un fallo intermitente en la métrica de tasa de éxito no se distingue de un fallo del sistema medido. **APLICADO EL 2026-08-24, con la evidencia que faltaba:** una misión del coordinador a ETM1 dio las **dos cifras de la misma llegada** —posición 0,413 m (fuera de 0,25) y rumbo 0,200 rad (dentro de 0,25)—, que es lo que ninguna corrida anterior había dado. El mecanismo queda cerrado: el carro llegaba en sentido contrario al `yaw` pedido, maniobraba 180° para cerrarlo y **la maniobra lo apartaba del punto** mientras el checker ya solo miraba el rumbo. Se aplica `stateful: False` **y además** `yaw_goal_tolerance: 3.15` (~π), o sea que el rumbo final deja de imponerse: no basta con volver a comprobar la posición si se sigue exigiendo una orientación que obliga a la maniobra que la estropea. El `yaw` de `puntos_interes.yaml` pasa a orientativo. Misión repetida con `exito: true`. **Sigue 🟡 y no 🟢** porque falta la comparación de cúspides contra el registro del 21-ago (§9.1), que es lo que convierte el arreglo en medida | 🟡 **Mitigado (2026-08-24)**, detectado el 2026-08-18; cierra con la comparación de cúspides |
| R13 | **El LiDAR simulado es ciego justo hacia adelante.** `deepracer.xacro` declara el barrido de −150° a +150° —pese al nombre `lidar_360_degree_*`, son 300°— y `hokuyo_joint` monta el sensor girado 180°, así que el cono ciego de 60° cae sobre el **morro**. En un vehículo que no gira sobre su eje, eso es la dirección de marcha | Dos frentes. **OE4:** el sensor real de fábrica sí da 360 muestras sobre 360° (medido el 2026-08-21), de modo que sim y hardware **no son comparables** en esta variable, y la comparación es lo que sostiene la validez externa de la campaña. **Navegación:** un obstáculo en la trayectoria no entra en `/scan` hasta quedar de costado, y `ETM10` es el único destino que se aborda de frente | Medido el 2026-08-23 desde tres poses de yaw distinto con `robot.sh <robot> lidar`; el cono sigue al morro en las tres. **Sin decidir**: cambiar el URDF a 360° acerca la simulación al hardware pero **invalida la comparación con las corridas anteriores**, y hay ya una decisión abierta del 21-ago sobre el número de muestras (600 en sim contra 360 reales). Las dos se deciden juntas o se contradicen. Antes de tocar nada hay que medir si Nav2 y AMCL se degradan de verdad. **Medido el 2026-08-24 y sale que NO:** en `piso2_escalera`, el peor caso disponible —se llega de frente con yaw = π, así que el cono ciego apunta a `Barrera_Escalera`, que es la única superficie nueva de esa zona del mapa—, AMCL localiza con **3,6 cm** de error contra `/odom` y la varianza cae de 0,44 a 0,05 m² durante el trayecto. El error de llegada, 0,190 m, es casi todo de **control**, no de localización. Eso **no cierra el frente de OE4** —sim y hardware siguen sin ser comparables en esta variable— pero sí quita la urgencia de tocar el URDF: la mitigación deja de ser un arreglo y pasa a ser una decisión de validez externa | 🟡 **Acotado (2026-08-24)**: no degrada la navegación; sigue abierto como amenaza a la comparación sim ↔ hardware |
| R9 | **Capacidad insuficiente.** El trabajo pendiente suma 9–10 semanas-persona y el calendario ofrece 7 | Se llega a octubre sin capa de coordinación, que es el aporte declarado | Reparto en dos frentes entre S18 y S21, y escalera de recortes en §9 del cronograma. Punto de decisión go/no-go en S21 | 🟡 Mitigado por planificación |

---

## 5. Discrepancias detectadas entre informes y artefactos

Registradas el 2026-08-03 para corrección explícita:

> **Auditoría interna del 2026-08-29.** Este tablero se contradecía a sí mismo en cinco puntos, y se
> anota porque la fuente única de verdad que se desmiente sola no sirve de nada. Corregidos en esta
> misma pasada: (1) OE1 decía que el piso 2 «hoy solo tiene la escalera», falso desde el 26-ago;
> (2) OE1 decía «240 planes verificados» y el hito 4 decía **992** —992 es el correcto—; (3) OE4
> declaraba el tiempo de asignación «estructuralmente inmedible», corregido el 29-ago; (4) OE4 hablaba
> de «seis prerrequisitos del §10», que son **siete** desde el 27-ago; y (5) el hito 8 decía que el
> LiDAR «todavía no convive con la pila», contradiciendo a OE2 en el mismo documento, que lo daba por
> cerrado el 28-ago. Las cinco eran texto rancio, no medidas equivocadas.

| Informe dice | Artefacto en disco | Acción |
|---|---|---|
| S15: mapa guardado como `primer_piso_mapa.pgm/.yaml` vía servicio `/slam_toolbox/save_map` | `primer_piso.pgm/.yaml`; la guía operativa instruye `map_saver_cli` | Unificar nombre y procedimiento |
| S15 Tabla 1 y §5: `resolution = 0.05` m/celda | `primer_piso.yaml` declara `resolution: 0.06` | Corregir el informe o regenerar el mapa |
| S15 §3.4: "la extensión del mapa es consistente con las dimensiones del pasillo (44 m × 5 m)" | El mapa cubre 23,0 m × 15,7 m | Afirmación no sostenida por el artefacto |
| S15 Tabla 2: "Captura del quiebre en L — Satisfactorio" | No es identificable en el mapa guardado | Reevaluar tras repetir el mapeo |
| S15 Tabla 1: `minimum_travel_distance = 0.5`, `minimum_travel_heading = 0.5` | El archivo declaraba `0.1` y `0.57` | Corregir el informe con los valores vigentes |
| S15 §3.1: "modo *online asynchronous*" | El launch instancia `sync_slam_toolbox_node` (nodo **síncrono**) | Corregir el informe |
| S15 §3.4: las aperturas de la pared sur explican las celdas desconocidas | La causa real es deriva de pose por parámetros mal dimensionados | Rehacer el análisis de la sección |
| Bitácora 2026-08-03: `max_laser_range` 12,0 → 9,5 y buffers de SLAM reajustados al pasillo | El workspace en ejecución conservaba `max_laser_range: 12.0`, `scan_buffer_maximum_scan_distance: 0.5` y `link_scan_maximum_distance: 0.75` | Los arreglos existían **solo en el repositorio**; el simulador nunca los ejecutó (ver R7). Corregido el 2026-08-05. **Todo resultado de mapeo anterior a esa fecha se obtuvo con la configuración vieja y no puede citarse como evidencia de los parámetros corregidos** |

---

## 6. Desviaciones respecto al anteproyecto

Deben justificarse por escrito en el documento final:

| Anteproyecto | Realidad | Justificación |
|---|---|---|
| §4.3: pruebas "en un entorno interior con multiples pisos" | **En simulación no hay desviación de alcance: los dos pasillos de `mundo_definitivo.world` son el piso 1 y el piso 2 del edificio**, cada uno con su agente (`robot1`, `robot2` en `herramientas/robot.sh`). Lo que se declara es una decisión de **representación**, revisada el 2026-08-20: se dibujan **lado a lado en Y, ambos a z = 0** en vez de apilados a 3,0 m, porque ningún robot cruza entre niveles (D2) y cada uno corre en su propio `gzserver`, de modo que el desplazamiento vertical no cumplía función alguna. `primer_piso_dos_niveles.world`, que sí los apila, se conserva en el repositorio. **En la parte física la desviación es la de siempre:** las corridas con vehículo real ocurren en un único piso —el pasillo del primer piso USTA— porque no hay forma de subir un DeepRacer por una escalera | Corregido el 2026-08-14 y revisado el 2026-08-20; la redacción anterior («laboratorio GED, un solo nivel dividido en dos zonas») describía un entorno que se descartó. La justificación se estrecha y se refuerza: el aporte del proyecto es que **ningún robot cruza entre niveles** —la transición es un relevo, no un desplazamiento—, de modo que la campaña física de un solo piso sigue ejercitando el protocolo completo salvo el traslado vertical del vehículo, que el diseño no contempla. La campaña de N = 30 con los dos niveles reales corre en simulación (D1) |
| §4.3: "una aplicación móvil que actuará como interfaz HRI" | Interfaz web responsiva sobre `rosbridge_suite`, accedida desde el navegador de un teléfono | §7.2 lista como software del proyecto "JavaScript, PHP, HTML" y como hardware "dispositivos móviles": la pila declarada era web desde el anteproyecto. OE3 exige selección de origen y destino y eso se cumple íntegro. **Aun así es una desviación y debe declararse**: no es una aplicación nativa |
| Plataforma DonkeyCar (presupuesto: chasis, PCA9685) | AWS DeepRacer | Pendiente de redactar |
| Simulador CoppeliaSim | Gazebo Classic 11 | Pendiente de redactar |
| ROS 2 "Humble o Jazzy" | Humble | Cerrar la ambigüedad |

---

## 7. Bitácora de decisiones

> Esta tabla arranca el 2026-08-03, cuando se adoptó `ESTADO.md` como tablero único. Lo
> anterior —cómo se levantó el stack Nav2 + AMCL, hasta el 17 de julio— está en
> [`Documentos/DESARROLLO_NAV2.md`](Documentos/DESARROLLO_NAV2.md), **cerrado como registro
> histórico**: su cabecera lista qué afirmaciones suyas quedaron refutadas y dónde está cada
> corrección. No es referencia de configuración.

| Fecha | Decisión | Motivo |
|-------|----------|--------|
| 2026-08-03 | Se restablece el vínculo git del directorio local con el repositorio remoto | El directorio de trabajo era una copia sin historial; `primer_piso_v2.world` (18-jun) llevaba 7 semanas sin respaldo |
| 2026-08-03 | Se adopta `ESTADO.md` como tablero único de trazabilidad | Los informes semanales registraban actividad, pero no el estado agregado frente a los objetivos |
| 2026-08-03 | Alcance: validación en simulación, hardware como demostración | Ver §3 |
| 2026-08-03 | Se reajustan los parámetros de `slam_toolbox.yaml` al tamaño real del entorno | `scan_buffer_maximum_scan_distance` (0.5→10.0), `link_scan_maximum_distance` (0.75→10.0) y `loop_search_maximum_distance` (3.0→8.0) eran valores de una pista de carreras; impedían enlazar nodos y cerrar bucle en un pasillo de 58 m |
| 2026-08-03 | Se incorpora `herramientas/verificar_mapa.py` como paso obligatorio antes de guardar un mapa | Ningún mapa vuelve a declararse "satisfactorio" sin verificación objetiva de cobertura y deriva |
| 2026-08-03 | `max_laser_range` de slam_toolbox: 12,0 → 9,5 m | Debe quedar por debajo de los 10,0 m del LiDAR (`deepracer.xacro`). Con 12,0 los rayos sin retorno se tomaban como impactos válidos y generaban paredes inexistentes |
| 2026-08-03 | `map_saver_cli` requiere `--ros-args -p use_sim_time:=true` en simulación | Sin ese parámetro agota su espera de 2 s y falla con `Failed to spin map subscription`. Diagnóstico por descarte: `/map` publicaba a 2 Hz con QoS `TRANSIENT_LOCAL` correcta |
| 2026-08-03 | Se declaran en `deepracer_bringup/package.xml` las dependencias de ejecución que estaban implícitas | `slam_toolbox`, `nav2_map_server`, `gazebo_ros` y otras se lanzaban sin declararse: `rosdep install` no las instalaba en un equipo nuevo |
| 2026-08-03 | README reescrito con procedimiento de instalación reproducible | El workspace debe enlazar el repositorio, no copiarlo, para que el código compilado sea el versionado |
| 2026-08-04 | El trabajo Nav2 + AMCL se publica en la rama `integracion-nav2` (commit `1242651`) sobre `origin/main` | Siete semanas de avance vivían sin commitear. Se resolvió por rama y no sobre `main` porque el historial local diverge del remoto y la rama es compartida con Jonny |
| 2026-08-05 | `deepracer-custom-car` instalado en Raspberry Pi 4 y en la tarjeta original del DeepRacer; locomoción verificada por interfaz web | Primer avance de OE2 sobre hardware real. Sube OE2 de 25 % a 40 %. Evidencia audiovisual: <https://youtu.be/ZGfAMnC4lYY> |
| 2026-08-05 | Se replanifica S17–S32 y se emite `Documentos/CRONOGRAMA_S17_S32.md` | Entregable exigido por el espacio académico Proyecto de Grado 2. La sustentación quedó en S28–S29 y el documento final se traslada después de ella, lo que saca la redacción del camino crítico |
| 2026-08-05 | Entorno experimental: laboratorio GED en lugar del pasillo USTA de dos pisos | El anteproyecto pide "entorno interior controlado" (OE4) y "condiciones controladas" (§4.3). Elimina la dependencia de permisos de acceso y retira el riesgo R1 del camino crítico |
| 2026-08-05 | El bring-up físico completo se mantiene en S21–S22, pero se antepone un **spike acotado de 3–4 días en S18** | El beneficio de adelantar el hardware es obtener información, no avance, y eso cuesta días y no semanas. Adelantar el bring-up entero arriesga llegar a octubre con dos robots andando y sin capa de coordinación que sustentar |
| 2026-08-05 | El trabajo se reparte en dos frentes entre S18 y S21 | El trabajo pendiente suma 9–10 semanas-persona y el calendario ofrece 7 semanas. Sin reparto no cierra |
| 2026-08-05 | Se emite `Documentos/CONTRATO_INTERFACES.md` (hito H1) | Congela namespaces, marcos TF, acciones y tipos de mensaje antes de escribir el nodo de coordinación y la HRI. Materializa D6: un solo código contra dos backends |
| 2026-08-05 | Cada robot tiene su propio árbol TF con raíz `robotN/map`; **no** hay marco global compartido | Ningún robot cruza entre niveles (D2): la transición es un evento lógico del protocolo de relevo, no un movimiento. Un `map` compartido resolvería un problema que el sistema no tiene y complicaría el bring-up |
| 2026-08-05 | El coordinador manda a los robots **únicamente** por la acción `navigate_to_pose`; nunca publica `cmd_vel` | Es lo que hace intercambiable un robot físico con uno simulado sin tocar el coordinador |
| 2026-08-05 | La HRI habla solo con `/coordinacion`, nunca con los robots | Mantiene mínima la superficie de `rosbridge` y permite reasignar robots sin tocar el frontend |
| 2026-08-05 | Se elimina el `static_transform_publisher` `base_link`→`camera_link` de `deepracer_spawn.launch.py` | Disputaba el marco `camera_link` con el URDF: en TF2 un marco solo admite un padre. Las dos transformadas **no coincidían** (15,39 mm de diferencia; rotación idéntica). En simulación manda el URDF, porque es lo que Gazebo usa para colocar el sensor. Era una copia del launch del robot físico, donde sí es la única fuente de TF |
| 2026-08-05 | `~/deepracer_sim_ws/src/aws-deepracer` pasa de copia a enlace simbólico al repositorio | Cierre real de R7. La copia era un clon de `aws-deepracer/aws-deepracer.git` con cambios sin commitear, y llevaba retrasada respecto al repositorio en los 6 archivos que diferían. `colcon build` limpio, 7/7 |
| 2026-08-05 | Se instrumenta la verificación con `herramientas/verificar_contrato.py` | El criterio de cierre de H1 pasa de afirmación a comprobación ejecutable. Su primera ejecución detectó por sí sola el conflicto de `camera_link` |
| 2026-08-05 | El namespaceado se aplica por pasos, cada uno con valor por defecto vacío y prueba previa de que **no cambia nada** | Separa el error de mecánica del error de diseño: si con el defecto vacío algo se rompe, la causa es la mecánica. Bitácora de aplicación en [`Documentos/Evidencia/S17_aplicacion_contrato.md`](Documentos/Evidencia/S17_aplicacion_contrato.md) |
| 2026-08-05 | Los marcos `odom`, `base_link` y `laser` se prefijan desde el xacro y no desde `robot_state_publisher` | Los escriben plugins de Gazebo, que ignoran el parámetro `frame_prefix`. Los otros 10 marcos del URDF sí los prefija `robot_state_publisher` |
| 2026-08-10 | Nav2 y AMCL quedan bajo espacio de nombres: `deepracer_localization_sim.launch.py` propio en vez de `nav2_bringup/localization_launch.py` | El de Nav2 no namespacea sus nodos —solo anida el YAML— y remapea `/tf` de forma fija, lo que parte el árbol TF bajo namespace. Evidencia en [`Documentos/Evidencia/S17_nav2_namespaces.md`](Documentos/Evidencia/S17_nav2_namespaces.md) |
| 2026-08-11 | El trabajo de `integracion-nav2` se publica en `main` y se etiqueta `v0.1-simulacion-verificada` | Néstor (codirector) necesita una versión instalable. `main` es lo que obtiene un clon sin argumentos y por eso debe funcionar siempre; el tag fija un punto reproducible. Comprobado clonando desde GitHub en limpio: `rosdep` sin faltantes, `6 packages finished`, verificador 30/0 |
| 2026-08-11 | Se declara en `deepracer_bringup/package.xml` la dependencia real `deepracer_drive_plugin` (decía `deepracer_gazebo`, que no existe) | `rosdep install` abortaba en un equipo nuevo. El paquete se llamaba así en el proyecto original de AWS |
| 2026-08-11 | Se incorpora `herramientas/verificar_instalacion.sh` (30 comprobaciones, sin levantar Gazebo ni nodos) | Unas instrucciones de instalación se prueban una sola vez, en el equipo de quien las escribió, donde todo ya funcionaba. Cada fallo imprime el comando que lo corrige |
| 2026-08-11 | La raíz del repositorio se **deduce** de la ubicación real de cada archivo (`realpath`), y se eliminan los candidatos adivinados bajo `$HOME` | Con ruta fija, en un equipo que clonara en otro sitio Gazebo abría un mundo VACÍO sin error; y si existía otra copia en la ruta adivinada, se cargaban en silencio los mundos del clon equivocado. Pasó de verdad: el launch resolvió a una copia de `~/Tesis` del 4-ago |
| 2026-08-11 | Se incorpora `herramientas/verificar_repositorio.sh`: 10 comprobaciones sobre los **documentos**, no sobre el código | Había 30 comprobaciones sobre el código y ninguna sobre la documentación: por eso el código convergía y los documentos divergían. Detecta rutas de una máquina concreta, instrucciones que mandan copiar el workspace, archivos citados que no existen, enlaces rotos y contradicciones entre el README, los launch y `ESTADO.md` |
| 2026-08-11 | Se retiran de las guías y planes las 14 rutas con el nombre de usuario incrustado, y los dos bloques `rsync` de `guia_navegacion_nav2.md` | El `rsync` reproducía el incidente R7: sincronizar al workspace lo que ya está enlazado. Una versión descargada tiene que funcionar entera, sin depender de dónde se haya clonado |
| 2026-08-11 | `deepracer_sim.launch.py` deja de exigir `world:=` a mano y la deducción de la raíz pasa a un solo módulo, `launch/deepracer_raiz_repo.py`, que ambos launch importan | Escribir la ruta absoluta en cada lanzamiento hacía trivial mapear contra la geometría de **otra** copia del repositorio sin enterarse; pasó el 2026-08-11 con `~/Tesis`, un clon del 4-ago. La lógica va en un módulo y no copiada en cada launch: dos copias de la misma regla divergen sin avisar |
| 2026-08-11 | **Mundo vigente: `primer_piso_v2.world`**, con `primer_piso_v2.yaml` como mapa por defecto de Nav2 | El README lo declaraba vigente mientras el launch cargaba `primer_piso.world` con el mapa de v1. Mundo y mapa van en pareja: descasarlos hace que AMCL localice contra una geometría distinta de la simulada, y la deriva resultante no se parece a un error de configuración |
| 2026-08-12 | Todo consejo que modifica `~/.bashrc` se emite como **comando pegable en su propia línea**, con guarda `grep -qxF` y el valor entrecomillado | Al instalar en un segundo equipo el `~/.bashrc` quedó inservible: dos órdenes pegadas en una línea (`export ... source ...` → `not a valid identifier`) y la misma línea repetida hasta cuatro veces. Dos causas: el verificador daba el consejo como prosa dentro de un párrafo, imposible de copiar de un tirón, y el paso 5 del README no comprobaba antes de añadir. La revisión encontró además que un rótulo `Ejecutar: ` **delante** del comando reproducía el fallo —al arrastrarlo, el `\|\|` toma el error como "no estaba" y añade igual, una vez por intento— y que un `grep -q` sin `-x` daba por buena una línea ya corrupta, de modo que **no reparaba** el archivo de quien más lo necesitaba |
| 2026-08-12 | **El mapa por defecto de Nav2 pasa a `primer_piso_definitivo.yaml`, derivado de la geometría del `.world`** con `herramientas/generar_mapa_desde_mundo.py`, en vez de un mapa construido con SLAM | `primer_piso_v2.pgm` tenía **2688 de 3618 celdas ocupadas (74 %) sin ninguna pared real detrás**: paredes que el SLAM se inventó por su propia deriva. Como el planificador trata esas celdas como espacio letal, Nav2 abortaba con `Starting point in lethal space` en tramos del pasillo que están libres. No se ve mirando el mapa; se ve mucho después y con otro nombre. El mapa nuevo pasa `verificar_mapa.py` con 99.7 % de cobertura y **0 obstáculos falsos**, y la deriva de AMCL medida contra `/odom` bajó de 0.323 m a 0.045–0.100 m |
| 2026-08-12 | Un `SUCCEEDED` de Nav2 **no** se acepta como evidencia: toda medida de navegación se contrasta con `/odom`, que el plugin calcula con `model_->WorldPose()` | `Reached the goal!` lo emite el controlador comparando contra la pose que le da AMCL. Con el mapa inventado, Nav2 devolvía `SUCCEEDED` sobre una pose que no era la real: el robot no había llegado, y en un caso ni se había movido. La verdad del simulador es lo único que no depende de lo que se está midiendo |
| 2026-08-12 | AMCL recibe la pose de spawn (`x`, `y`, `yaw`) desde el launch, y los costmaps reciben el `scan` con el namespace ya resuelto | Dos defectos que solo aparecían con namespace y ninguno daba error. (1) `initial_pose: [0.0, 0.0, 0.0]` era una **línea muerta**: AMCL declara `initial_pose.x/.y/.z/.yaw` por separado y ROS descartaba la lista en silencio (`ros2 param get /amcl initial_pose` → `Parameter not set`), así que un robot lanzado con `y:=2.0` arrancaba con 2 m de error y AMCL arrastraba la nube de partículas al avanzar. (2) `topic: /scan` es absoluto y Nav2 en Humble **no** resuelve namespaces en las capas del costmap —el propio `nav2_bringup` lo esquiva escribiendo `/robot1/scan` a mano en un YAML por robot—, de modo que los dos costmaps se suscribían a un topic sin publicador y el robot solo esquivaba lo que ya estaba en el mapa estático. Verificado: `/robot1/scan` pasó de 1 a 3 suscriptores |
| 2026-08-14 | El entorno de evaluación pasa a ser **`primer_piso_dos_niveles.world`**: `primer_piso` extraído a modelo reutilizable e instanciado dos veces, a z=0 y z=3,0, con losa propia para el nivel 2 y la zona de transición en (41,40 · 3,03) | El laboratorio GED no sirve como entorno de evaluación (16 m², un solo nivel, sin discontinuidad vertical). Replicar la planta con desplazamiento vertical **no es una simplificación**: el segundo piso real del edificio es idéntico al primero, y el recinto del que se dedujo geométricamente la zona de transición resultó ser el descanso de la escalera real. Validado navegando en el nivel 2: 4,79 m sobre un objetivo de 5,0 m con la z constante a 1,9 µm — necesario porque las dos plantas son idénticas y el LiDAR no las distingue: un robot caído al nivel 1 daría el mismo `/scan` |
| 2026-08-14 | **Se descarta mapear el nivel 2.** El mapa vigente sirve para los dos niveles | Un segundo mapa de la misma geometría solo puede divergir del primero. Vale mientras las dos plantas sean idénticas, que es el caso real del edificio |
| 2026-08-14 | `herramientas/generar_mapa_desde_mundo.py` solo cuenta como pared las cajas dentro de `<collision>` | Tomaba el primer `<box>` del enlace fuera cual fuera su contenedor, de modo que un enlace con visual y sin colisión —la zona de transición— habría salido como obstáculo en el mapa. Un obstáculo fantasma no da ningún error: AMCL converge y Nav2 acepta objetivos igual; el síntoma aparece después como una ruta que rodea algo que no existe. Comprobado que no altera el mapa vigente: mismas 12 paredes |
| 2026-08-14 | Los bloques `<model>` y `<state>` de `primer_piso_v2.world` **no se contradecían** | En `35c47da` se registró una discrepancia de ~21 m entre ambos. No la hay: las poses de `<state>` son absolutas y las de `<model>` relativas al modelo; restando la pose del modelo coinciden en los doce muros con residuo < 0,1 mm. Lo desplazado era el bloque entero, no la geometría. Importa porque de ahí salía la duda de si el mapa correspondía a lo simulado: sí corresponde |
| 2026-08-14 | **Acta firmada.** Armando Mateus emite constancia de revisión sobre *Actividad 1 – Corte 1*: cronograma S17–S32, matriz objetivo–evidencia, nueve hitos y tabla de desviaciones | Cierra el pendiente de §3 dentro del plazo (antes de S19) y cubre la actividad 5 del cronograma. Deja dos cosas registradas: que la firma es de **un** director y no de los tres, y que el documento firmado tiene fecha de corte del 13-ago, un día antes de adoptar el entorno de dos niveles — su actividad 12 y su matriz de OE1 siguen citando el laboratorio GED |
| 2026-08-14 | Se registra como riesgo **R11** que uno de los dos DeepRacers está en intervención técnica | Lo declara el director por escrito en el acta. Cambia el orden del spike de S19: las preguntas 1, 2 y 4 se responden con un solo vehículo; la 3 —latencia entre vehículos— exige los dos y queda supeditada a la reparación. Sin registrarlo, el spike se habría planificado suponiendo dos vehículos disponibles |
| 2026-08-15 | **D3 y D4 revisadas.** El laboratorio GED deja de ser el entorno de evaluación y pasa a ser la etapa 2 del escalamiento —puerta de validación del hardware—; la evidencia física la produce el pasillo real del primer piso USTA, que es la geometría que el mundo simulado replica | `ESTADO.md` §3 y §6 todavía describían el laboratorio como entorno de pruebas y como lo replicado en Gazebo, mientras `CRONOGRAMA_S17_S32.md` §4 ya decía lo contrario desde el 14-ago. Dos documentos del mismo proyecto afirmando cosas distintas sobre dónde se mide es exactamente lo que el jurado encuentra |
| 2026-08-16 | Se versiona la configuración de RViz con la que se tomó la evidencia de S17 (`nav2_robot1_view.rviz`), citada desde `S17_nav2_namespaces.md`, y **con sus herramientas republicando en `/robot1/initialpose` y `/robot1/goal_pose`** | Estaba sin versionar: la captura del informe no se podía reproducir sin rearmar el panel a mano. Al revisarla antes de commitearla apareció que *2D Pose Estimate* y *2D Nav Goal* apuntaban a los tópicos globales —RViz los escribe así por defecto—, que bajo namespace no escucha nadie: el clic se publica y no pasa nada, sin error. El contrato de interfaces fija `/<ns>/initialpose`. Un archivo de configuración que ningún documento cita se queda obsoleto en silencio, así que entra citado o no entra |
| 2026-08-16 | El README declara los **dos** mundos y para qué sirve cada uno: `primer_piso_v2.world` como defecto de los launch y `primer_piso_dos_niveles.world` como entorno de evaluación de OE4 | El README solo nombraba el vigente, y `verificar_repositorio.sh` lo daba por coherente porque únicamente compara el README contra la constante del código: los dos decían `v2` y la comprobación pasaba. Mientras tanto `ESTADO.md` declaraba desde el 14-ago que el entorno que importa para OE4 es el de dos niveles, que no aparecía en el README ni una vez. Quien clona el repositorio lee el README, no el tablero |
| 2026-08-16 | **`Documentos/DESARROLLO_NAV2.md` se cierra como registro histórico** (cubre hasta el 17-jul), con una cabecera que enumera sus afirmaciones refutadas, y queda enlazado desde esta bitácora | Era una segunda bitácora en paralelo a esta, sin enlazar desde ningún documento y sin tocar desde hace un mes. Dos registros del mismo proyecto no se mantienen los dos. Y no era inofensivo: su §3.2 enseña `initial_pose: [0.0, 0.0, 0.0]` afirmando que AMCL lo soporta —la línea muerta que costó dos metros de error y se corrigió el 12-ago— y da por verificada la navegación con un `SUCCEEDED`, que desde el 12-ago no se acepta como evidencia. Quien lo leyera reintroduciría los dos defectos |
| 2026-08-16 | La cabecera de `herramientas/generar_mapa_desde_mundo.py` deja de afirmar que `<model>` y `<state>` difieren en 21 m, y explica que están en marcos distintos | La corrección del 14-ago llegó al tablero y no a la herramienta. El código siempre estuvo bien —compone la pose del modelo con la del enlace—, pero su documentación seguía enseñando la discrepancia que el propio proyecto había desmentido. Comprobado que el arreglo no altera nada: el `.pgm` regenerado es byte a byte idéntico al versionado |
| 2026-08-16 | `verificar_repositorio.sh` gana una comprobación: **ningún documento ni configuración puede quedar sin que nadie lo cite** (`.md` y `.rviz`); pasa de 10 a 11 | El verificador miraba los enlaces solo desde el lado del que apunta, así que un archivo al que no llega ningún enlace le era invisible. Ese fue el agujero exacto por el que se colaron los dos defectos de hoy: `nav2_robot1_view.rviz` sin versionar desde S17 y `DESARROLLO_NAV2.md` un mes sin tocar afirmando cosas ya refutadas. La comprobación nueva los habría señalado a los dos. Al estrenarla apareció un tercero, `urdf_config.rviz`, que ningún launch cargaba: queda citado en `Robot/README.md` con su comando |
| 2026-08-16 | **`generar_mapa_desde_mundo.py` resuelve los `<include>` y corta por altura** (`--altura`, 0,30 m por defecto); `verificar_mapa.py` acepta la altura como tercer argumento | Sobre `primer_piso_dos_niveles.world` encontraba **1 pared en vez de 12**: la planta entra por `<include>` y el programa solo miraba los `<model>` escritos en el propio `.world`. Al arreglarlo apareció el segundo defecto, que era el grave: un mapa 2D es un **corte**, no una sombra. Proyectando todo al plano, la losa del nivel 2 —una caja de 50 × 20 m a z=2,95— habría pintado el mapa entero de ocupado. Mientras hubo un solo nivel las dos lecturas coincidían, así que la distinción no costaba nada; con dos niveles decide si el mapa sirve o es basura. El `model://` se busca primero junto al `.world`, de modo que un clon recién descargado funciona sin exportar nada |
| 2026-08-16 | **La carpeta de evidencia estrena índice** ([`Documentos/Evidencia/README.md`](Documentos/Evidencia/README.md)): cada imagen y cada registro con qué muestra, de cuándo es y qué sostiene. La regla queda escrita: toda evidencia nueva entra con su fila, en el mismo commit | Diez imágenes de S12 a S14 llevaban meses versionadas sin que ningún documento las mencionara —estaban guardadas, no documentadas—, así que los pies de foto hubo que escribirlos mirándolas. Al leerlas aparecieron dos cosas que nadie había leído: `rviz_configuracion.png` (mayo) ya mostraba **`RobotModel` en rojo**, que es el defecto de las URIs `package://` sin nombre de paquete diagnosticado tres meses después en S17; y `S12_lidar_screencast.png` no es una captura de Gazebo sino la foto de un reproductor de vídeo, con el `.webm` original sin versionar. Dos archivos se llamaban `Screenshot from …png`, con espacios: el mismo patrón que rompió cuatro enlaces en agosto |
| 2026-08-16 | **El entregable S16 se cierra sin emitir** y deja de contarse como arrastre | Llevaba desde el 11-ago como «decisión pendiente». Un pendiente que nadie resuelve durante semanas no es un pendiente: es ruido que compite por atención con lo que sí está en curso. El hallazgo que lo motivaba ya está registrado en la bitácora del 2026-08-03, así que lo único que se pierde es un documento que nadie iba a leer |
| 2026-08-16 | `Cronograma_S17_S32.tex` deja de pedir el mapa del nivel superior, con nota fechada que explica por qué | Era el último documento que seguía exigiendo una actividad suprimida el 14-ago y desmentida con herramienta el 16-ago. Se corrige el `.tex` y **no** el PDF firmado ni `Entregable_semana_17.tex`: esos dos son artefactos entregados, y reescribir su fuente rompería la correspondencia con lo que de verdad se entregó. La divergencia del acta firmada se comunica en el corte, no se edita |
| 2026-08-16 | `ENTORNO_DE_EVALUACION.md` deja de declararse «pendiente de reflejarse» en los dos entregables | Los siete cambios que pedía su §11 estaban aplicados desde el 11-ago; el encabezado seguía diciendo que no. Comprobados uno a uno antes de cambiarlo, no dados por buenos porque otro documento lo afirmara |
| 2026-08-16 | Se publica el tag **`v0.2.2-dos-niveles-y-evidencia`** y el README pasa a mandar instalar ese | El README seguía apuntando a `v0.2.1-instalacion-robusta`, del 12-ago, once commits por detrás: quien siguiera las instrucciones instalaba un repositorio sin el entorno de dos niveles, sin los arreglos del generador de mapas y con la guarda del `~/.bashrc` que duplicaba. `main` estaba bien y el camino que el propio README prescribe llevaba a otro sitio, que es la clase de desfase que nadie nota porque el que instala no compara. Las cinco referencias al tag se actualizaron **antes** de etiquetar, para que el tag no contenga un README que mande instalar el tag anterior |
| 2026-08-16 | La guarda que añade `GAZEBO_MODEL_PATH` a `~/.bashrc` pregunta **por el efecto y no por el texto**: ¿hay alguna línea activa que meta ya esta raíz en la variable? Y cuando la línea ya está, el consejo pasa a ser `exec bash` en vez de volver a añadirla | Dos defectos encadenados. (1) `grep -qxF` compara la línea entera, así que solo reconocía su propia forma exacta: el `~/.bashrc` de este equipo tenía desde el 11-ago la versión sin comillas —equivalente— y al pegar el consejo se añadía una segunda copia de la misma ruta. Reproducido antes de tocar nada. (2) La comprobación mira la variable de **esta** terminal, no el archivo, de modo que una terminal abierta antes de instalar fallaba y el consejo mandaba escribir en `~/.bashrc`, que no cambia una terminal ya abierta: el verificador volvía a fallar igual y quien lo leía pegaba la orden otra vez. El consejo equivocado no es inofensivo cuando lleva a un bucle sin explicación. Probado en los cinco estados posibles del archivo —sin archivo, forma canónica, forma sin comillas, línea comentada y otra ruta—, pegando la orden tres veces en cada uno |
| 2026-08-16 | El README deja de afirmar que los mundos de `primer_piso` funcionan sin `GAZEBO_MODEL_PATH` | `primer_piso_dos_niveles.world` —el **entorno de evaluación de OE4**— incluye la planta con `model://primer_piso`, así que la necesita. El README enumeraba los mundos afectados en una lista escrita a mano el día que solo había tres, y el que se añadió después es justo el que nadie ha instalado todavía. Es el mismo patrón que la lista fija que se quitó del verificador el 11-ago: sin la variable Gazebo no aborta, carga el mundo vacío y devuelve 0 |
| 2026-08-16 | De aquí en adelante **la fuente `.tex` de cada entregable se versiona junto al PDF** | De los ocho entregables solo dos conservan su `.tex` (semana 17 y el cronograma); los de S10 a S15 existen únicamente como PDF compilado. Consecuencia concreta: las imágenes de S12, S13 y S14 fueron casi con certeza a esos entregables y **no hay forma de comprobarlo**, porque no queda el archivo que las incluía. Si un jurado pide el origen de una figura de esas semanas, la respuesta es que no se conserva. Retroactivamente no tiene arreglo; queda anotado en el índice de evidencia para no afirmar más de lo que se puede sostener |
| 2026-08-16 | **Queda comprobado con herramienta, no por argumento, que un solo mapa sirve para los dos niveles** | Era una afirmación de R10 apoyada en una corrida de navegación. Ahora los cortes a z=0,30 y z=3,30 de `primer_piso_dos_niveles.world` producen mapas **idénticos píxel a píxel** entre sí y con `primer_piso_definitivo.pgm`, y los dos pasan `verificar_mapa.py` con las mismas cifras: 99,7 % de cobertura en X, 0 obstáculos falsos de 5149. Comprobado también que el arreglo no toca el mundo de un nivel: el `.pgm` de `primer_piso_v2.world` sale byte a byte idéntico al versionado |
| 2026-08-18 | **Se emite [`Documentos/REQUISITOS.md`](Documentos/REQUISITOS.md) y se cierra R5.** 34 requisitos (RF-01…RF-27, RNF-01…RNF-07), cada uno con fuente, prueba, estado y **la semana en que se construye lo que falta** | Se escribe como especificación de lo que queda por construir —el nodo de coordinación de S20 y el protocolo de relevo de S21 leen de aquí—, no como trámite de cumplimiento. Se declara de entrada que es una **reconstrucción**: el anteproyecto nunca enumeró requisitos, así que no hay documento perdido que recuperar, y afirmar lo contrario sería insostenible ante el jurado. Al enumerarlos aparecieron tres huecos que ningún documento registraba: (1) los tres defectos abiertos son **precondición de OE4** y no estaban escritos como requisito de nadie; (2) `coordinacion_msgs` y `puntos_interes.yaml` se citan en el contrato de interfaces y **no existen todavía**; (3) `puntos_interes.yaml` **no tiene contenido definido** —ni cuáles son las localizaciones de interés ni sus poses—, y de ahí salen los orígenes y destinos de las 30 repeticiones de OE4: bloquea RF-04, RF-17 y la campaña entera |
| 2026-08-18 | **Spike de hardware, pregunta 4: se caracteriza R8 sin encender ningún vehículo** ([`S19_spike_p4_humble_jazzy.md`](Documentos/Evidencia/S19_spike_p4_humble_jazzy.md)). El desajuste Humble ↔ Jazzy mide **6 diferencias, 37 líneas de YAML y 2 XML**; y **`nav2_msgs/NavigateToPose` difiere entre las dos distribuciones** | Se eligió esta pregunta primero porque no depende del vehículo en intervención (R11) y porque era la única capaz de invalidar una decisión de arquitectura ya tomada. Resultó que sí: la definición de la acción cambió —Humble devuelve `std_msgs/Empty`, Jazzy devuelve `error_code` y `error_msg`—, y es **la única vía de mando que el contrato admite**. De ahí tres consecuencias. (1) **D6 se precisa**: vale como portabilidad de código fuente, no como interoperabilidad en un grafo. (2) **Desaparece un respaldo que nadie había verificado**: demostrar el relevo con un robot físico y uno simulado —la salida natural mientras R11 siga abierto— no funciona, y descubrirlo la semana de la demostración habría sido tarde. (3) **La simulación se queda en Humble por restricción, no por preferencia**: `gazebo_ros2_control` no está liberado para Jazzy y `gazebo_ros_pkgs` está ahí en una versión *anterior* a la de Humble. Tres de las seis diferencias son **silenciosas** —`progress_checker_plugin` en singular, los parámetros de Groot ya inexistentes y `global_frame` del `behavior_server`, que en Jazzy significa otra cosa—: el sistema arrancaría y devolvería `SUCCEEDED` con una configuración que no es la escrita, que es el patrón que ya costó `initial_pose`, `/scan` bajo namespace y `--set-state start` |
| 2026-08-18 | **`CONTRATO_INTERFACES.md` se corrige en tres puntos, ninguno de decisión: dos argumentos caducados y un ejemplo.** (1) Su §2 afirmaba que un robot físico es intercambiable con uno simulado; queda anotado como **refutado** por el spike. (2) Su §3 descartaba el `map` compartido porque «la relación métrica entre pisos en el laboratorio GED es una ficción»; con `primer_piso_dos_niveles.world` esa relación es **exacta** (3,0 m en z), así que el motivo se sustituye por tres que sí se sostienen sobre el entorno vigente. (3) El ejemplo de `PuntoInteres` decía `lab_ged_entrada`; pasa a `piso1_escalera`, la zona de transición real en (41,40 · 3,03) | **Las tres decisiones siguen siendo las mismas; lo que estaba podrido eran las razones.** Un argumento correcto sobre un mundo que ya no se usa es un argumento falso, y el contrato es el documento del que van a leer el nodo de coordinación de S20 y la HRI de S22: si su §3 justifica el diseño con un entorno descartado el 14-ago, el jurado encuentra la incoherencia antes que nosotros. Es el mismo defecto que se cerró en `DESARROLLO_NAV2.md` el 16-ago, en el documento que **sí** está vigente. Ninguna de las tres correcciones toca un artefacto entregado |
| 2026-08-18 | **`angular.z` se dirigía como ángulo de volante y no como velocidad angular**, en el plugin de Gazebo y en el nodo del robot real. Se aplica el modelo de bicicleta despejado, `delta = atan(wz·L/v)`, en los dos sitios | `geometry_msgs/Twist` define `angular.z` en rad/s y los dos lo metían directo en `tan()`. La ganancia efectiva del volante quedaba en `v/0,164`: a 0,5 m/s giraba **3,05 veces más cerrado** de lo pedido y a 0,12 m/s un 27 % menos —**variaba por un factor de 4** a lo largo del rango de velocidad, y el controlador no lo sabía. Es la clase de defecto que no da error: Nav2 planifica bien, el vehículo obedece mal y la culpa parece del planificador. Medido sobre dos maniobras grabadas antes y después: RMS del giro real contra el comandado 0,298 → 0,111 y 0,256 → 0,130 rad/s. La deriva en reposo, anotada como defecto abierto sin cuantificar, resultó ser **el mismo defecto**. La mitad de hardware queda sin validar: no había vehículo disponible |
| 2026-08-18 | Se registra **R12**: el verificador de meta de Nav2 corre en `stateful: True` y, cumplida la tolerancia de posición, solo exige el **rumbo** | Dos corridas terminaron en `ABORTED` y `CANCELED` con **80 cúspides ejecutadas contra 2 planificadas**, maniobrando en el sitio *después de haber llegado*. Un Ackermann no cierra rumbo girando sobre su eje. Bloquea la campaña de OE4 porque contaría como fallos maniobras que llegaron bien, y **la tasa de éxito es una de las cuatro métricas**. Se retira además la cifra de que la deriva en reposo bajó 7,6 veces: esta corrida midió 0,1192 °/s con una ventana tres veces más larga, así que el mecanismo se sostiene pero la cifra no, hasta medirla con ventanas iguales |
| 2026-08-19 | **Spike de hardware, preguntas 1 y 2: primera medida sobre el vehículo físico** ([`S19_spike_p1_p2_hardware.md`](Documentos/Evidencia/S19_spike_p1_p2_hardware.md)), con `herramientas/verificar_lidar.py` versionada junto al informe | El LiDAR repite la misma pared con **2,8 mm de dispersión** y el vehículo obedece a `/ctrl_pkg/servo_msg` de forma **proporcional** en dirección y tracción — luego la cuantización binaria que veíamos es defecto **nuestro**, reparable, y no del hardware. La herramienta halla las superficies por su **forma** (RANSAC secuencial) sin recibir nunca la distancia esperada, y por eso se versiona: su docstring conserva los dos métodos descartados, incluido el de buscar lecturas «cerca de 1 m», que es circular. **El sensor medido ese día era un LiDAR externo, no el del kit** — ver la entrada del 21-ago |
| 2026-08-20 | **`mundo_definitivo.world` pasa a ser el mundo vigente**, con `puntos_interes.yaml` y sus **15 destinos del piso 1**. Los dos pisos dejan de dibujarse apilados y pasan a ir **lado a lado en Y** (revisa D4 y §6) | El pasillo de la USTA queda modelado de verdad. **Siguen siendo el piso 1 y el piso 2** —`robot1` y `robot2` en `herramientas/robot.sh`—; lo que cambia es el dibujo. Elevar un nivel no aportaba nada: cada robot corre en su propio `gzserver` con su `ROS_DOMAIN_ID`, así que no comparten escena, y duplicar las 59 paredes solo duplicaba lo que puede divergir. **El mundo vigente estaba escrito en cuatro sitios que no se hablaban entre sí** —`deepracer_raiz_repo.MUNDO_VIGENTE`, el mapa por defecto de `nav_amcl_demo_sim`, la lista de `verificar_instalacion.sh` y el README—: cambiarlo en tres habría dejado el cuarto apuntando a otra geometría. Dos defectos que el cambio destapa y que **no fallan de forma ruidosa**: (1) la pose de spawn por defecto era (0, 0), que en `primer_piso_v2` caía dentro del pasillo y aquí cae en la explanada **entre** los dos —Gazebo abre, el vehículo aparece, los 7 controladores quedan activos y no hay pasillo alrededor—, corregida a y = 10,05 con 1,08 m de holgura medidos por transformada de distancia sobre el `.pgm`; (2) `GAZEBO_MODEL_PATH` **deja de ser opcional**, porque el mundo por defecto usa `model://`. Los quince destinos salen de detectar los vanos sobre el mapa, no de mirar RViz, y cada parada se arrima a la pared de **su** puerta: la regla obvia —el punto más despejado cerca del vano— ponía ETM13 y ETM3 a 0,18 m, por debajo de la `xy_goal_tolerance`, o sea **dos nombres para un mismo destino**. Cierra el hueco que bloqueaba RF-04 y RF-17 |
| 2026-08-21 | **Primera navegación sobre `mundo_definitivo`: los tres destinos se alcanzan, y uno de los tres devuelve `SUCCEEDED` estando fuera de tolerancia** | El mundo vigente cambió el 20-ago y **nunca se había navegado sobre él**: las corridas de la maniobra de retorno fueron sobre `primer_piso_v2`. Se lanzaron tres destinos reales de `puntos_interes.yaml`, no clics en RViz. Resultado contra `/odom` —la verdad del simulador, regla del 12-ago—: ETM1 **0,2092 m**, ETM13 **0,2960 m**, Escaleras **0,1769 m** con el rumbo a **5,65°**. **Lo que queda demostrado:** el radio de giro Ackermann cabe en los pasillos nuevos, incluido el tramo norte-sur estrecho, así que la geometría **no** es lo que bloquea la condición 4. **Lo que aparece:** ETM13 dio `SUCCEEDED` a 0,296 m, por encima de la `xy_goal_tolerance` de 0,25 del propio Nav2, porque el controlador mide contra la pose de AMCL y no contra la verdad — si la campaña de OE4 contara éxitos por el `SUCCEEDED`, ese contaría como acierto sin serlo. **Y una predicción fallida, anotada como tal:** se anunció que Escaleras abortaría por R12 y cerró el rumbo sin problema; R12 existe pero es intermitente |
| 2026-08-21 | El aviso de `herramientas/robot.sh` sobre el mapa por defecto estaba caducado, y para `robot2` pasa a ser un **error que aborta** | Decía que sin `map:=` se carga un mapa de `primer_piso`; desde el 20-ago el launch trae `mundo_definitivo_piso1.yaml`, así que el aviso era **falso** y mandaba pasar a mano algo que ya estaba bien. Debajo había un defecto de verdad: `robot2` arranca en el pasillo del **piso 2** y ese mapa **no existe**, de modo que habría cargado la geometría del piso 1 estando en otro sitio. AMCL no da error —converge contra las paredes equivocadas— y el síntoma sale mucho después como una ruta imposible, que es exactamente el patrón del mapa inventado del 12-ago |
| 2026-08-21 | El README deja de **nombrar** un tag concreto y enseña a preguntárselo al remoto (`git ls-remote --tags --refs --sort=-v:refname`); se publica `v0.3-mundo-definitivo` | Apuntaba a `v0.2.2-dos-niveles-y-evidencia`, once commits atrás: quien siguiera las instrucciones instalaba un repositorio **sin `mundo_definitivo.world`, sin `REQUISITOS.md`, sin `robot.sh` y sin `puntos_interes.yaml`**. Es la **segunda vez** que pasa lo mismo —ya ocurrió el 16-ago—, y ahí está la lección: actualizar el nombre no arregla nada, porque vuelve a caducar al siguiente tag. Por eso se quita el nombre del texto. Comprobado que el camino funciona de verdad, en las dos direcciones: sin `GAZEBO_MODEL_PATH` el verificador da 30/2 con `model://` irresoluble en cuatro mundos, y con ella 32/0 |
| 2026-08-21 | **El LiDAR no bloquea S20: de las tres capas que el spike culpaba, falla una sola** ([`S19_lidar_original_evo.md`](Documentos/Evidencia/S19_lidar_original_evo.md)). El sensor del kit es un **RPLIDAR A1M8-R5**, no el YDLidar G4 del 19-ago | Los dos informes de hardware de S19 hablan de **sensores distintos**, y leerlos como si fueran el mismo hace parecer que uno midió mal. Con el sensor de fábrica, la detección por chip es **correcta** y el desajuste de protocolo serie **no aplica**: lo único roto es que el lanzador pide `rplidar_node` y el ejecutable instalado se llama `rplidar_composition`. El punto 1 del backlog del spike —«instalar un driver YDLidar para Jazzy», marcado bloqueante de S20— **apuntaba a un sensor que no está en el carro**. Medido: 6,80 Hz (σ 0,0039 s sobre 50), **360 muestras** a 1,000° exactos sobre 360°, alcance 0,15–12,0 m. Eso invalida también el punto 5: la URDF hay que corregirla a 360 muestras y 12 m, no a 600 y 10 — y **la simulación ve hoy más rayos que el hardware**, lo que contaminaría la comparación sim ↔ hardware de OE4. Queda una decisión abierta: bajar la simulación a 360 rayos o subir el driver por encima de 360, que el sensor crudo permite (~588 puntos/vuelta). Se anota además que `systemctl` **miente**: informaba `active (running)` con 109 tareas y 2 tópicos, contra 292 y 19 en estado sano |
| 2026-08-22 | **La pose de spawn pasa a tener una sola tabla**, `POSE_INICIAL` en `deepracer_raiz_repo.py`, que leen los dos launch y `herramientas/robot.sh` | Lo detectó Santiago clonando el repositorio en otro equipo y siguiendo la guía: `robot1` nacía **fuera del pasillo**. La pose vivía en **tres** sitios que no se hablaban, y al cambiar el mundo vigente el 20-ago se actualizaron dos; el que se quedó en (0, 0) era `nav_amcl_demo_sim.launch.py`, que **incluye** a los otros y por tanto gana. El fallo es mudo: Gazebo abre, el vehículo aparece, los siete controladores quedan `active`, y lo único que falta es el pasillo alrededor — con (0, 0) fuera del mapa, AMCL arranca sin geometría contra la que localizar. `verificar_repositorio.sh` gana la comprobación «cada robot nace en celda libre de su mapa», que convierte el error en ruidoso. **Al arreglar la guía cometí el mismo error que arreglaba:** puse una **ruta fija** con el `Documents/` de mi propio equipo en siete comandos, y el verificador bajó a 11/12 antes de que nadie la ejecutara; se sustituyó por `$TESIS`, definida una sola vez por terminal. **Anotado el 2026-08-30: `$TESIS` también estaba mal, y se retiró.** Cambiaba una ruta fija por una variable que hay que acertar, y su fallo era mudo: sin definir, `cd ""` devuelve 0 sin moverse y el error acusa al script siguiente. Los comandos vuelven a la forma del resto del repositorio —`herramientas/…` relativo, ejecutado desde la raíz del clon—, que es la única que no depende del equipo |
| 2026-08-22 | **Se emite [`PROTOCOLO_EXPERIMENTAL.md`](Documentos/PROTOCOLO_EXPERIMENTAL.md) y se cierra la condición 3 de S19**: las cuatro métricas de OE4 con definiciones operativas, criterio de éxito, N y política de corridas fallidas | Se escribe **antes** de instrumentar por un motivo que es metodológico y no de orden: un protocolo redactado después de ver los datos se acomoda a lo que salió —se elige el umbral que deja fuera las tres corridas malas— y la campaña deja de poder refutar nada; la fecha en el historial de git es parte de la evidencia. Lo que fija: (1) el éxito se mide contra `/odom`, nunca contra `SUCCEEDED`, y la muestra del 21-ago es la demostración —los tres destinos dijeron `SUCCEEDED` y uno estaba a 0,296 m, así que la tasa ingenua habría sido 100 % en vez de 67 %—; (2) **el rumbo no decide el éxito** y `stateful` pasa a `False`, que es la decisión pendiente de R12; (3) una corrida fallida **cuenta como fallo** salvo causa de una **lista cerrada** de cuatro, documentada con evidencia, y con techo del 20 % de descartes por encima del cual la campaña no vale. Al escribirlo salieron **seis prerrequisitos abiertos** (§10) que hoy impiden empezar a medir, entre ellos el `/odom` cruzado —del que salen las cuatro métricas— y el mapa del piso 2 |
| 2026-08-23 | **El mundo se parte en uno por piso**, `mundo_definitivo_piso1.world` y `mundo_definitivo_piso2.world`, y `mundo_definitivo.world` pasa a referencia histórica | La razón no es de orden, es una **contaminación de sensor medida**. El comentario del mundo y D4 afirmaban que los dos robots «nunca comparten escena» porque cada uno corre en su `gzserver` con su `ROS_DOMAIN_ID`; eso aísla el **grafo de ROS**, no la **escena de Gazebo**. Las 59 paredes estaban en un solo `.world`, el LiDAR simulado alcanza **10,0 m** (`deepracer.xacro`) y entre los dos pasillos hay **5,44 m**. Muestreadas 1701 celdas libres del mapa del piso 1: **61 posiciones ven geometría del piso 2, 159 rayos contaminados, el peor caso 12 rayos a 6,48 m**, todos en x −19,25…−17,81 e y 5,07…8,91 — el tramo norte-sur donde nace `robot1` (−19,165, 7,292) y donde está el punto de transferencia `Escaleras` (−19,43, 5,91), a **0,89 m** del foco más cercano. **Una hipótesis mía falló antes:** lancé los rayos solo desde las 15 paradas y salieron **0 de 10800** contaminados; ampliar la prueba a todo el espacio libre fue lo que encontró el foco. **Lo que apilar a z = 3,0 hacía y nadie había anotado:** un láser plano no ve una planta que está encima, así que aplanarlos el 20-ago quitó una protección que no figuraba como tal. La partición es **extracción pura** —25 paredes al piso 1 y 34 al piso 2— y se verificó por tres vías: XML bien formado, rectángulos de pared **idénticos** antes y después, y el `.pgm` del mapa del piso 1 **igual byte a byte**. **No está medido** que AMCL se desplace por esto: lo medido es que los rayos llegan |
| 2026-08-23 | **El piso 2 tiene mapa**, `maps/mundo_definitivo_piso2.{pgm,yaml}`, y `verificar_pose_spawn.py` deja de imprimir `[ -- ]` para `robot2` | Cierra el prerrequisito de §10 del protocolo que bloqueaba la **condición B** de la campaña, y el criterio de cierre del lunes de [`PLAN_S20.md`](Documentos/PLAN_S20.md) queda hecho antes de que empiece S20. El piso 2 no es un pasillo recto sino una **S** —ala oeste, tramo norte-sur, pasillo largo y ramal sur al este—, así que hacen falta **cinco `--region`**: un solo rectángulo grande deja que el relleno se escape por los vanos de ~0,9 m hacia las salas, que es lo que pasó en el intento del 21-ago con 95,1 % libre. El corte del extremo oeste es a ras, x = −23,39, la opción **A** de `mapa_destinos.txt`: no modifica ni un byte del `.world`, solo declara hasta dónde llega el mapa. `robot2` nace en celda libre con **1,50 m** de holgura, y el mapa sale `ACEPTADO` con 0 de 6096 obstáculos sin pared real. **Revocado la misma tarde:** el corte a ras se sostenía en que al oeste no había nada que importara, y lo que hay es la escalera; se pasó a la opción **C** —ver las dos últimas filas de esta bitácora— y el mapa se regeneró con la misma receta, ahora con 6306 obstáculos |
| 2026-08-23 | **`verificar_mapa.py` medía las celdas desconocidas contra el denominador equivocado**, y por eso rechazaba dos mapas correctos | Contaba `desconocidas / caja envolvente entera`. Mientras la planta fue un pasillo recto las dos cosas coincidían; con una planta en **L** o en **S** dejan de coincidir y por mucho, porque la caja envolvente es casi toda esquina vacía —suelo que nunca estuvo dentro del edificio y que es **correcto** que figure como desconocido—. El piso 1 daba **58,5 %** y el piso 2 **65,0 %**, contra un máximo del 35 %. Medido sobre las **regiones declaradas**, que el propio `.yaml` ya registra en su cabecera porque el generador la escribe solo, el piso 1 da **1,5 %** y el piso 2 **0,1 %**. Un mapa de SLAM no declara regiones y ahí se sigue midiendo sobre la imagen entera, que para ese caso es lo que siempre se quiso. Partir el mundo arregló además la otra mitad del rechazo del piso 1: la cobertura en Y pasa de **29,7 %** a **100,6 %**, porque medía contra los dos pasillos y el mapa cubre uno |
| 2026-08-23 | **El LiDAR simulado no ve hacia adelante: barre 300°, no 360°, y el cono ciego de 60° cae sobre el morro** | Salió al verificar los dos mundos partidos con `herramientas/robot.sh <robot> lidar`, que marcaba un eje sin rayo en cada robot. La causa está declarada en `deepracer.xacro`: `lidar_360_degree_min_angle` = −150° y `max_angle` = +150° —el nombre del parámetro **miente**—, lo que deja 60° ciegos centrados en los 180° del láser; y como `hokuyo_joint` monta el sensor con `rpy 0 0 3.1416`, esos 180° del láser son los **0° del chasis**. Confirmado desde **tres poses de yaw distinto**: `robot1` a +91,34° ciego en +y, `robot2` a −0,60° ciego en +x, y `robot2` a −36,77° con los cuatro ejes medibles. El cono sigue al morro, no a un eje del mundo. **Importa porque el vehículo no gira sobre su eje**: el sector sin datos es justo la dirección de marcha, y `ETM10` es el único destino que se aborda de frente (`lado_pared: fondo`) — su propio comentario en `puntos_interes.yaml` ya pedía probar esa llegada antes de darla por buena, y ahora hay una causa candidata. Se cruza además con lo medido el 21-ago sobre el sensor real, que sí da **360 muestras sobre 360°**: sim y hardware no son comparables en este punto. **No está medido** que AMCL ni Nav2 se degraden por esto: lo medido es que los rayos no existen. Se abre **R13** |
| 2026-08-23 | **El vehículo reposa inclinado hacia el sentido contrario del que decía la documentación**, y las lecturas largas en direcciones abiertas son el `ground_plane` | `medir_paredes.py` avisaba de que el carro va «1,4 deg de morro abajo» y que el rayo choca contra el piso a unos 7,3 m. La magnitud es correcta; **el sentido no**: los rayos que bajan son los de **atrás**. Se midió sin buscarlo, porque el mismo eje `−x` de `robot2` dio **7,350 m** desde el spawn y **9,191 m** tras girar el vehículo 36°. No hay geometría al oeste de x = −23,39, así que las dos lecturas son suelo, y la razón entre ellas lo demuestra: con `r = (h/θ)/\|cos φ\|` y φ medido desde el morro, la razón predicha es **1,247** contra **1,250** medida, y `h/θ` sale **7,35 m** por una vía y **7,37 m** por la otra. **Por qué nadie lo había corregido:** el rayo del morro cae dentro del cono ciego de la entrada anterior, de modo que la afirmación «morro abajo» **nunca fue comprobable** con esta herramienta. Corregido en la cabecera de `medir_paredes.py`, con la fórmula, para que una lectura larga deje de leerse como pared |
| 2026-08-23 | `herramientas/robot.sh` pasa `map:=` explícito, y el guardián que protegía a `robot2` se convertía en su contrario | El guardián abortaba si el nivel **no** tenía mapa. Al darle mapa al piso 2 dejó de saltar, y debajo quedaba el mismo fallo que existía para impedir: el launch trae `mundo_definitivo_piso1.yaml` por defecto —tiene que traerlo, porque un launch no sabe qué robot lo invoca—, así que `robot2 nav` habría cargado **en silencio** la geometría del piso 1. Es el patrón de siempre: AMCL converge contra las paredes equivocadas sin dar error. Ahora el mundo y el mapa salen los dos de la fila del robot en `POSE_INICIAL`, que gana la clave `mundo` |
| 2026-08-23 | **El punto de transferencia del piso 2 existía y estaba descartado en el inventario**, y recuperarlo obligó a meter en el `.world` un obstáculo que no es una pared | `mapa_destinos.txt` tenía el vano 1 del piso 2 —4,20 m, extremo oeste— marcado `N (boca abierta)` con la razón «no es un vano, es donde se acaba el edificio». Lo que se acababa ahí era el dibujo: es el hueco por donde suben físicamente **las escaleras del piso 1 al 2**, o sea el único punto de relevo del piso 2. **Mi prueba de la hipótesis falló por confiar en esa etiqueta en vez de en la geometría:** los dos pasillos distan 14,50 m exactos en Y, apliqué el desfase a la escalera del piso 1, predije (−19,43, −9,31), no encontré vano y di la hipótesis por falsa; con la ubicación real (−23,07, −9,10) la predicción en Y erraba **21 cm** — era correcta, y el vano que buscaba estaba etiquetado como no-vano. En X no transfiere nada porque el piso 2 es 2,67 m más ancho por el oeste. Señal que también se pasó por alto: `robot2` nace a **0,88 m** de ese punto, igual que `robot1` nace a 1,41 m del suyo. Escrito como `piso2_escalera` en `puntos_interes.yaml`, que pasa de 15 a **16 destinos** |
| 2026-08-23 | **La boca de la escalera se cierra con `Barrera_Escalera`, que representa un riesgo de caída y no geometría del edificio** — opción **C** de `mapa_destinos.txt`, en sustitución de la **A** aplicada esa misma mañana | La boca **no** está cerrada en el edificio real: es el hueco de la escalera. Aun así hay que taparla, y la razón no es arquitectónica sino de sensor: **un LiDAR plano a 0,16 m del suelo no puede ver un desnivel hacia abajo**. El hueco no devuelve ningún rayo, el costmap lo declara libre hasta `range_max` y Nav2 planifica por encima. Una caja es la única forma de escribir «por aquí no se pasa» en un mapa plano. La objeción que había descartado C —«cambiar el `.world` invalida el mapa del piso 1»— murió sola al partir el mundo esa mañana: partirlo abarató C sin que fuera el objetivo. Va en **x = −23,07**, el punto marcado en Gazebo, 32 cm al este del final de `Wall_81` y `Wall_88`, que es la elección conservadora: a −23,39 la franja intermedia quedaría del lado transitable. De eje a eje de esas dos paredes, 2,5 m de alto, en **Gazebo/Orange** y no en el gris de las demás para que se distinga a simple vista. Mapa regenerado con la misma receta de `--region`: cobertura 100 %, **0 de 6306** obstáculos sin pared real. La parada de `piso2_escalera` pasa de (−22,49, −9,03) a **(−21,50, −9,03)** con holgura 1,50 m, que coincide al centímetro con la distancia a la barrera — la restricción activa es ella, no las paredes laterales, que quedan a 2,12 m. **Riesgo de mantenimiento:** si alguien borra la barrera por parecer un muro inventado, Nav2 vuelve a planificar sobre el hueco. Documentado en el propio link, en la cabecera del `.world` y en el destino |
| 2026-08-23 | **La protección contra la caída por la escalera existe solo en simulación: el carro real no tiene sensor de precipicio** | Es la contrapartida de la entrada anterior y conviene no perderla de vista. En el mundo simulado la barrera es un obstáculo sólido y el costmap la respeta; en el edificio no hay nada equivalente, y el LiDAR físico —que sí barre 360°, medido el 21-ago— sigue siendo plano y sigue sin ver un escalón hacia abajo. La única mitigación hoy es geométrica: la parada de `piso2_escalera` se retira a **1,50 m** en vez de los 0,90 m del patrón `ETM10`, y esos 0,60 m extra absorben además la incertidumbre de dónde empieza el primer escalón, que **no se pudo medir**. **No está decidido** si la campaña física llega a hacer el relevo en la escalera; si llega, esto hay que resolverlo antes y no con un número más grande |
| 2026-08-24 | **`robot2` navega a su punto de transferencia, y el cono ciego de R13 no degrada la localización** | Primera navegación del piso 2 sobre su propio mapa. `piso2_escalera` es el peor caso que hay para R13: se llega de frente con yaw = π, así que el sector de 60° sin datos apunta a `Barrera_Escalera`, la única superficie nueva de esa zona. Resultado contra `/odom` —nunca contra el `SUCCEEDED`, regla del 12-ago—: **0,190 m** de error total y 6,27° de rumbo, los dos dentro de tolerancia, y **R12 no se disparó**. Descompuesto con la TF `map → odom` (−0,354 · +0,824 · +2,10°, que no es la identidad, luego AMCL corrigió de verdad): **3,6 cm son de localización** y el resto de **control**. La varianza del filtro cae de 0,44 a 0,05 m² durante el trayecto. **Lo que sí conviene mirar:** el error apunta hacia la escalera —el carro acabó a 1,344 m de la barrera en vez de a 1,500— o sea el mismo frenado tardío del 21-ago, pero aquí el sentido de la marcha es el del vacío. Con el retiro de 0,90 m del patrón `ETM10` habría acabado a **0,749 m** del borde: los 1,50 m no eran decorativos, se comieron el error. R13 pasa de 🔴 a 🟡 |
| 2026-08-24 | **`amcl_pose` es `TRANSIENT_LOCAL` y AMCL solo publica cuando el robot se mueve: leerlo al llegar devuelve una muestra caducada sin dar ningún error** | Salió intentando medir lo de arriba, y **engañó dos veces seguidas, la segunda ya sabiendo qué se buscaba**. Con el vehículo parado `ros2 topic hz /robot2/amcl_pose` da **0 mensajes en 10 s**; en movimiento da 12. Como el tópico está *latched*, `echo --once` no espera: entrega la muestra retenida, que era la **semilla del arranque** —`stamp: sec 0`, la pose de spawn con 12 decimales exactos y una «covarianza» de −8e−11, negativa, que no es una covarianza sino un filtro que todavía no ha corrido—. Nada en la salida delata que el dato es viejo. **Consecuencia directa para RF-25**, que se congela el miércoles: el instante en que un registrador de misión quiere la pose de llegada es exactamente el instante en que el robot está quieto y AMCL calla. **La pose de llegada se toma de `/odom`**; si además se quiere registrar la creencia de AMCL, hay que muestrearla *durante* el trayecto o leer la TF `map → odom`, que sí se republica a 9,5 Hz con el vehículo parado |
| 2026-08-24 | **Existe `coordinacion_msgs`: el aporte declarado del proyecto ya tiene con qué escribirse** | Los cuatro tipos del §5 de [`CONTRATO_INTERFACES.md`](Documentos/CONTRATO_INTERFACES.md) —`EstadoRobot`, `PuntoInteres`, `EstadoMision` y la acción `GuiarUsuario`—, en `Robot/aws-deepracer/coordinacion_msgs/`. Era el desbloqueo del hito 4, que arrastraba el 5 y el 7: **coordinación y relevo no podían existir hasta que existieran estos mensajes**, y su ausencia estaba anotada como hueco desde el 18-ago. Se declararon las constantes (`LIBRE=0`…, `INACTIVA=0`…) en vez de dejar los números sueltos en los nodos. **No se acepta «compila» como evidencia:** `test/prueba_round_trip.py` publica y recibe los cuatro tipos por DDS y compara campo a campo —incluidos el `PuntoInteres` anidado dentro de `EstadoMision`, los `float64` sin pérdida y el ciclo completo goal → feedback → result de la acción—, 13 de 13. Existe porque el paquete tiene que correr en **dos distribuciones** (Humble en el PC, Jazzy en el carro) y generar los headers no prueba que `rmw` transporte igual en las dos; hay que volver a ejecutarlo al compilar en la tarjeta |
| 2026-08-24 | **El §4 del contrato pide un tópico cuyo tipo no puede existir** | Apareció al construir el paquete, no al escribir el contrato. La tabla declara `/coordinacion/puntos_interes` de tipo `coordinacion_msgs/PuntoInteres[]` *(latched)*, pero **un tópico de ROS 2 transporta un solo mensaje, no un arreglo**: `PuntoInteres[]` es una sintaxis válida para un *campo*, no para un tipo publicable, y no hay ninguna clase que importar. El §5 cierra con «cuatro definiciones, nada más», así que el hueco estaba en los dos sitios a la vez. **Cerrado el mismo día** con un quinto mensaje envoltorio, `ListaPuntosInteres` (`origen`, `stamp`, `PuntoInteres[] puntos`), y los §4 y §5 corregidos. La prueba no comprueba que el arreglo viaje —eso es lo fácil— sino el caso real: **un suscriptor que se conecta después de la única publicación** recibe igual los **16 puntos** del YAML real, con los dos puntos de transferencia marcados y los dos niveles presentes. Si el *latching* fallara, la HRI se quedaría con los desplegables vacíos y no se sabría hasta la demostración. De paso quedó escrita la regla que separa este caso del de `amcl_pose`, que tiene el mismo QoS y engañó dos veces ese mismo día: **se retienen catálogos, nunca medidas** |
| 2026-08-24 | **El nodo de coordinación existe, y el relevo se escribió como función pura para poder probarlo sin simulador** | `Robot/aws-deepracer/coordinacion/`, en dos piezas a propósito. `planificador.py` decide **qué** hacer —qué robot, cuántos tramos, cuántos relevos— y **no importa `rclpy`**; `coordinador.py` es solo el cableado: sirve `/coordinacion/guiar_usuario`, publica el catálogo latched y llama a `navigate_to_pose` de cada robot. El motivo de partirlo es que si el relevo viviera dentro del nodo, la única forma de comprobarlo sería levantar dos Gazebos. Así se agotan **las 240 combinaciones** origen-destino del catálogo real en milisegundos: 20 comprobaciones, 508 tramos. La que de verdad importa es **D2 hecha comprobable** —ningún robot recibe jamás un goal en un nivel que no es el suyo—; si alguien rompe D2 la prueba lo dice, y la simulación no, porque el robot obedecería el goal y se iría contra una pared. El nodo **verifica cada llegada contra `/odom`**, no contra el `SUCCEEDED` (regla del 12-ago), y rechaza el tramo si el robot dice haber llegado pero no publica odometría. Las dos rutas de fallo se probaron **sin ningún robot vivo**: origen inexistente aborta en **2,8 ms** con el motivo ya redactado en español, y una misión válida sin robots aborta a los 20 s señalando el desajuste Humble/Jazzy del §2 del contrato como primer sospechoso |
| 2026-08-24 | **La prueba exhaustiva pasó con los 30 planes de relevo mal, porque comprobaba la forma y no el significado** | Las 240 combinaciones pasaban 17 comprobaciones —4 tramos, 2 robots, 1 relevo— y aun así **los 30 planes con relevo mandaban al mismo robot dos veces seguidas al mismo punto**. Se vio **imprimiendo un plan a mano**, no con la prueba. El texto llegaba a decirle «siga al robot hasta Escaleras» a alguien que ya estaba en las escaleras. Nav2 tampoco lo habría delatado: un goal de cero metros devuelve `SUCCEEDED` al instante. La causa es que el **piso 2 tiene un solo punto y es el de transferencia**, así que el origen o el destino coinciden siempre con la escalera. Arreglado fundiendo tramos consecutivos del mismo robot al mismo punto, y añadido el invariante que faltaba. **Lo que no se arregla con código:** con el catálogo de hoy, **0 de 30 misiones con relevo ejercitan los cuatro tramos** —las 30 se colapsan a tres—, así que el protocolo de relevo **no se puede demostrar completo hasta que el piso 2 tenga destinos que no sean la escalera**. Eso asciende los nombres y poses reales del piso 2, anotados hasta hoy como pendiente menor, a **prerrequisito de la campaña de OE4** |
| 2026-08-24 | **El marco `map` se prefija con el namespace, como pedía el §3 desde el 5-ago, y no hacerlo abortaba las misiones del coordinador sin decir por qué** ([`S20_marco_map_prefijado.md`](Documentos/Evidencia/S20_marco_map_prefijado.md)) | Salió al estrenar el coordinador ese mismo día: compone el objetivo con `frame_id: robot1/map` siguiendo el contrato, ese marco **no existía** porque los tres launch dejaban `map` a secas, el planificador no podía transformarlo y Nav2 acababa en `ABORTED` tras agotar las recuperaciones. **En ningún punto de la cadena aparece la palabra «marco»**: el único síntoma es un vehículo que se recupera en el sitio sin haber avanzado nunca. Había un segundo fallo aún más callado: con el `map_server` sellando su `OccupancyGrid` en `map` y el costmap global esperando `robot1/map`, **la capa estática se queda vacía y nadie protesta**. La desviación respecto al contrato no era un descuido —estaba razonada por escrito en los tres launch, con el argumento de que `map` «es el ancla común» y de que los dominios DDS separados impiden la colisión. **Las dos mitades son falsas:** no hay un ancla común sino **dos mapas distintos con el mismo nombre**, y el aislamiento por dominio es temporal, porque el relevo de H3 exige grafo compartido; no hacía correcto el marco sin prefijar, solo **aplazaba la colisión** al punto del proyecto donde más caro sale. Corregido en los tres launch, en `evaluar_navegacion.py` —que ahora deduce el marco del namespace en vez de fijarlo—, en las tres vistas de RViz y en `verificar_contrato.py`, donde la comprobación pasa de `info` tranquilizador a **fallo** si hay `<ns>/odom` sin `<ns>/map`: esa excusa —«normal si no hay AMCL levantado»— se imprimía igual con AMCL levantada, y fue la que tapó el defecto. Los YAML **no** se tocan: `map` a secas sigue siendo lo correcto cuando no hay namespace. Verificado el 2026-08-25 en la corrida del hito H3, con los dos objetivos enviados en `robot1/map` y `robot2/map` |
| 2026-08-24 | **El mapa del piso 1 declaraba libre medio edificio, y el defecto estaba en el umbral, no en el dibujo** | Salió de una misión que se fue **fuera del mapa**: Nav2 planificó una diagonal de 38 m atravesando paredes. La causa son dos defectos que se tapaban entre sí. **Primero**, el relleno de espacio libre se escapaba por los **dieciséis vanos** del pasillo —las puertas no tienen puerta en el `.world`— e inundaba el exterior; se llevaba tapando con la opción `--region`, que recorta el resultado sin arreglar la fuga. **Segundo, y peor:** `free_thresh: 0.25` clasificaba como **libre** el valor 205, que es el que la convención de ROS reserva para *desconocido* — `(255−205)/255 = 0,196 < 0,25` —, así que todo lo que el generador escribía para decir «aquí no sé» el `map_server` lo leía como «aquí se puede pasar». Corregido a `0.1`, que deja 205 del lado correcto sin mover el 254. **Los vanos se cierran con geometría, no con incógnita**, y esa fue la decisión de fondo: Nav2 **infla los obstáculos letales pero no las celdas sin información**, de modo que una pared regala el colchón de 0,55 m del `inflation_radius` y un hueco desconocido no regala nada; además el LiDAR ve el panel y AMCL no se degrada frente a cada puerta. Dieciséis links `Limite_vano_*` en `Gazebo/Orange` —el mismo código de color que `Barrera_Escalera` del 23-ago: naranja significa «esto no es geometría del edificio»—, derivados analíticamente de las caras de las paredes vecinas más 0,10 m de solape, no medidos a ojo. El generador los pinta con el valor **50**, que Nav2 lee como ocupado igual que el 0 pero un humano distingue. **Criterio de cierre acordado de antemano:** regenerar **sin** `--region`; si el mundo está sellado de verdad, el relleno se contiene solo. Se contuvo. Resultado: **0 celdas de frontera** de 25 946 libres, **0 obstáculos fantasma** de 6044, umbrales coherentes, los 15 destinos libres y en una sola componente conexa. **Consecuencia que hay que asumir: toda medida de navegación anterior a esta fecha se tomó sobre mapas con espacio libre inventado**, incluidas las del 21 y 24 de agosto. `verificar_mapa.py` aprende a detectar esta clase sola —contrasta el `.pgm` contra los umbrales del `.yaml` y cuenta frontera libre↔desconocido—, y aplicado al **piso 2** lo delata sin ayuda: 270 celdas de frontera en 210 sitios y la misma incoherencia del 205. El piso 2 queda pendiente, y se cierra ese mismo día (fila siguiente) |
| 2026-08-24 | **R12 medido de verdad y aplicado: el rumbo final dejaba la llegada fuera de tolerancia, y era la mitad de los casos** | Desde el 18-ago R12 se apoyaba en dos corridas con las cúspides contadas pero **sin las dos cifras de la misma llegada**; por eso el 22-ago se decidió `stateful: False` y no se aplicó. Una misión real del coordinador lo cerró: destino ETM1, `SUCCEEDED` de Nav2, y al leer `/odom` con el carro parado — **posición 0,413 m (FUERA de los 0,25) y rumbo 0,200 rad = 11,4° (DENTRO de los 0,25)**. El mecanismo entero, ya sin hipótesis: el carro venía de ETM10 hacia el **oeste** y ETM1 pide mirar al **este**; con `stateful: True` el checker, cumplida una vez la tolerancia de posición, **deja de comprobarla** y solo exige rumbo; el Ackermann maniobró los 180° —el «medio giro» visible en Gazebo— y **la maniobra lo apartó del punto**, de menos de 0,25 m a 0,413. Nav2 dijo `SUCCEEDED` y el coordinador lo rechazó contra `/odom`, que es exactamente para lo que se escribió. **El razonamiento del yaw en `puntos_interes.yaml` era correcto y aun así insuficiente:** alinear con el pasillo evita la maniobra de quien llega de frente, no la de quien llega por detrás, y con quince destinos en línea recta **la mitad de los pares origen-destino se recorren en el sentido contrario**. Aplicado `stateful: False` y `yaw_goal_tolerance: 3.15` (~π): el rumbo final **no se impone** y el vehículo queda apuntando hacia donde lo dejó la ruta, que para guiar a una persona es lo útil. El `yaw` del catálogo pasa a **orientativo** y así queda escrito, en vez de dejarlo como una garantía falsa. **Lo que NO cambió, y es lo que hace legítimo el cambio:** el criterio de éxito sigue siendo 0,25 m contra `/odom`, el mismo de antes de ver estos datos — se cambió lo que se le *pide* a Nav2, no el listón con el que se le *mide*. Verificado repitiendo la misión, que pasó a `exito: true`. **R12 pasa de 🔴 a 🟡:** cierra del todo cuando se repitan los tres destinos del 21-ago comparando cúspides contra aquel registro (§9.1 del protocolo) |
| 2026-08-24 | **El piso 2 sellado, y la clase de defecto convertida en herramienta** | Cerrado el pendiente que dejaba la fila anterior. El piso 2 no es un pasillo recto sino una **S** con treinta y cinco paredes, así que los vanos no se podían enumerar a ojo como los dieciséis del piso 1. El problema real no era encontrar huecos sino **distinguir puerta de paso**: emparejar puntas sueltas de pared da también la sección transversal del pasillo, que es un hueco entre dos paredes tan legítimo como una puerta. La separación limpia es la **colinealidad** —un vano real prolonga el eje de la pared que lo abre; una sección de pasillo es perpendicular a ella—, y en el piso 2 salió tajante, sin zona gris: todo vano real dio un coseno de al menos 0,92 en valor absoluto y toda transversal 0,71 o menos. Con eso, **27 vanos**. Cuál dejar abierto lo decidieron las cinco `--region` del 23-ago, que ya declaraban la red navegable: si los dos lados del hueco caen dentro de la red, es paso. Quedaron **4 pasos y 23 paneles** `Limite_p2_*`. Ése es el único uso que le queda a `--region`, y es el honesto: declarar la intención, no recortar el resultado. **Tres defectos del buscador que el criterio de cierre no habría delatado**, porque los tres borran vanos en silencio y un vano borrado es una zona incomunicada sin mensaje de error: (a) quedarse con el vecino *más cercano* de cada punta oculta todo empalme en T —la punta de `Wall_83` topa con `Wall_98` a 0,075 m y eso enterraba la boca oeste del pasillo, 2,393 m, cuyo sellado habría dejado a `robot2` encerrado en su ala—; (b) medir la línea de visión por distancia al *eje* declara bloqueado todo vano en T, porque el rayo muere sobre el eje de la segunda pared; (c) medirla por distancia sin más deja pasar rayos a lo largo del *cuerpo* de la propia pared. La prueba correcta es **punto-dentro-de-caja** con el rayo recortado medio grosor al final. Se detectaron comparando contra huecos verificados a mano, no con el mapa. **Criterio de cierre, el mismo del piso 1:** regenerar **sin** `--region`. Se contuvo: **0 celdas de frontera** de 40 573 libres —antes 270 en 210 sitios—, **0 obstáculos fantasma** de 7977, umbrales coherentes, y los diez puntos de control de la red navegable libres, del spawn de `robot2` al extremo este. **Diferencia con el mapa del 23-ago que hay que saber:** la cámara sureste ahora sale libre. No falta un panel —`Wall_130`/`Wall_154`/`Wall_153` cierran el ramal y `Wall_153`/`Wall_152`/`Wall_151`/`Wall_149` la cámara, pero ambos están abiertos al pasillo por el norte y no hay pared que los separe—: la región 5 llegaba a x = 21,71 y **recortaba espacio navegable de verdad**. El mapa nuevo es más fiel que el viejo. El buscador queda versionado en `herramientas/buscar_vanos.py` en vez de morir en `/tmp`, porque el comentario del `.sdf` afirma que la elección fue analítica y eso hay que poder rehacerlo; la orden exacta está en `GUIA_EJECUCION.md`. **Queda pendiente** regenerar los tres mapas antiguos (`primer_piso*.yaml`), que siguen en `free_thresh: 0.25` |
| 2026-08-25 | **Hito H3 cerrado: los dos agentes navegan a la vez, cada uno en su piso, y el `/odom` cruzado tenía la causa en la herramienta** | Evidencia completa en [`S20_hito_h3_dos_agentes.md`](Documentos/Evidencia/S20_hito_h3_dos_agentes.md). Las dos metas son **los dos puntos de transferencia** —`piso1_escalera` y `piso2_escalera`—, o sea el escenario del relevo y no dos destinos de conveniencia, y salieron **en paralelo** desde una sola orden para que los vehículos se movieran a la vez y no uno detrás de otro. Contra `/odom`: **0,281 m** para `robot1` y **0,143 m** para `robot2`. La comparación que decide el hito es la de `robot2` contra su propia corrida en solitario del 24-ago, **0,190 m**: correr dos `gzserver`, dos Nav2 y dos AMCL en la misma máquina **no degradó la navegación**. Con una corrida por condición eso no mide un efecto —la diferencia cabe en la variación entre corridas— pero descarta la degradación gruesa, que era la preocupación. **El hallazgo colateral nº4 de S17 se cierra, y con causa, no por no reproducirse:** el dominio 0 no lista **ningún** tópico `/robot2/*` y el dominio 2 ninguno `/robot1/*`, de modo que ninguna lectura correcta puede devolver los datos del otro robot; la publicación de `/odom` bajo espacio de nombres queda descartada y lo que quedaba era el daemon de `ros2cli`. La mitigación deja de ser «leer con `--no-daemon`» y pasa a **matarlo con `pkill -9` al cambiar de dominio** —`SIGTERM` no lo mata, cosa que también hubo que descubrir—; las métricas de OE4 se calculan sobre bags, que no pasan por el daemon. **Tres defectos nuevos, los tres silenciosos.** (a) **El vehículo sale de tolerancia después de haber llegado:** `robot1` estaba a 0,243 m cuando la acción devolvió `SUCCEEDED` y a **0,281 m** al cerrar el bag, con 93 s y **ni un solo `/cmd_vel`** entre medias, durante los cuales recorrió 4,0 cm. La `xy_goal_tolerance` es 0,25 m, o sea que el instante de la medición cambia el veredicto y **la tasa de éxito de OE4 no está bien definida** hasta fijar cuándo se lee la pose de llegada — decisión de `PROTOCOLO_EXPERIMENTAL.md`, antes de la campaña y no después. (b) **AMCL se vuelve más confiado y más equivocado** en la aproximación de `robot2`: converge hasta 0,022 m a media ruta y desde ahí el error **crece hasta 0,152 m mientras la covarianza sigue cayendo**, justo en la entrada de frente a `Barrera_Escalera` con `yaw = π`, que es donde apunta el cono ciego de **R13**. Segundo indicio en el mismo sitio y con la misma geometría; no reabre R13 pero es la medición que la campaña de S24 tiene que repetir. Hay que reconciliar antes las dos formas de medirlo, porque el 24-ago la descomposición de la TF `map → odom` dio 3,6 cm y hoy la comparación de `amcl_pose` contra `/odom` da 10,8 cm. **Y la mediana no sirve:** `analizar_maniobra.py` imprimió 0,101 y 0,102 m para los dos robots, dos cifras casi iguales que sugieren un error común y no lo hay —la serie de `robot1` es decreciente y la de `robot2` tiene forma de U—; para la campaña hay que reportar el error **en la llegada**, no la mediana del trayecto. (c) **La activación de los controladores es una carrera que bajo carga se pierde:** `robot2` levantó con **6 de 7** y el que faltaba aparecía en la lista pero `unconfigured`, porque `deepracer_spawn.launch.py` lanza los seis restantes **en paralelo** contra un solo `controller_manager`. Se recupera en caliente pasando por `inactive` antes de `active` —el ciclo de vida no admite el salto—, lo que confirma el diagnóstico. Aparece ahora porque es la primera vez que dos Gazebo compiten por la CPU. **No se secuenció el lanzador**, para no meter una variable nueva el día del hito: queda como decisión. Mientras tanto **`robot.sh <robot> estado` y su «7 de 7» es obligatorio antes de medir**, porque una bisagra de dirección inactiva no da error — cambia la cinemática en silencio, el mismo patrón del mapa equivocado del 21-ago |
| 2026-08-26 | **Los dieciséis destinos del piso 2 quedan escritos, y con ellos cae el cuarto prerrequisito de OE4** | Los nombres de las salas los dio Jonny con el recorrido que fija cuál es cuál —desde la escalera, ida por la pared norte de oeste a este y vuelta por la sur—. De los dieciocho vanos, el `1` es la escalera y el `I` mide 0,36 m, por donde el vehículo no cabe; los dieciséis restantes cuadran con los dieciséis nombres, nueve al norte y siete al sur. **Las paradas no son la columna cruda de `mapa_destinos.txt`**, donde las cuatro parejas enfrentadas comparten parada exacta con una `xy_goal_tolerance` de 0,25 m: se aplicó la regla del encabezado de `puntos_interes.yaml` sobre el mapa sellado, y la implementación se validó antes contra el piso 1 reproduciendo las cifras que ese encabezado predice (`y = 10.41` para ETM1, `9.67` para ETM13). Las parejas enfrentadas quedan a 0,90 m o más. **Dos desviaciones decididas y escritas en el YAML:** `Lab. Sistemas 313` e `IEEE` están en esquina, con una pared perpendicular a ~0,3 m de la abscisa de su vano; conservando la abscisa la primera obliga a parar a 3 m de su puerta y la segunda es imposible (holgura máxima 0,26 m, bajo el mínimo de 0,284 m de `verificar_pose_spawn.py`), así que en las dos se cede la abscisa y se conserva la holgura. **La pareja más junta queda a 0,38 m** (Aula 307 y Sala de Docentes), por encima de la tolerancia pero por debajo de su doble: ahí el pasillo se estrecha a 1,80 m y con 0,70 m a cada lado no quedan más que 0,40 m. Las quince paradas del piso 1 **no se tocan**; que ya no cumplan la regla sigue siendo decisión pendiente donde estaba. El catálogo pasa de 16 a 32 destinos y la prueba del planificador de 240 a **992 planes**, todos verificados |
| 2026-08-26 | **Seis de los veintitrés paneles del sellado sobraban, y dos tapiaban espacio navegable** | Detectado a ojo sobre RViz por Jonny: había paneles diagonales y sobredimensionados, cuando ninguna puerta real es diagonal y todas miden entre 0,86 y 1,14 m. **Cuáles sostenían el sellado se comprobó, no se supuso:** quitando cada candidato por separado y regenerando el mapa, seis dan las mismas 40 573 celdas libres y solo `20` y `21` abren fuga; después se comprobó que los seis fueran redundantes **juntos**, que es donde suele estar la trampa. Los dos que quedaban tapiaban de más: el `20` medía 1,88 m y cerraba la **hornacina entera** del Aula 306 en vez de su puerta —el hueco real son 0,755 m sobre la línea de pared `y = -3.42`, que en ese tramo se escalona hacia el norte—, y el `21` medía 2,14 m para un hueco de 0,355 m. Pasan a 0,85 y 0,45 m. El mapa sube de 40 573 a **40 760 celdas libres**: los 187 de diferencia son la hornacina, que era espacio navegable tapiado. Por eso se mueve también la parada del Aula 306, 0,18 m más cerca de su puerta. **Defecto encontrado de camino, sin arreglar:** al probar la eliminación en bloque sin comprobar, el relleno se desbordó —160 534 celdas libres, 119 % de cobertura en Y, suelo libre fuera del edificio— y `verificar_mapa.py` lo dio por **ACEPTADO**. Su comprobación de frontera pasa trivialmente cuando la fuga es *total*, porque no queda ninguna celda desconocida con la que hacer frontera. Bastaría rechazar por cobertura |
| 2026-08-26 | **La carrera de controladores era un reintento sobre una operación no idempotente, no el arranque en paralelo — cae el quinto prerrequisito de OE4** | La cura que dejó anotada el informe de H3 —secuenciar los seis `load_controller`— **se midió antes de aplicarla y no cura**. Línea base lanzando los dos robots con Nav2 a la vez, que es la condición en la que aparece: **2 fallos de 8** arranques dobles (25 %), 37–39 s. Encadenados: **1 de 6**, y el arranque sube a 51–57 s; el encadenado funciona —en el log cada proceso empieza cuando termina el anterior— y aun así falla, lo que descarta la concurrencia entre los seis como causa. **La causa está en `controller_manager/controller_manager_services.py`:** el cliente espera `call_timeout` (10 s) y, si no llega respuesta, **repite la misma petición** hasta tres veces; `load_controller` no es idempotente, así que si la primera llegó y solo tardó, la segunda recibe `was already loaded` y el cliente aborta antes de configurar y activar. Por eso el orden da igual: un solo cliente también puede pasar de 10 s con dos Gazebo y dos Nav2 en el mismo equipo. Se cambia a `controller_manager spawner`, **el único que expone ese timeout** —el CLI de `ros2 control` solo ofrece `--spin-time`, que además solo aplica sin daemon—, subido a 60 s; de paso acepta la lista entera y seis procesos pasan a uno. Resultado: **18 arranques dobles, 0 fallos**, y el arranque **baja** a 28–35 s (probabilidad 0,006 de sacarlos limpios por azar con la tasa base). **No se levanta la obligación de comprobar «7 de 7» antes de medir:** cero en 18 no es garantía y el fallo sigue siendo silencioso. **Las tres tandas se midieron sobre el mundo de 23 paneles**, anterior a la limpieza de esta misma fecha; la comparación es válida porque el antes y el después usaron el mismo mundo, y seis cajas estáticas no cambian la latencia del `controller_manager`, pero conviene saberlo si alguien repite la medida |
| 2026-08-26 | **La media vuelta de la llegada estaba en el plan, no en el comprobador de meta** (commit `cc43a9a`) | Después de R12 —`yaw_goal_tolerance: 3.15`, o sea el rumbo final ya no se impone— el vehículo **seguía** dando los 180° al llegar a ETM1. La explicación del 24-ago era incompleta: relajar el comprobador no podía arreglarlo porque **`SmacPlannerHybrid` planifica en SE(2)**, busca una *pose* y no un punto, y con `motion_model_for_search: "REEDS_SHEPP"` tiene primitivas de marcha atrás para satisfacer el rumbo pedido. **La maniobra va cosida dentro del camino**, así que el carro no entra en la tolerancia de 0,25 m hasta haberla terminado y el comprobador **nunca llega a opinar**. Lo que arregla esto en el planificador es `goal_heading_mode: BIDIRECTIONAL`, que existe **a partir de Jazzy** y el PC corre Humble; y bajar a un planificador 2D no es alternativa porque ignoraría el `minimum_turning_radius: 0.35`, que es la razón de haber elegido Hybrid. **Se arregla donde sí se puede, en el coordinador:** `_yaw_de_llegada` elige entre el `yaw` del catálogo y su opuesto según el **rumbo de aproximación** medido con `/odom`, y le pasa a Nav2 el que el vehículo ya trae. La regla escrita es que **el punto (x, y) es sagrado y el rumbo no**, que es coherente con haber declarado el `yaw` orientativo el 24-ago: **28 de los 31 puntos lo tienen en `0.0` por defecto**, o sea que nunca fue un dato, era el valor que quedó. Queda la salida `yaw_estricto` en el catálogo para los casos donde el rumbo sí importa —la escalera, donde hay que quedar de frente a la barrera—. Verificado por tres vías: llamada directa con un `self` postizo, análisis del bag (todos los planes del tramo 2 terminan ahora en −180°) y comprobación visual en Gazebo. **El cambio es puramente aditivo, 70 líneas y ninguna borrada.** Y produjo la regresión que abrió la fila siguiente: el error de llegada **empeoró** de 0,767 a 1,198 m, porque la media vuelta le estaba regalando a AMCL una corrección que nadie había pedido |
| 2026-08-26 | **El `sim ✅` de RF-13 se obtuvo contra un oráculo, y el GO/NO-GO de S21 estaba calibrado para no verlo** | Persiguiendo por qué el carro no para en ETM1 —0,767, 1,198 y 0,559 m en tres corridas, contra una tolerancia de 0,25— la causa no resultó ser control ni planificación sino **AMCL**. En el instante del `SUCCEEDED`: `/odom` en (−17,124, 10,590) y `/amcl_pose` en (−15,651, 10,594), **1,473 m de desfase y entero en x** (dy = +0,003). La serie completa da un pico de **1,977 m**, con `dy` que nunca pasa de 0,026 m, y la propia covarianza de AMCL lo confirma: **σx = 1,325 m contra σy = 0,213 m**, 6 a 1. **Lo que convierte esto en un hallazgo y no en un número feo:** en simulación `/odom` **no es una estimación**. El plugin lo publica desde `model_->WorldPose()` (`gazebo_ros_deepracer_drive.cpp:229`) y con `publish_odom_tf: true`, así que la TF `odom → base_link` es verdad de terreno. Luego `map → odom` —lo único que AMCL produce— **debería ser una constante**, y derivó 1,977 m: AMCL **fabricó** el error a partir de una entrada sin ruido. La causa es aliasing perceptual: en un tubo uniforme de 2,10 m la posición lateral está perfectamente restringida y la longitudinal no lo está. **De ahí lo que de verdad importa, que es de sim-to-real:** el DeepRacer **no lleva encoders de rueda** —dos cámaras RGB, un LiDAR plano y una IMU—, así que su única odometría es `rf2o_laser_odometry` (`deepracer_bringup/launch/deepracer.launch.py:90`), o sea **scan matching**, que falla en el **mismo eje** que AMCL y sin ninguna medida independiente que lo delate. Medido, no supuesto: rf2o corrido **fuera de línea** sobre el bag `~/tesis_evidencia/S20_localizacion/mision3`, sin tocar la simulación, registra **1,71 m de 29,94 m reales (5,7 %)** en el tramo este y **0,33 m de 24,98 m (1,3 %)** en el oeste. **No deriva: se congela.** Y no es un nodo roto — siguió bien los primeros 10,4 m desde el spawn (0,79 m de error) y el `yaw` es excelente todo el trayecto (2,8° final): la rotación y la traslación lateral son observables, la longitudinal no. **Decisión, y es la parte discutible: no se construye la solución ahora.** El pasillo simulado es una extrusión perfecta sin marcos de puerta, mobiliario ni gente, más pobre que el real, así que **5,7 % es un piso y no una estimación** — diseñar contra esa cifra sería ingeniería contra una magnitud desconocida, y las tres vías (IMU, odometría visual, aproximación relativa a landmarks) son cada una un proyecto. **Lo que sí se decide hoy es cómo se mide**, que es lo que tenía fecha de caducidad: el GO / NO-GO de S21 corre en el laboratorio GED, **16 m² donde cualquier método pasa**, y tal como estaba escrito habría devuelto un **GO falso**, con el fallo apareciendo en la campaña de S25 y el código congelado desde S23. Se parte en **G1** —lo que el laboratorio sí decide, con sus ramas Sí/No intactas— y **G2**, que saca la localización longitudinal fuera de su alcance y la manda a una corrida recta de **≥ 20 m** añadida al frente B de S20, con umbrales fijados antes de ver el dato: **M1 ≥ 0,90** (desplazamiento registrado ÷ real) y **M2 ≤ 0,50 m** (error de `amcl_pose` contra cinta). No es trabajo nuevo: es la misma salida de mapeo con el bag grabado. Y en `REQUISITOS.md` §7.4 queda escrito que el `sim ✅` de RF-13 **compara el oráculo consigo mismo**, o sea que da bien siempre y no informa. **Lo que ese asterisco NO invalida:** los errores de llegada medidos «contra `/odom`» —0,190 m en `piso2_escalera`, 0,281 y 0,143 del hito H3— siguen siendo válidos y son la mejor verdad de terreno disponible en simulación; miden control y localización, no el odómetro. Respaldo previo a la investigación en la etiqueta `respaldo-pre-rf2o` |
| 2026-08-27 | **El registrador de misión (RF-25) existe y produce un registro validado desde un bag** | Era el hueco que el §10 del protocolo señalaba como «no es un prerrequisito de este documento sino instrumentación». Cuatro piezas. El **esquema** deja de ser un documento y pasa a ser comprobable —JSON Schema versionado, con prueba—; las **marcas y el veredicto** se escriben como funciones puras, y al probarlas salieron **dos métricas de OE4 en negativo** que nadie habría visto sobre datos reales; `grabar_mision.sh` **sella el bag con tiempo de simulación** y no con reloj de pared, que es lo que exige el §3 del protocolo; y el **error de llegada se calcula solo**, contra `/odom` y en el robot que efectivamente llegó, no contra el `SUCCEEDED` de Nav2 (regla del 12-ago). Compuesto y validado un registro real de `S20_rutas_03`: `exito: true`, error de posición **0,1168 m**, 83,53 m recorridos, 8 cúspides, deriva `map→odom` 0,0097 m |
| 2026-08-27 | **El error de llegada no lo causaba el pasillo: eran los `alpha` de AMCL y una tolerancia con margen cero** (commits `b6808b0`) | Los dos defectos son de configuración y los dos se corrigen con un argumento que **no mira ningún resultado de llegada**, que es lo que los hace legítimos. **(a) Los `alpha`.** Estaban en `0.2`, declarándole a AMCL un 20 % de ruido de odometría sobre `/odom`, que en simulación es la pose exacta del motor de Gazebo. En un pasillo recto el láser fija la posición lateral y no dice nada de la longitudinal, así que ese ruido inventado se acumulaba en X. **(b) La tolerancia.** `xy_goal_tolerance` valía `0.25`, el **mismo número** con el que el coordinador juzga la llegada: margen cero. Nav2 para cuando *su estimación* baja de 0,25 y el veredicto se mide contra `/odom`. El instante quedó capturado en `S20_rutas_01`: se creía a **0,240 m** y estaba a **0,297 m**. Baja a `0.15`, con el presupuesto escrito en el YAML. **Validado con dos misiones completas de dos tramos:** `S20_rutas_03` llega a 0,204 y 0,117 m con tasa de deriva 0,0049 m/m y peor error de AMCL 0,407 m; `S20_rutas_04` a 0,153 y 0,129 m, tasa 0,0044, peor 0,374. Contra `S20_rutas_02`, con la configuración vieja: **0,429 m y ABORTADA**, tasa 0,0343, peor 1,473 m. **El crédito se reparte por argumento, no por corazonada:** `xy_goal_tolerance` no toca AMCL, así que la caída de ~7× en la deriva solo la pueden explicar los `alpha`; y el primer tramo de `S20_rutas_03` —creía 0,124, verdad 0,204— es el que muestra el efecto de la tolerancia, porque a 0,25 habría parado hacia 0,33 m y la misión habría salido FALLIDA. **El listón del protocolo sigue intacto en 0,25 m contra `/odom`:** se cambió lo que se le *pide* a Nav2, no con lo que se le *mide* |
| 2026-08-27 | **Tres bags perdidos en dos días, tres causas distintas, ninguna visible mientras se grababa — de ahí la compuerta de pre-misión** | `S20_piloto_01` se grabó entero contra una **pila muerta**: cero mensajes en `cmd_vel`, `amcl_pose` y `plan`, y no se notó hasta abrir el bag. `S20_piloto_02` arrancó desde una **condición inicial que no era la declarada** por reusar el `gzserver` de otra corrida. Y hoy se perdió una misión entera por **parámetros rancios**: se editaron los `alpha` y `xy_goal_tolerance`, se corrió y se analizó el bag como si midiera el cambio; el `gzserver` llevaba 1278 s vivo, o sea que se había lanzado antes de la edición. Los nodos leen su configuración **una vez, al arrancar**, y con `--symlink-install` no hace falta compilar pero sí **relanzar**: un YAML nuevo con una pila vieja se ve exactamente igual desde la terminal. Las tres comprobaciones se venían haciendo a mano o no haciéndose. Ahora son una sola orden encadenada antes de grabar (`esperar_nav2.sh`), y se niega a seguir si falta algo: 7 nodos de Nav2 y 7 controladores en `active`, parámetros vivos iguales a los del repo, y el robot donde dice la tabla de spawn. **El tercer chequeo destapó un hecho que hay que tener presente al planificar el día:** el modelo **resbala ~17 mm/min y ~1,4°/min** en Gazebo con los siete controladores activos y **cero `/cmd_vel`** —medido sobre 60 s—, y AMCL no se entera porque solo corrige cada 0,25 m o 0,2 rad. Con la pila ~48 min en reposo se midieron **0,643 m** de discrepancia entre Gazebo y `amcl_pose` con el robot **parado**. La condición inicial no depende de la misión: depende del **tiempo que la pila lleva quieta**, y se sale de la tolerancia de 0,15 m en unos 7–9 minutos. Dos misiones «idénticas» lanzadas con diez minutos de diferencia no parten de lo mismo |
| 2026-08-27 | **La pieza que decía «Nav2 se creía dentro» estaba midiendo una constante, y casi se reporta como hallazgo** | Se anota porque es un defecto de *instrumento*, que es la clase que produce conclusiones falsas con aspecto de medida. `diagnosticar_llegada.py` imprimía «Nav2 se creía a 0,240 m» en los tres bags disponibles; el número no era una medida sino la longitud del último tramo de rejilla de `SmacPlannerHybrid`, **idéntica siempre**. Y la creencia se leía de `/amcl_pose`, que solo se publica cada 0,25 m o 0,2 rad y en la parada está rancio —en `S20_rutas_03` daba una muestra de 1,1 s antes, 8,3 s antes del fin del bag—. Reescrita contra la transformada `map → odom`, que sí es continua (**2019 mensajes contra 305** de `amcl_pose` en el mismo bag) y contra el instante real de parada, la misma llegada da **0,032 m** en vez de 0,390 m. **De camino se estuvo a punto de reportar una truncadura del planificador que no existía:** el plan de `S20_rutas_01` acababa a 0,556 m de `piso1_escalera`, pero esa misión abortó en el tramo 1 y su meta era `piso1_representacion`, contra la que el plan acaba a **0,023 m**. Lo cazó correr la herramienta entera antes de escribir |
| 2026-08-27 | **`robot2` queda medido con la configuración nueva, y la concurrencia deja de estar respondida a 1,7 m** ([`S20_rutas_largas_y_concurrencia.md`](Documentos/Evidencia/S20_rutas_largas_y_concurrencia.md)) | La sospecha de partida —que las correcciones del día solo se habían aplicado a `robot1`— **era falsa**: `robot.sh` lanza para los dos el mismo `nav_amcl_demo_sim.launch.py`, que carga el mismo YAML. Lo que faltaba era **medir**. Y la hipótesis que motivó la corrida también resultó falsa: apretar la tolerancia a 0,15 tenía que hacer daño en `piso2_escalera`, la peor aproximación del sistema —de frente con `yaw = π`, con el cono ciego de R13 sobre `Barrera_Escalera`, y con un frenado tardío ya registrado el 24-ago—, y no lo hizo. `S20_p2_01`: **0,124 m en 90,81 m**, tasa 0,0039 m/m, peor AMCL 0,353 m, y en el instante de parada **0,020 m** de error de localización, la mejor cifra del proyecto. **El error de AMCL en el piso 2 no crece de forma monótona:** sube a 0,320 m a mitad del pasillo largo y **baja** a 0,052 m al final, cosa que en el tubo uniforme del piso 1 no pasaba; sigue estando todo en X —firma de R3— pero la forma de **S** del piso 2 devuelve geometría en los extremos y el filtro se reancla. **Repetido el hito H3 a escala de campaña:** sus rutas fueron de 1,67 y 1,75 m, y estas de 83,86 y 90,46 m. `robot2` mejora en las tres columnas (0,078 m, tasa 0,0023, peor 0,204); `robot1` empeora en la llegada (0,151 contra 0,117 y 0,129) y mejora en localización. Con una corrida por condición **eso no es una medida de efecto**, misma reserva que en H3; lo que queda descartado ahora a 90 m es la degradación gruesa. **Y hay un dato que parece concluyente y no lo es:** `robot2` tardó **186,7 s** las dos veces, idéntico a la décima, pero `tiempo_total_s` sale del reloj de **simulación**, que se frena junto con el simulador — prueba determinismo, no ausencia de contención. **Queda abierto el RTF bajo carga:** no se muestreó durante la corrida y lo único medido es con las dos pilas vivas y los dos vehículos **parados**, 0,993 y 0,994, que cumplen el RNF-06 por tres y cuatro milésimas. Es la carencia nº 2 del registrador con consecuencia concreta por primera vez |
| 2026-08-27 | **Tres arreglos de instrumentación, y uno de ellos era no tocar el número que parecía desfasado** | **(a)** `esperar_nav2.sh` se colgó **138 s** en `ros2 lifecycle get /robot2/controller_server` mientras ese mismo comando respondía `active` al instante desde otra terminal: un cliente creado mientras el servidor levanta su servicio puede no emparejar nunca y `wait_for_service` no tiene plazo. La espera ocurría **dentro** de la sustitución de comando, antes de la línea que evalúa el plazo de 120 s de la propia compuerta, así que no se rendía jamás. Acotada con `timeout`, que hace que la vuelta siguiente cree un cliente nuevo. **(b)** `diagnosticar_llegada.py` tenía el `--spawn` por defecto fijado a la pose de `robot1`, y con `--robot robot2` acusó **15,868 m** de desvío en una corrida que arrancó a **0,039 m** de su pose declarada. **(c)** El §3.3 del protocolo justificaba el criterio de éxito de 0,25 m diciendo que era la `xy_goal_tolerance`, que desde ayer vale 0,15. **El arreglo no era igualar los números otra vez:** la tolerancia decide cuándo Nav2 **para**, contra lo que **cree** AMCL, y el criterio decide cuándo la llegada se **acepta**, contra la verdad; igualarlos dejaba margen cero, que es lo que ya costó una llegada, y bajar el criterio a 0,15 habría declarado fallido el primer tramo de `S20_rutas_03`, que llegó a 0,204 m. Se corrigió la **justificación**, la tabla del §5 —`xy_goal_tolerance`, `yaw_goal_tolerance` y los `alpha` estaban obsoletos— y el comentario de `coordinador.py:51`, dejando el valor intacto. **Consecuencia declarada por fin:** la tabla del §5 dice que si un valor congelado cambia, las corridas anteriores dejan de ser comparables; cambiaron tres, así que **la campaña de condición A empieza el 2026-08-27** y los pilotos, `S20_rutas_01`, `S20_rutas_02` y `S20_localizacion` pasan a ser control de la configuración vieja |
| 2026-08-30 | **El relevo entre pisos se ejecuta: el aporte declarado deja de ser un plan** ([`S21_relevo_ejecutado.md`](Documentos/Evidencia/S21_relevo_ejecutado.md)) | `piso1_representacion` → `piso2_lab_313`, los dos robots vivos en un solo PC: `exito: true`, **1 relevo**, 47,6 s, con las seis etapas en orden. **Las llegadas se juzgan contra `/odom`, nunca contra el `SUCCEEDED`** (regla del 12-ago): **0,128 m** para `robot1` y **0,077 m** para `robot2`, contra un criterio de 0,25. **Que hubo relevo de verdad no lo dice el coordinador, lo dice el bag:** 373 mensajes en `/robot1/cmd_vel` y **580 en `/robot2/cmd_vel`** — el segundo robot fue *conducido*, no *notificado*. 54.322 mensajes en 16 tópicos, **`/robot2/clock` con 3.324** (el modo de fallo del 26-ago, que producía un bag vacío sin dar ningún error, queda cerrado), y de **94.981 transformadas de `/tf`, 0 con marco hijo sin prefijar**: los dos árboles comparten tópico sin colisionar, que era la duda abierta desde el 24-ago. **RTF 0,9981** con las dos pilas y los dos Nav2 arriba, o sea RNF-06 cumplido — y esto **contradice** la medida del 26-ago (0,955 y 0,811), sin que ninguna de las dos declarara si la GUI de Gazebo estaba abierta; queda como discrepancia abierta y no se usa para justificar nada. **Lo que NO cierra:** es **n = 1 y sin sortear**, luego la condición B sigue abierta y esto no sostiene ninguna afirmación sobre *tasa de éxito*. El hito 5 pasa a 🟢; la campaña, no |
| 2026-08-30 | **El bloqueo de dominios DDS se levanta, y la salida ya estaba escrita desde el 14-ago** ([`S21_bloqueo_dominios.md`](Documentos/Evidencia/S21_bloqueo_dominios.md)) | Los dos robots pasan al **dominio 0**. Lo que los separa ya no es el dominio sino los **nombres**: namespace por robot, prefijo por marco TF —incluido `map`—, puerto propio de `gzserver` y reloj propio. **Lo primero que se hizo fue releer, no probar**, y apareció que `S17_dos_simuladores.md` §4 ya había evaluado el dominio único el 14-ago y lo había descartado *por comodidad*, dejando escrito «se deja registrado por si en el futuro se necesita». O sea que no era un muro: era una decisión con salida documentada, y el 24-ago ya se había advertido que el aislamiento por dominio «solo aplazaba la colisión al punto donde más caro sale». **El diseño de relojes tiene que ser asimétrico y no por elegancia:** `ros2 bag record --use-sim-time` está cableado a `/clock` y **rechaza `--ros-args`**, así que remapear los dos relojes produce un bag con **cero mensajes y ningún error**; `robot1` conserva `/clock` como reloj de referencia y `robot2` usa `/robot2/clock`. Tres trampas más quedan anotadas allí, entre ellas que `-r` en `gzserver` **no** es remapeo sino `--record`, y arrancaba una grabación de estado en silencio |
| 2026-08-30 | **RF-22 tiene por fin una cifra, y una conclusión mía que los datos borraron** ([`S21_banco_tiempo_asignacion.md`](Documentos/Evidencia/S21_banco_tiempo_asignacion.md)) | Cierra el **séptimo ítem del §9** del protocolo. El bag no puede dar el tiempo de asignación —todo sello está cuantizado a 100 ms y la asignación tarda ~0,17 ms—, así que se mide **dentro del proceso** del coordinador con `time.perf_counter_ns()`, envolviendo `_marcar` y no `_publicar_estado`, que también lo llama el latido de 1 Hz. Cuatro corridas de 30 misiones: **mediana 154–175 µs, máximo 306,4 µs**, o sea **326 veces por debajo de un tic de `/clock`**. **Dos errores propios que quedan escritos porque estuvieron a punto de publicarse.** (1) Con tres corridas coincidentes se redactó una explicación causal convincente —que la rama inter-nivel de `planificar()` cuesta más que la intra-nivel, por 4 tramos contra 2—; **la cuarta corrida la invirtió**. La diferencia intra↔inter (~10 µs) es menor que la variación de la mediana **entre** corridas (~21 µs), luego el efecto no es resoluble con este diseño y no hacía falta para RF-22. (2) El propio banco **pisó su primera corrida en silencio** al escribir a un nombre fijo; corregido con la semilla en el nombre y una negativa a sobrescribir que se comprueba **antes** de levantar ROS. **Lo que el banco no prueba:** nada sobre el carro físico, nada sobre catálogos mayores, y no sustituye a la campaña |
| 2026-08-30 | **Las tres ramas de Jonny fusionadas, y la fusión creó un defecto que ninguna de las dos partes tenía** | `mando-vehiculo-fisico` entró limpia; `registrador-mision` chocó en siete sitios de `coordinador.py` por ser anterior al arreglo del 29-ago; `camara-y-dominio` chocó en `robot.sh`. **El defecto:** la mezcla textual dejó en `_marcar` el `publish` de una rama **más** la llamada a `_publicar_estado()` de la otra, que hacen lo mismo. Cada cambio de etapa habría salido **duplicado al bag** y el conteo de transiciones de la campaña habría salido al doble **sin dar ningún error**. Se detectó leyendo el método fusionado, no ejecutándolo. **Dos afirmaciones de la rama `camara-y-dominio` quedaron falsas el mismo día** y se corrigieron en su comentario en vez de borrarse: que el relevo no se puede demostrar de extremo a extremo en un solo equipo, y que el RTF cae por debajo de RNF-06 con las dos pilas en una máquina. Se conserva `DOMINIO_FORZADO` porque la idea de repartir en dos máquinas sigue siendo buena; lo que se quita es la premisa falsa. Verificado tras fusionar: las **seis** pruebas del repositorio pasan y el banco de RF-22, que envuelve `_marcar`, **sigue midiendo** |
| 2026-08-29 | **La asignación por nivel se ejecuta por primera vez de extremo a extremo, y el control negativo es la mitad que vale** ([`S20_asignacion_por_nivel.md`](Documentos/Evidencia/S20_asignacion_por_nivel.md)) | Una solicitud entra por la acción `GuiarUsuario`, el coordinador elige agente, se graba un bag por misión y `componer_registro.py` produce un registro JSON validado contra el esquema congelado. **Se lanzaron dos misiones y no una, a propósito:** con una sola no se puede distinguir «eligió bien» de «siempre responde lo mismo». M1 (`piso2_ieee` → `piso2_lab_313`) asignó `robot2` y llegó a **0,036 m** contra `/odom` con RTF 0,999; M2 (`piso1_representacion` → `piso1_etm2`) asignó **`robot1`, que es la respuesta correcta y distinta**, y falló limpio porque `robot1` vive en `ROS_DOMAIN_ID=0` y la sesión corría en el 2. **El fallo no es una corrida perdida: es la primera manifestación *medida* del bloqueo de dominios.** Las dos van marcadas `es_piloto: true` (§7 del protocolo), que es lo que impide que en octubre se mezclen con los datos de campaña. La corrida destapó cuatro defectos que no se ven leyendo código, y el más grave es el de la fila siguiente |
| 2026-08-29 | **El tiempo de asignación deja de valer cero por construcción — y al arreglarlo aparece que el reloj tampoco puede medirlo** (commits `8e870ac`, `9cfc52b`) | **El defecto.** `t_solicitud` y `t_robot_activo` salían **idénticos** (189,6 s en M1), y no por falta de resolución: `_marcar()` fijaba `etapa` y `robot_activo` en la misma llamada y publicaba **una** vez, así que **nunca existía un instante intermedio** entre «llegó la solicitud» y «ya hay agente». El *tiempo de asignación* es **una de las cuatro métricas de OE4** y habría valido cero en las 30 corridas de S24, se ejecutara lo que se ejecutara. **El arreglo.** Etapa nueva `RECIBIDA = 6`, publicada al aceptar el goal, **antes** de `planificar()` y con `robot_activo` todavía vacío ([`coordinador.py:172-200`](Robot/aws-deepracer/coordinacion/coordinacion/coordinador.py)). Las constantes son de compilación, no campos de cable, así que la serialización no cambia y **los bags viejos siguen componiendo**. **Verificado sobre bag, no declarado** ([`registros/S20_RECIBIDA_01.json`](Documentos/Evidencia/registros/S20_RECIBIDA_01.json)): donde había un mensaje ahora hay dos —`etapa=6 robot_activo=''` seguido de `etapa=1 robot_activo='robot2'`—, misión con éxito a 0,0467 m y RTF 0,9994. **Y aquí está lo que hay que saber decir en la sustentación:** los dos mensajes salen **con el mismo sello**. `/clock` de `gazebo_ros_init` late a **10 Hz**, `ros2 bag record --use-sim-time` sella con ese reloj, y por tanto **todo sello está cuantizado a 100 ms**; la asignación tarda ~0,14 ms, tres órdenes de magnitud por debajo de un tic. Subir `/clock` a los ~10 kHz que harían falta competiría por CPU justo donde el RNF-06 exige RTF ≥ 0,99. **El cambio de problema es el hallazgo:** antes era **defecto estructural** —el instante no existía—, ahora es **límite de instrumento** —el instante existe y el reloj no lo resuelve—, y son cosas distintas con remedios distintos. Por eso el §3.2 del protocolo se enmendó el mismo día: la métrica **no se reporta como valor por misión**, sino como **cota superior < 100 ms** en campaña más una **caracterización en banco** con `time.perf_counter_ns()` sobre el coordinador aislado, ≥ 30 repeticiones, mediana y máximo. El banco entra como **séptimo ítem del §9** y no existe todavía |
| 2026-08-31 | **El analizador de campaña destapa dos defectos que ninguna corrida habría revelado, y uno de ellos habría invalidado el experimento entero** | Cierra los **dos últimos pendientes del §9** del protocolo: `sortear_misiones.py` (§9.5) y `herramientas/analizar_campana.py` (§9.6), este último con **43 comprobaciones** que corren sin ROS, sin bags y sin simulador. **Defecto 1, el grave: RF-24 no tenía dónde escribirse.** El §2 del protocolo llama a la continuidad entre niveles «la variable de respuesta principal», y el registro compuesto **no llevaba ni un campo** para ella — solo el `hueco`, del que la continuidad se *deduciría* a mano. Llegar a S24 así habría significado **ejecutar las 30 misiones sin medir la métrica que justifica el experimento**, y darse cuenta después, con la congelación de código ya pasada. Corregido con el esquema **1.1.0** y `continuidad_de()` en el compositor. **Tres decisiones de diseño que no son obvias y quedan escritas en el §3.7.1 del esquema:** (a) la ventana **no** puede ser la literal del §3.4 —`[t_solicitud, t_completada]`—, porque el estado `RECIBIDA` se publica con `robot_activo` vacío **por diseño**, así que la definición literal declara discontinua **hasta la misión perfecta** y RF-24 valdría 0 % por construcción; arranca en `t_robot_activo`, y el tramo excluido es exactamente el que cubre RF-22. Es **el mismo defecto estructural** que tenía el tiempo de asignación hasta el 29-ago: una métrica que devuelve el mismo número pase lo que pase mide la definición, no el sistema. (b) La continuidad vive **dentro** de `veredicto` pero **fuera** del AND que decide `exito`: el §3.3 lista tres condiciones y esta no es una, así que una misión puede cumplirlas las tres y aun así haber tenido un hueco de coordinación. (c) `null` **no** es `false`: sin `t_completada` la ventana no se puede cerrar, y llamarla «discontinua» confundiría *no terminó* con *nadie estaba a cargo*, que ya lo cuenta otra condición. **Defecto 2: el intervalo de confianza del protocolo no se reproducía.** El §6.1 y el §11 prometían «del 80 % al 97 %» para 27/30; **Wilson al 95 % da 74,4–96,5 %**. El 97 es Wilson y el 80 es Wald — dos métodos mezclados en un mismo intervalo. El §11 había fundado sobre esa cota mala la regla *«"la tasa supera el 80 %" es defendible»*, y **no lo es**: harían falta 28 aciertos. Corregido a **74 %** en los dos sitios, con el método nombrado por fin de forma explícita —se dedujo del ejemplo 4/5 → «38 % al 96 %» del propio §6.1, que solo Wilson reproduce— y con el analizador imprimiendo la frase defendible **calculada**, no escrita a mano. **El intervalo real es más ancho que el prometido, no más estrecho: nadie sale beneficiado del error.** **Defecto 3, encontrado al probar el analizador contra los registros de verdad y no contra los sintéticos:** un lote con **cero corridas válidas** salía `VEREDICTO: VALIDA` y con **código de salida 0**. El §7 del runbook manda correr el analizador **después de cada misión** y encadenar con ese código, así que un `--campana` mal escrito o un directorio equivocado habrían devuelto la señal más fuerte que el programa sabe emitir **desde ninguna evidencia**. Corregido: `n == 0` invalida y explica de qué se compone el cero —pilotos, descartadas, con errores—, porque una campaña vacía no es buena ni mala, es que no hay campaña. **Por qué esto entra hoy y no rompe nada:** el cambio de esquema es aditivo, luego sube **minor**, y su `required` va condicionado a `esquema_version == "1.1.0"` dentro del `allOf`, así que **los tres registros de S20 siguen validando en 1.0.0 sin tocarlos** — evidencia ya entregada que no se reescribe. Y llega con **cero corridas de campaña ejecutadas**, que es la única ventana en que un cambio así es gratis |
| 2026-08-31 | **El Taller 2 obliga a mirar el anteproyecto de frente, y el peor criterio de su propia lista es la metodología** ([`TALLER2_TRANSICION.md`](Documentos/TALLER2_TRANSICION.md)) | Diligenciado el corte 1 del Taller 2 —auditoría de las secciones heredadas, matriz objetivo–evidencia, metodología proyectada contra ejecutada, y proyección de los capítulos de desarrollo y de resultados—. El formato traía **tres** objetivos específicos y el anteproyecto aprobado define **cuatro**: se agregó la fila **OE4** en todas las matrices en lugar de omitirla, porque OE4 es donde vive la evidencia que el jurado va a pedir. Lo que sale de ahí y no estaba escrito en ningún otro sitio: **tres desviaciones que necesitan aval del director** —plataforma (§4.2 y §4.3 dicen *«DonkeyCar»*, se trabaja con **AWS DeepRacer**, y el propio §7.2 del anteproyecto ya nombraba el DeepRacer, o sea que **el documento se contradecía a sí mismo desde antes**), interfaz (§4.3 promete *«aplicación móvil»*, lo acordado es **web responsiva por QR**) y métricas (§7.4 pide *«tiempo por fuera de rutas preestablecidas»*, que **nunca se midió ni se va a medir**)—, con fecha límite **antes del 2026-09-06**, o sea **esta semana**. De los catorce criterios de calidad, el único en **NO** rotundo es *«la metodología describe lo realmente ejecutado»*: §7.2 y §7.3 nombran **CoppeliaSim, OpenCV, Jetson Nano y Raspbian**, ninguno usado. Y el segundo agujero es **OE3**, el único objetivo sin ninguna evidencia asociada — que es exactamente lo que S22 tiene que producir |
| 2026-09-01 | **La corrida del relevo pasa a dato agregable, y los dos productores del registro resultan no producir el mismo registro** | **Punto 1.** El resultado central del proyecto existía como bag y como narración, no como algo que la campaña pudiera agregar; ya está en [`registros/S21_dominio_unico_001.json`](Documentos/Evidencia/registros/S21_dominio_unico_001.json), validado contra el esquema 1.1.0. **Componerlo destapó dos defectos, los dos arreglados antes de escribir el artefacto.** (a) **RF-24 salía `continua: false` en una misión que nunca perdió la custodia.** `continuidad_de()` recortaba la ventana **comparando tiempos** para excluir el `RECIBIDA` de agente vacío, y en esta misión `RECIBIDA` y `TRAMO_1` comparten sello —los dos en t = 402,400— porque `/clock` va a 10 Hz y la asignación tarda microsegundos. Como eso pasa en **toda** misión, RF-24 habría dado **0 % de continuidad por construcción**: es el mismo defecto estructural que tuvo el tiempo de asignación hasta el 29-ago, **reintroducido por la forma misma de evitarlo** —el docstring escrito para prevenirlo lo causaba—. La ventana se recorta ahora **por posición en la secuencia**, con un guarda explícito para que un `COMPLETADA` anterior al arranque no devuelva `continua: true` por no haber mirado nada, que sería un falso positivo silencioso en la variable de respuesta principal. La prueba anterior no lo cazaba porque usaba 10,15 y 10,2, dos tics distintos. (b) **La procedencia guardaba media escena:** desde el 23-ago el mundo es del **nivel**, así que una condición B usa dos mundos y dos mapas; se añade `escenario_por_robot`, **opcional** para no invalidar lo ya compuesto. **Y la comprobación que el runbook arrastraba desde el 30-ago, hecha por fin.** El §6 mandaba un `diff` entre `registrador.py` (vivo) y `componer_registro.py` (del bag) partiendo de que *«los dos declaran el mismo esquema congelado»*. **La premisa era falsa**, medido y no deducido: validando la salida de `a_dict()` contra el esquema salen **15 errores**, y de las **10 claves de primer nivel que el esquema exige el vivo trae 2**. Además llama `condicion` al **banco** (`simulacion`/`hardware`) donde el esquema usa esa palabra para **A/B**, **infiere** la condición del resultado en vez de tomarla del sorteo —una B que falla antes de la transferencia se juzga como A y su `c3` pasa gratis—, y convierte a `false` lo que no sabe, que es el defecto que el compositor corrigió el 29-ago. **Lo que decide es la resolución:** el coordinador publica a 1 Hz pero solo llama a `registro.marca()` desde `_cambiar_etapa`, así que el vivo recorre **~6 marcas** y el compositor los **~339 mensajes** publicados; si `robot_activo` se vaciara *entre* dos transiciones, el bag lo tendría y el vivo no anotaría nada. **El productor vivo es ciego, por construcción, al modo de fallo que RF-24 existe para detectar.** Se declara autoritativo el del bag y se retira `ruta_registros` **también del piloto**: encenderlo solo gasta CPU contra dos Gazebo justo donde RNF-06 exige RTF ≥ 0,99. No se pierde nada —`num_relevos` sale de `t_inicio_tramo2`, la pose de destino de `puntos_interes.yaml`, `punto_id` de `EstadoMision`—, y el único dato exclusivo del vivo era el resultado de la acción, que el §3.3 **prohíbe** usar como evidencia. El coordinador no se toca; `metricas()` queda como código muerto que nadie debe citar |
| 2026-09-01 | **El G2 del viernes no se podía analizar en este PC, y no se habría visto hasta tener el carro montado** ([`S21_preparacion_G2.md`](Documentos/Evidencia/S21_preparacion_G2.md)) | El G2 es lo único que impide que el GO/NO-GO de S21 devuelva un **GO falso**, y está en el cronograma para el viernes 4 por la mañana. Se revisó **tres días antes** si esa mañana podía salir. **No podía, por tres cosas y ninguna visible desde el calendario.** (1) **Los bags del carro no se abren aquí.** La tarjeta corre Jazzy, el PC Humble, y falla por el sitio menos evidente: no por el `.mcap`, que es autodescriptivo, sino por el `metadata.yaml`. Jazzy la escribe en **versión 9**, con `offered_qos_profiles` como secuencia anidada y los enums en texto; Humble espera ahí una **cadena** con enteros de `rmw/types.h`, y su `yaml-cpp` aborta con `bad conversion` en la línea 15 —que es exactamente el guion del primer elemento—. Resuelto con `herramientas/adaptar_bag_jazzy.py`, que traduce la metadata y deja el bag adaptado aparte con **enlaces simbólicos**: la evidencia del carro no se toca ni se duplica. 24 comprobaciones, incluida la metadata real del 28-ago, y **rechaza en vez de adivinar** ante un enum fuera de tabla o una metadata ya adaptada. (2) **Falta `ros-humble-rosbag2-storage-mcap`**, bloqueo **independiente**: con la metadata ya arreglada el error *cambia* a `Could not load/open plugin`, que es la prueba de que el primero está quitado. Pide `sudo` y queda como preparación con fecha. (3) **El mapa que M2 necesita no existe y no se puede construir con lo que hay:** los cinco bags del 28-ago traen **un solo tópico**, `/rplidar_ros/scan`, y sin `/tf` ni `/odom` no hay `slam_toolbox` fuera de línea; a `bag_mapa_1456` le falta además la metadata entera. **El viernes son dos pasadas, no una** —mapa con SLAM vivo, luego AMCL contra ese mapa—, y planificarlo como una sola es media jornada perdida. **Y los dos umbrales están mal puestos, que es lo que de verdad importa.** Evaluados contra los números ya medidos: el circuito de 18,4 m contra flexómetro da ratio **1,029**, y los dos tramos de rf2o fuera de línea **0,943** y **0,987** — **los tres pasan M1**. **M1 no puede fallar:** tolera un 10 % cuando el peor error medido es 5,7 %, y al estar escrito **a un solo lado** el **+2,9 % largo** —el defecto real, el del hallazgo «repetibilidad no es calibración»— pasa por definición, y pasaría a +50 %. Es **la crítica del 26-ago —«calibrado para no verlo»— reaparecida en el criterio que se escribió para corregirla.** Y **el resultado de M2 ya está determinado antes de correrlo**: 0,029 × 20 m = **0,58 m** contra un umbral de 0,50, con R3 diciendo que AMCL no corregirá ese eje. Se propone M1 de dos lados (`\|ratio − 1\| ≤ 0,10`) y M2 como **medida y no como puerta** —si falla, R3 ya está documentado y ya se decidió no construir la solución ahora, así que un NO-GO ahí no manda parar nada—. **Queda propuesto, no decidido:** es decisión de protocolo y el §6.3 obliga a tomarla **antes** de ver el dato |
| 2026-09-01 | **La razón por la que no hay mapa del pasillo no era la que estaba escrita: el 84 % de lo grabado es un LiDAR quieto** ([`S21_preparacion_G2.md`](Documentos/Evidencia/S21_preparacion_G2.md) §3, [`GUIA_PASADA_MAPEO.md`](Documentos/GUIA_PASADA_MAPEO.md)) | El registro decía que los bags del 28-ago no servían **por faltarles `/tf` y `/odom`**. Se intentó salvarlos generando la odometría ausente: reproducirlos contra `rf2o_laser_odometry` —única fuente de odometría del carro, que no lleva encoders— y meter el resultado en `slam_toolbox`. **La cadena funciona; lo que no hay es materia prima.** De **812,8 s grabados, 682,6 s son el sensor quieto en el suelo (84,0 %)**, y los **130,2 s** de movimiento están fragmentados. **Aunque se hubiera grabado la TF el mapa no existiría:** un sensor que no se traslada no da de dónde triangular estructura, y `bag_mapa_1451` por la cadena entera produce un **disco radial de 11,30 × 10,90 m, 1.168 píxeles ocupados y ni una sola pared**. **Se midió sin odometría a propósito:** barridos separados **1 s** y **mediana** de la diferencia entre rayos finitos en ambos —usar `rf2o` para juzgar si hubo movimiento sería circular, porque es justo lo que R3 dice que se congela aquí; mediana y no media porque una persona cruzando mueve pocos rayos muchísimo—. **Y esto despeja a `rf2o` de la sospecha:** sus 3,37 m sobre `1451` corresponden al poco movimiento que de verdad hubo, luego **no se congeló**, y la pregunta de R3 sobre el pasillo real **sigue abierta y solo se contesta yendo** — que es mejor que haberla dado por contestada en falso. **`bag_mapa_1456` resulta recuperable y ser el más largo de los cinco** (404,4 s): al `.mcap` se le apunta directo con el lector, y el aviso `no message indices found` **es** la explicación de la metadata ausente —el índice se escribe al cerrar limpio, luego esa grabación se cortó de golpe—. Pero **leer no es reproducir**: `ros2 bag play` sobre un `.mcap` suelto muere con `yaml-cpp: bad conversion` (comprobado en 1447 y 1456 con el plugin ya puesto), así que sigue haciendo falta `adaptar_bag_jazzy.py`. **Quedan tres artefactos que convierten una mañana perdida en una comprobación de cinco minutos:** [`comprobar_movimiento_bag.py`](herramientas/comprobar_movimiento_bag.py) —la comprobación que faltó el 28-ago, 20 pruebas sin ROS—, [`mapear_desde_bag.sh`](herramientas/mapear_desde_bag.sh) —la cadena entera, verificada de extremo a extremo, **repetible sin volver al pasillo**— y la guía de la pasada, que codifica como pasos obligatorios las **tres** causas del 28-ago: tópico bajo el espacio de nombres de AWS, sensor quieto y grabación cortada. **Dos defectos se encontraron leyendo, no ejecutando.** (1) La TF `base_link → laser` del guion estaba mal por dos motivos a la vez: el URDF cuelga el sensor de `chassis`, no de `base_link`, así que copiar el `0,16145` del xacro deja el sensor **23 mm bajo**, y el `rpy 0 0 3,1416` olvidado habría construido **el mapa girado 180°** respecto al que AMCL vería después, sin que nada dijera por qué. Compuesto: `x 0,02913, z 0,184699, yaw π`. (2) `verificar_repositorio.sh` daba 11/12 marcando un `scp -r` como si fuera `cp -r`: era un **defecto de la regla** —coincidencia por subcadena— y se arregló con frontera de palabra en vez de reescribir el documento. **Trampa que costó una corrida y queda cableada en el guion:** un `ros2 bag play` huérfano deja **dos publicadores de `/clock`**, el tiempo salta hacia atrás y el mapa sale roto **sin ningún error**; el guion mata los nodos antes y después y avisa si aparece `jump back in time`. **Lo que esto NO cierra:** se invocó `sync_slam_toolbox_node` directamente, no `slam_toolbox.launch.py`, así que su argumento `clock_topic` **sigue escrito, comiteado y nunca corrido** |
| 2026-09-01 | **El teleop con mando funciona sobre el vehículo real, y de camino salieron cuatro restricciones operativas que ningún documento recogía** | Se condujo el DeepRacer físico con el Pro Controller, con las cuatro protecciones comprobadas sobre el carro en alto. **Lo que importa no es que funcione, son las cuatro cosas que hubo que descubrir para que funcionara, y que afectan al G2 del viernes.** **(a) Todo lo nuestro en el carro tiene que correr como `root`, y sin `root` falla sin decir nada.** El descubrimiento de Fast DDS en ese dominio pasa por `/dev/shm/fastrtps_port700N`, buzones que `deepracer-core` crea al arrancar como `root` con permisos `-rw-r--r--`: un proceso de usuario los lee pero **no puede escribir su anuncio de participante**, así que para el resto del grafo *nunca existió* — ni excepción, ni `WARN`. Medido: publicador de usuario contra suscriptor de la pila, **0 mensajes**; publicador `root`, **56**. La lectura intermedia que parecía contradecirlo —dos procesos de usuario con `ROS_AUTOMATIC_DISCOVERY_RANGE=LOCALHOST` sí se hablan, 55 y 33 mensajes— la confirma: `LOCALHOST` va por UDP a `127.0.0.1` y **se salta `/dev/shm`**, por eso funciona entre pares de usuario y por eso mismo no sirve para hablar con la pila. **Consecuencia directa para el viernes: `ros2 bag record` también es nuestro y también necesita `sudo`.** **(b) El descubrimiento no es determinista para un participante recién nacido.** Contra un grafo estático, `ros2 topic list --no-daemon` devolvió **2, 10 y 17** tópicos en tres corridas seguidas; con el demonio levantado, **22, 22 y 22**. `ros2 bag record` es exactamente eso, un participante recién nacido, así que puede suscribirse a un subconjunto —o a nada— y decir `Recording...` igual de contento. Es el **candidato más fuerte** a explicar los bags del 28-ago con un solo tópico y uno sin `metadata.yaml`; se anota como candidato y no como diagnóstico cerrado, porque aquel bag sí capturó `/rplidar_ros/scan`. **(c) Enchufar cualquier cosa por USB con la pila corriendo mata el LiDAR en silencio**: el bus se re-enumera, el nodo de `/dev/ttyUSB0` se borra y `rplidar_node` se queda con un descriptor abierto a `/dev/ttyUSB0 (deleted)` publicando nada, mientras `systemctl` insiste en `active (running)`. Es la **tercera vez** que un `active` miente en este proyecto. **(d) El carro no se puede reiniciar**: se reinició una vez y **se quedó en GRUB pidiendo qué sistema arrancar**, sin salida por SSH; hubo que resolverlo físicamente. En el pasillo no va a haber monitor. **Y un hecho que corrige un documento:** la pila **no publica `/odom`, `/tf` ni `/tf_static`** —comprobado contra la lista estable de 22 tópicos—, que es justo lo que `ESTADO.md` exigía grabar el viernes. No es una avería: `GUIA_PASADA_MAPEO.md` §0.2 ya había elegido a propósito la cadena B, donde el carro graba solo `/scan` y `/odom` y la TF se generan después con `rf2o`. **El que estaba equivocado era este tablero**, corregido en el §7. Del lado del programa, los cuatro índices de eje y las dos banderas de inversión pasan de supuestos a **medidos**: `invertir_traccion` estaba en `True` y con eso el stick hacia adelante daba marcha atrás, comprobado sin el mando de por medio publicando tracción conocida con las ruedas en el aire. Y el «gatillo sin estrenar que se lee `0.0`», que era una hipótesis defensiva con prueba escrita, **ocurrió**: con el umbral en `-0,5` cuenta como *sin pulsar*; con un umbral positivo el carro habría arrancado habilitado él solo. Todo esto reescribe [`GUIA_TELEOP_MANDO.md`](Documentos/GUIA_TELEOP_MANDO.md) alrededor del camino que de verdad se midió —mando por USB al carro, todo como `root`— y añade el Paso 2.5 a [`GUIA_PASADA_MAPEO.md`](Documentos/GUIA_PASADA_MAPEO.md) |
| 2026-09-03 | **La misión sorteada ya estaba corrida desde el 30-ago, y el piloto de condición B también: cinco bags llevaban cuatro días sin figurar en ningún documento** | Aparecieron buscando el comando de solicitud para escribir una guía de misión que **resultó existir ya**: [`RUNBOOK_CAMPANA.md`](Documentos/RUNBOOK_CAMPANA.md) cubre el procedimiento entero y está **más al día** que lo que se iba a escribir —usa `herramientas/robot.sh robot1 nav2`, no los comandos del Escenario F—. Escribir la segunda guía habría creado una **segunda fuente de verdad del mismo procedimiento**. En `~/tesis_evidencia/` hay **cinco grabaciones del 30-ago entre las 16:35 y las 17:45** (`S21_piloto_A_01`, `A_02`, `B_01`, `C_01`, `C_02`) que **no se citan en este tablero ni en ningún archivo de evidencia** y que nunca se compusieron. Pasadas por el compositor, **dos son aprovechables y tres no.** `S21_piloto_A_02` **es la fila 1 del listado sorteado** (`piso1_etm2 → piso1_etm11`, condición A): RTF **0,9988**, error de llegada **0,215 m**, 52,8 s, deriva `map→odom` 0,041 m, `exito: true`. `S21_piloto_B_01` es `piso1_representacion → piso2_ieee`, **condición B con relevo real** —tres tramos, el tercero de `robot2`—: RTF **0,9915**, error **0,120 m**, 36,4 s, `c3_relevo: true` y **continuidad íntegra** en la ventana 181,3–217,7 s. Es decir: la actividad *«repetir la misión ya sorteada»* **no hay que ejecutarla, hay que componerla**, y el piloto de condición B que el §9.3 del runbook daba por pendiente **ya existe**. Los otros tres **no son recuperables como pilotos**, y el compositor lo explica sin que haya que deducirlo: les falta `rtf.json` y el bag **no puede darlo**, porque con `--use-sim-time` sella todo en tiempo de simulación y sim/pared vale **1 por construcción**; la simulación que los grabó ya se cerró, luego ese RTF se perdió. **Lo que esto NO cierra, que es la parte incómoda:** de los cinco criterios del §8 del runbook, el **criterio 1 —pose inicial de los dos robots dentro de 0,15 m— es una compuerta *previa* a la corrida y no queda guardada en el bag** (`controladores_activos` sale `{}` en los dos registros), así que para estas dos corridas es **irreconstruible** y solo se pueden dar por buenos **4 de 5**. Que el criterio 1 no sobreviva al bag es un defecto del instrumento, no de estas corridas, y afecta igual a las 30 de S24. **Y una cifra que conviene no pasar por alto:** la deriva `map→odom` de `B_01` es **0,730 m**, dieciocho veces la de `A_02`, sobre una misión que dura **menos**; el error de llegada salió bien igual, pero ésa es la magnitud que vigila R3 |
| 2026-09-03 | **El pasillo del G2 queda medido y marcado, y no es el que se creía: es un pasillo de pruebas, medido con el iPhone porque no hay flexómetro** | **Montaje cerrado el jueves 3, antes de salir el viernes:** recta de **20,08 m**, ancho **2,7 m**, eje longitudinal a **1,1 m del muro izquierdo y el mismo número en los dos extremos** —o sea trayectoria paralela al muro, sin error lateral inducido—, y **dos cruces** de punto, en el 0 m y en el 20,08 m. No lleva la tercera cruz de los 20,00 m porque esa regla solo entra a partir de 25 m de recta. **No es el pasillo del edificio que está modelado en simulación, y da igual para G2:** la cadena del viernes es **autorreferente al pasillo que se use** —la pasada 1 construye el mapa de ese mismo pasillo y la pasada 2 corre AMCL contra ese mapa—, así que el mundo simulado no entra en ningún eslabón. Lo que G2 contesta es una propiedad **del robot**: si `rf2o` se congela en el eje longitudinal con geometría real y cuánto recupera AMCL. La correspondencia modelo↔edificio del §10 de [`ENTORNO_DE_EVALUACION.md`](Documentos/ENTORNO_DE_EVALUACION.md) **sigue siendo un supuesto no verificado**, pero no bloquea el viernes; se confundieron las dos cosas en la conversación y conviene que quede separado. **La salvedad seria, y se declara en vez de maquillarse:** los 20,08 m salen de la app de medición del iPhone —**odometría visual-inercial**—, no de flexómetro, porque no hay flexómetro disponible. Incertidumbre estimada en **~0,20 m** (1 % sobre 20 m) y **no caracterizada**. Eso se come el **40 % del presupuesto de 0,50 m de M2**, luego **M2 no puede declararse como puerta** por una razón nueva e **independiente** de la del §4 de [`S21_preparacion_G2.md`](Documentos/Evidencia/S21_preparacion_G2.md): allí no gateaba porque su veredicto era predecible (0,029 × 20 = 0,58 > 0,50); aquí, además, **el veredicto no es resoluble con el instrumento que hay**. Las dos razones convergen en la propuesta (c) del Paso 1.3 —M2 pasa de puerta a medida— y la refuerzan. En M1 pesa mucho menos: 1 % contra una tolerancia del 10 %. **Y queda apuntada una amenaza a la validez que merece ir al §11 del protocolo:** el instrumento de verdad de terreno **falla por el mismo mecanismo que el sujeto medido** —la app estima el avance comparando fotogramas, `rf2o` comparando barridos, y las dos derivan en el **eje longitudinal** y empeoran en un pasillo largo, uniforme y pobre en rasgos—. Si los dos derivaran en el mismo sentido, M1 saldría bien sin significar nada. Es error de modo común, y es exactamente la familia de defecto que el 26-ago se resumió como «calibrado para no verlo» |
| 2026-09-03 | **El `clock_topic` de `slam_toolbox` sí funciona —y al ejercitarlo apareció que `ros2_control` nunca estuvo cubierto por ese remapeo** | El `clock_topic` era **la única ruta de código escrita, comiteada y nunca ejecutada** del arreglo de relojes. Se corrió sobre una pila viva de `robot2` (`herramientas/robot.sh robot2 sim gui:=false` y luego `robot2 slam`) y **pasa**: `/robot2/slam_toolbox` queda suscrito a **`/robot2/clock`**, no a `/clock`; publica mapa real de **124 × 86 celdas a 0,05 m** con `frame_id: robot2/map`; y el TF sale con prefijo (`robot2/map → robot2/odom`), luego no choca con `robot1`. **Lo que no se esperaba:** `/clock` global tenía **0 publicadores y 8 suscriptores**, y los ocho eran nodos de `ros2_control` de `robot2` —`controller_manager` y los 7 controladores—, mientras `/robot2/clock` tenía 1 publicador y 5 suscriptores, los plugins de Gazebo. La causa está en [`deepracer_sim.launch.py`](Robot/aws-deepracer/deepracer_bringup/launch/deepracer_sim.launch.py): el remapeo `/clock:=/robot2/clock` se añade a la **línea de comandos de `gzserver`**, así que alcanza a los plugins que corren dentro de ese proceso y **no** a `controller_manager`, que `gazebo_ros2_control` crea con sus propias `NodeOptions`. **Hoy no está roto**, y conviene decir por qué para no arreglar lo que no toca: los sellos de `/robot2/joint_states` son tiempo de simulación válido (43,764 s contra 42,213 s de `/robot2/scan`) porque `gazebo_ros2_control` le pasa el tiempo de Gazebo **explícitamente** a `controller_manager::update()`, sin pasar por el reloj del nodo. **El riesgo es latente y solo se manifiesta con las dos pilas arriba:** ahí `robot1` sí publica `/clock`, así que cualquier `node->now()` interno de los controladores de `robot2` devolvería el reloj de `robot1` —en la sesión anterior se midieron desfases de **16 s y 143 s**—. Ningún dato de las campañas depende hoy de esa llamada; queda anotado para que, si algún día un controlador sella algo por su cuenta, se sepa de entrada dónde mirar |
| 2026-09-04 | **Cae el bloqueo de los bags vacíos que venía del 28-ago: el primer bag válido del proyecto, y con él dos correcciones a lo que este tablero daba por sabido** | **El hecho:** `mapa_pasillo_0012`, **918 barridos de `/scan` en 141,3 s a 6,49 Hz**, 2,7 MB, **66,7 % de sensor en movimiento** y `SIRVE` según `comprobar_movimiento_bag.py`. Es el primer bag de este proyecto que no está vacío ni congelado. **La causa raíz no era «grabar como `root`», que es lo que decían las guías:** el transporte del dominio pasa por buzones `/dev/shm/fastrtps_port70NN` con permisos `-rw-r--r--`, y **el publicador tiene que poder escribir en el buzón del suscriptor**, luego la condición es **que los dos extremos tengan el mismo dueño**. Medido en cuatro corridas: publicador `root` → grabador usuario, **0**; publicador `deepracer` → grabador `root`, **0** (dos veces, los bags de las 23:25 y 23:29, ambos de **5123 bytes exactos** pese a durar 22 s y 166 s); publicador `deepracer` → grabador `deepracer`, **60 en 9 s** y **918 en 141 s**. La guía anterior había medido solo el primer caso y generalizó al revés; el 3-sep el LiDAR se arrancó a mano por el Paso 2.3 —que no lleva `sudo`— y los papeles se invirtieron. **Y la regla es más grande que `bag record`: vale igual para `ros2 topic hz` y `ros2 topic echo`, y engañó dos veces en tres horas.** La segunda fue midiendo `ros2 topic hz /rplidar_ros/scan` desde el usuario contra un publicador de `root`, que dio un falso «no publica»; repetida como `root`, el negativo sí resultó válido. **El fallo siempre es silencio, no error:** el tópico aparece en `ros2 topic list` —el descubrimiento sí cruza dueños— y luego no llega un dato. **Segundo hallazgo, colateral y no resuelto: hay dos drivers del LiDAR con `/dev/ttyUSB0` abierto a la vez**, comprobado en `/proc/<pid>/fd` — el `rplidar_node` de `deepracer-core` (`root`, espacio `/rplidar_ros`) y el `rplidar_composition` del Paso 2.3 (`deepracer`, sin espacio). El de AWS **tiene el puerto cogido y no publica ni un barrido**, verificado como `root`. No impide grabar, pero **cierra la vía «grabar `/rplidar_ros/scan` y remapear fuera de línea»**, que parecía la salida limpia: ese tópico está vacío. **Tercer hallazgo, y es el que queda abierto: el mapa no sirve, y no se sabe por qué.** Sobre la recta medida de 20,08 m, empujada de ida y vuelta **sin girar el carro** —cierre de bucle perfecto por construcción—, el mapa salió de **47,50 × 29,05 m** de estructura ocupada: **2,4 veces el largo real y diez veces el ancho**, curvado, con las paredes dibujadas varias veces en abanico y la vuelta sin superponerse a la ida. La cadena del portátil corrió limpia (`codigo: 0`, sin saltos de tiempo hacia atrás), así que el defecto no está ahí. **Se planteó como primera medida de R3 en hardware y se retira**: la deriva de `rf2o` medida en simulación sobre este tipo de pasillo fue del **5,7 %**, no del 240 %, y un resultado a tres órdenes de magnitud de lo esperado obliga a sospechar del dato de entrada antes que del algoritmo. **La causa más probable es la batería**, y no por descarte: a las 23:47, con el carro a punto de morir, `/scan` daba `max 0,464 s` entre barridos y `std dev 0,072`; recargado, a las 00:36, con el mismo sensor, los mismos procesos y los dos drivers igual de abiertos, daba `max 0,177 s` y `std dev 0,008` — **nueve veces más estable**. El bag que se mapeó se grabó entre esas dos medidas. Es **el mismo patrón del 28-ago**, cuando se propusieron tres causas de software y las tres eran falsas. **No se da por probado**: la prueba está dentro de la pasada de mapeo del viernes, que construye el mapa en el sitio y lo mide contra el flexómetro. **Consecuencias documentales, todas hechas:** se corrigieron el Paso 3.1 de [`GUIA_PASADA_MAPEO.md`](Documentos/GUIA_PASADA_MAPEO.md) y el 3.2 de [`GUIA_PASADA_LOCALIZACION.md`](Documentos/GUIA_PASADA_LOCALIZACION.md) con la comprobación de dueño y con `grep message_count` como paso de cierre obligatorio —la comprobación de cinco segundos que habría ahorrado la noche—; se sustituyó `pgrep -f rplidar | head -1`, que devuelve el proceso de AWS y miente, por leer las líneas filtrando `__ns:=/rplidar_ros`; se añadieron los dos huecos de la Parte 1 que aparecieron al ejercitarla (`source /opt/ros/humble/setup.bash` antes de `comprobar_movimiento_bag.py`, y `bash ... > log` en vez de tubería, porque con una tubería el `$?` es el de `tail`); y se creó [`HOJA_CAMPO_G2.md`](Documentos/HOJA_CAMPO_G2.md), extracto operativo de las dos guías para quien vaya al pasillo, con la regla de decisión del «caso plátano» —si el mapa mide más del doble, comprobar batería, repetir una vez, y **si vuelve a salir curvo parar y no correr las seis pasadas de localización**, que se miden contra ese mapa—. De paso se corrigió la cita `§6.3 → §7` del protocolo en los cinco sitios donde estaba mal |


---

## 8. Próximos pasos

La planificación completa de S17 a S32, con criterio de cierre verificable por semana, está en
[`Documentos/CRONOGRAMA_S17_S32.md`](Documentos/CRONOGRAMA_S17_S32.md). Arrastre de S17, al 2026-08-15:

1. ✅ **Contrato de interfaces ROS 2** (hito H1) — redactado en [`CONTRATO_INTERFACES.md`](Documentos/CONTRATO_INTERFACES.md), aplicado con bitácora en [`S17_aplicacion_contrato.md`](Documentos/Evidencia/S17_aplicacion_contrato.md) y **verificado en simulación el 2026-08-14**: `gazebo_ros2_control` y `agent_control.yaml` bajo namespace, dos árboles TF separados, controladores activos y mando aislado por robot ([`S17_nav2_namespaces.md`](Documentos/Evidencia/S17_nav2_namespaces.md), [`logs/S17_aislamiento_mando.txt`](Documentos/Evidencia/logs/S17_aislamiento_mando.txt)).
2. ✅ **Decisiones de alcance ante los directores** — en acta firmada el 2026-08-14, antes de S19 (§3). Queda por comunicar en el próximo corte la revisión de D3 y D4, posterior a la firma.
3. ⬛ **Entregable S16 — cerrado sin emitir.** Decidido el 2026-08-16: se absorbe y no se reprograma. El material técnico que lo motivaba no se pierde: la causa raíz del `max_laser_range` (12,0 m contra un LiDAR de 10,0 m) y el diagnóstico por descarte de `map_saver_cli` están registrados en la bitácora de §7, ambos con fecha 2026-08-03. Lo que se descarta es el documento, no el hallazgo. Deja de contarse como arrastre.

**Estado de S18 al 2026-08-15.** El criterio de cierre era: «H1 cerrado; el mundo de dos niveles
carga y se navega sobre él; el diagnóstico de hardware responde las cuatro preguntas». Los dos
primeros están cumplidos ([`S17_nav2_namespaces.md`](Documentos/Evidencia/S17_nav2_namespaces.md),
[`S18_entorno_dos_niveles.md`](Documentos/Evidencia/S18_entorno_dos_niveles.md)). **Falta el
tercero:** el **spike de hardware** acotado a 3–4 días, que responde cuatro preguntas y ninguna
más: `/scan` usable con el LiDAR real, `/cmd_vel` desde ROS 2, latencia entre vehículos, y qué
cambia entre Humble y Jazzy. Pasa a S19.

**El spike ya no se puede correr en el orden previsto.** Con un DeepRacer en intervención técnica
(R11), la pregunta 3 —latencia de ida y vuelta— exige los dos vehículos y queda supeditada a la
reparación. Las preguntas 1, 2 y 4 se responden con **un solo** vehículo y son las que se
ejecutan primero. **Las tres están respondidas** (2026-08-18 y 2026-08-19); la 3 sigue sin fecha.

### Cierre de S19 al 2026-08-22

El cronograma fija **cuatro condiciones**: se cumplen **tres**, y la cuarta pasa a S20 como
arrastre declarado, con el motivo escrito más abajo. El reparto día a día está en
[`PLAN_S19.md`](Documentos/PLAN_S19.md).

| # | Condición de cierre | Estado |
|---|---|---|
| 1 | Informe del spike con las **preguntas 1, 2 y 4** respondidas y su impacto en el plan | ✅ [`S19_spike_p4_humble_jazzy.md`](Documentos/Evidencia/S19_spike_p4_humble_jazzy.md) (P4) + [`S19_spike_p1_p2_hardware.md`](Documentos/Evidencia/S19_spike_p1_p2_hardware.md) (P1 y P2) |
| 2 | **Matriz de requisitos** publicada | ✅ [`REQUISITOS.md`](Documentos/REQUISITOS.md) es la matriz RF ↔ OE ↔ prueba; ningún OE queda huérfano |
| 3 | **Protocolo experimental de OE4**, escrito antes de instrumentar nada | ✅ [`PROTOCOLO_EXPERIMENTAL.md`](Documentos/PROTOCOLO_EXPERIMENTAL.md) (2026-08-22). Su dependencia —`puntos_interes.yaml` sin contenido— quedó resuelta el 20-ago con los 15 destinos del piso 1 |
| 4 | Los **dos agentes navegando simultáneamente**, cada uno en su nivel | ✅ **Cerrada el 2026-08-25** — [`S20_hito_h3_dos_agentes.md`](Documentos/Evidencia/S20_hito_h3_dos_agentes.md). Los dos a la vez a sus puntos de transferencia: 0,281 m y 0,143 m contra `/odom` |

**Cerrada el 2026-08-25**, con lo que S19 queda 4 de 4. Lo que sigue es el razonamiento con el
que se declaró el arrastre, que se conserva porque tres de sus cuatro bloqueos siguen vivos como
trabajo: el (b) —los nombres de las salas del piso 2— es hoy **prerrequisito de la campaña de
OE4**, no un pendiente menor. Lo que sí caducó es el temor del párrafo final: la corrida no
produjo «una captura de dos Gazebo abiertos» sino dos bags medidos contra `/odom`.

**Por qué la condición 4 pasó a S20, y no era una excusa.** **Lo que ya no es el problema:** el
21-ago se navegó por primera vez sobre `mundo_definitivo` y los tres destinos se alcanzaron, así
que la geometría de los pasillos nuevos **cabe** para un Ackermann y esa sospecha queda descartada
con medida, no con opinión. Lo que bloquea es lo demás: (a) ~~solo existe el mapa del piso 1~~
**resuelto el 23-ago**: el piso 2 ya tiene mundo y mapa propios, y `robot2` nace en celda libre
del suyo con **1,08 m** de holgura —la cifra bajó desde 1,50 m al añadir `Barrera_Escalera`, y la
celda ocupada que la fija es esa barrera, no ninguno de los paneles del 24-ago—; (b) los 15
destinos de `puntos_interes.yaml` siguen todos en
`nivel: 1` y no hay a dónde mandarlo, porque faltan los **nombres** de las salas del piso 2;
(c) siguen abiertos defectos que **bloquean OE4** y que
contaminarían la medida si se tomara ahora. Forzar la corrida produciría una captura de dos
Gazebo abiertos, que es exactamente el tipo de evidencia que este proyecto dejó de aceptar el
12-ago.

**Lo que hay que construir para cerrarla es concreto y cabe en S20:** el mapa
`mundo_definitivo_piso2` se generó el 23-ago; queda añadir a `puntos_interes.yaml` los destinos
de `nivel: 2`, y para eso hacen falta los nombres de las salas —decisión pendiente, no trabajo
técnico—. El único destino del piso 2 que está en la ruta crítica es su **punto de
transferencia**, el par de `piso1_escalera`.

Hallazgos abiertos que **bloquean OE4** y hay que resolver antes de instrumentar métricas:

- **`SUCCEEDED` no equivale a «dentro de tolerancia».** El 21-ago, ETM13 devolvió `SUCCEEDED`
  estando a **0,296 m** del destino, por encima de la `xy_goal_tolerance` de 0,25 configurada: el
  controlador mide contra la pose de AMCL, no contra la verdad del simulador. **Ya está resuelto en
  el papel:** el protocolo define el éxito contra `/odom` y no contra `SUCCEEDED`
  ([`PROTOCOLO_EXPERIMENTAL.md`](Documentos/PROTOCOLO_EXPERIMENTAL.md) §1 y §3.3). Falta la
  instrumentación que lo aplique. En las tres corridas el error apuntó **en el
  sentido de la marcha** (0,19 y 0,28 m pasado de largo), lo que sugiere frenada tardía del
  controlador más que ruido de localización; con tres muestras es hipótesis, no hallazgo.
- El verificador de meta cuenta como fallo llegadas que fueron buenas — **R12**, detectado el
  18-ago con evidencia gráfica. Es el que más urge: la *tasa de éxito* es una de las cuatro
  métricas y hoy se mediría mal.
- Una lectura de `/odom` reportó el desplazamiento del **otro** robot (detalle en
  [`S17_nav2_namespaces.md`](Documentos/Evidencia/S17_nav2_namespaces.md)). Las cuatro métricas de
  OE4 salen de odometría.
- ~~Los vehículos se desvían hasta 18° con `angular.z = 0`~~ — **resuelto el 2026-08-18**: era el
  mismo defecto de conversión de `angular.z`, no una causa aparte. El mecanismo está explicado; la
  cifra de mejora concreta se retiró hasta poder medirla con ventanas iguales.

**Dos huecos de sensado que S20 hereda.** (1) El LiDAR del kit mide y publica, pero todavía no
convive con la pila del vehículo: falta decidir entre el enlace simbólico `rplidar_node` →
`rplidar_composition` o editar el lanzador bajo `/opt/aws/deepracer/`. Ya **no** es bloqueante de
S20 —lo era bajo el diagnóstico del 19-ago, hecho sobre otro sensor—. (2) Las dos cámaras
**enumeran en USB pero no se ha comprobado que publiquen en ROS**, y una cámara que enumera y no
publica se ve idéntica a una que funciona hasta que se mira el tópico.

### S20 · 24 – 30 ago — reparto día a día en [`PLAN_S20.md`](Documentos/PLAN_S20.md)

Escrito el 2026-08-22, **antes de que empiece la semana**. El plan no reinterpreta el cronograma:
lo reordena y añade dos tareas que aquel no nombra —el mapa del piso 2 y el esquema del registro—
porque son prerrequisito de lo que sí nombra. Tres cosas que conviene tener presentes desde aquí:

1. **El piso 2 bloquea el propio criterio de cierre de S20.** El criterio pide que una solicitud
   produzca «la asignación del **agente correcto**», y eso solo se puede *probar* si hay algo a lo
   que mandar al agente del nivel 2. ~~Hoy los 15 destinos están los 15 en `nivel: 1`, así que la
   asignación acertaría por no tener alternativa.~~ **Cierto al escribir el plan el 22-ago, falso
   desde el 24-ago**, cuando `puntos_interes.yaml` incorporó destinos de `nivel: 2`. Se deja tachado
   y no borrado porque es lo que explica el diseño de la prueba del 29-ago: se lanzaron **dos**
   solicitudes de niveles distintos, precisamente para que la asignación no pudiera acertar por no
   tener alternativa. Y de los 18 puntos del piso 2, **solo uno está en
   la ruta crítica** —su punto de transferencia, sin el cual no hay relevo ni RF-24—; los nombres
   de las salas, que siguen aparcados a la espera de confirmarlos, hacen falta en S24 y no antes.
2. **La congelación de código es del 14 al 20 de septiembre: cuatro semanas** para el coordinador,
   el relevo, la HRI, el registrador, el analizador, el piso 2 y el despliegue físico.
3. **Hay una contradicción escrita sobre dónde corre la campaña física** —§4.3 de este tablero dice
   un piso y el cronograma dice dos plantas—, y de ella depende que **RF-24 tenga o no una sola
   medida física**. Está en §4 del plan; la decisión no es técnica.

#### Estado de S20 al jueves 2026-08-27 por la noche — planeado contra hecho

El cronograma fija para S20 este criterio de cierre: *«una solicitud origen–destino produce la
asignación del agente correcto y deja registrados los dos tiempos en un log estructurado. El mapa
real del laboratorio queda guardado y verificado con `herramientas/verificar_mapa.py`»*.

| Día | Lo que pedía [`PLAN_S20.md`](Documentos/PLAN_S20.md) | Estado |
|---|---|---|
| Lun 24 | Desbloquear el piso 2: corte oeste, mundo, mapa, punto de transferencia | ✅ Hecho, **y de más**: se sellaron los dos mundos con paneles sintéticos, que no estaba en el plan porque el defecto no se había nombrado |
| Mar 25 | H3 —los dos agentes a la vez— y cerrar el `/odom` cruzado | ✅ Las dos. H3 con 0,281 y 0,143 m contra `/odom`; el cruce cerrado **con causa** (el daemon de `ros2cli`), no por no reproducirse |
| Mié 26 | `coordinacion_msgs` en las dos distribuciones · congelar el esquema de RF-25 · aplicar `stateful: False` comparando cúspides | 🟡 **2 de 3.** El esquema quedó congelado y `stateful: False` está aplicado desde el 24-ago. **`coordinacion_msgs` no se ha compilado en la tarjeta Jazzy**, que era la mitad que daba valor a la tarea (§2.2 del plan), y **la comparación de cúspides contra el registro del 21-ago sigue sin hacerse**, que es lo que cierra R12 |
| Jue 27 | El nodo de coordinación con los dos tiempos instrumentados, probado con destino en el nivel 2 | 🟡 **Cumplido a medias, y por un motivo que no es pereza.** El nodo existe desde el 24-ago y **el registrador se construyó hoy**: una solicitud real produce un registro validado con `t_solicitud` y `t_robot_activo` dentro. Lo que **no** se hizo es probarlo con destino en el nivel 2, y ahí apareció el bloqueo estructural de la fila de abajo |
| Vie 28 | Frente B por la mañana (mapa del laboratorio + recta de ≥ 20 m) · corte semanal por la noche | 🟡 **Frente B hecho y de más; el corte no.** [`S20_frente_b_hardware.md`](Documentos/Evidencia/S20_frente_b_hardware.md): mapa guardado, odometría láser calibrada contra flexómetro, el LiDAR ya convive con la pila, y **teleoperación del vehículo desde ROS 2**, que no estaba en el plan. El corte semanal se pasó al sábado |
| Sáb 29 | *No estaba en el plan* | ✅ **Primera ejecución completa de RF-25.** [`S20_asignacion_por_nivel.md`](Documentos/Evidencia/S20_asignacion_por_nivel.md): dos misiones con destinos de niveles distintos, dos agentes asignados correctamente, dos registros JSON validados contra el esquema. Es la mitad de software del criterio de cierre |

**Lo que el jueves añadió sobre lo planeado, y ocupó la tarde entera.** Al correr la primera misión
instrumentada el error de llegada salió fuera de tolerancia, y perseguirlo destapó dos defectos de
configuración —los `alpha` de AMCL y el margen cero de `xy_goal_tolerance`— más un defecto en la
propia herramienta de diagnóstico, que estaba midiendo una constante. Los tres están en la bitácora
del 27-ago. **No es una desviación del plan: es el trabajo que el plan daba por hecho.** Sin esto,
las 30 corridas de S24 se habrían grabado sobre una configuración que no llega.

**El bloqueo nuevo, y es el que manda sobre lo que sigue.** *(Anotado el 2026-08-30: este párrafo describe el estado al cerrar S20 y se conserva tal cual. El bloqueo cayó al día siguiente y el relevo se ejecutó —ver la bitácora del 30-ago—. Se deja escrito porque un tablero que borra sus bloqueos cuando se resuelven no deja ver cuánto costaron.)* La condición B —la misión con relevo,
que es el aporte declarado del proyecto— **no se puede ejecutar hoy**, y no por falta de código:
`robot1` corre en `ROS_DOMAIN_ID=0` y `robot2` en el 2, con un `gzserver` cada uno, porque
`gazebo_ros` de Humble aplica a todos los plugins el namespace del primer modelo cargado. Un nodo de
ROS 2 vive en **un** dominio, luego el coordinador alcanza a uno de los dos y nunca a los dos. El
relevo está escrito y probado como función pura —992 planes verificados— y **no tiene por dónde
ejecutarse**. Ya estaba anotado el 24-ago que el aislamiento por dominio *«no hacía correcto el
marco sin prefijar, solo aplazaba la colisión al punto del proyecto donde más caro sale»*; el punto
es este. Queda registrado como séptimo prerrequisito en el §10 del protocolo.

#### Cierre de S20 al sábado 2026-08-29

El criterio tiene **dos mitades**, y no cierran igual. Conviene decirlo así y no promediarlas.

| Mitad del criterio | Estado |
|---|---|
| «Una solicitud origen–destino produce la asignación del **agente correcto** y deja registrados los dos tiempos en un log estructurado» | ✅ **Cumplida, y por encima de lo pedido.** El criterio pedía *una* solicitud; se ejecutaron **dos**, con destinos en niveles distintos, porque una sola no distingue «eligió bien» de «siempre responde lo mismo». Dos agentes distintos asignados correctamente y dos registros validados contra el esquema congelado ([`S20_asignacion_por_nivel.md`](Documentos/Evidencia/S20_asignacion_por_nivel.md)) |
| «El mapa real del laboratorio queda guardado **y verificado con `herramientas/verificar_mapa.py`**» | 🟡 **Guardado sí; verificado no, y no por omisión.** La herramienta exige un `.world` contra el que comparar las paredes, y un laboratorio físico no tiene `.world`. Se corrió a mano la mitad que no depende del mundo. **El criterio, tal como está redactado, no es aplicable al banco físico** ([`S20_frente_b_hardware.md`](Documentos/Evidencia/S20_frente_b_hardware.md) §5) |

**No se declara cumplida la segunda mitad por analogía, ni se bajan los umbrales para que pase.** El
umbral de frontera de la herramienta es del 1 % y el mapa del laboratorio da 15,00 %; esa diferencia
no dice que el mapa esté mal, dice que el 1 % está calibrado sobre mundos simulados cuyos vanos se
sellaron a propósito. Ajustarlo para que el criterio pasara convertiría la verificación en un
trámite. **Lo que corresponde es reescribir el criterio para el banco físico**, y eso es una decisión
de protocolo que se lleva a S21.

**Los tres hallazgos de la semana que cambian trabajo futuro:**

1. **El *tiempo de asignación* valía cero por construcción — corregido el mismo sábado, y el arreglo
   cambió la naturaleza del problema.** `_marcar()` fijaba `etapa` y `robot_activo` en la misma
   llamada y publicaba una sola vez, así que nunca había estado intermedio. Con la etapa `RECIBIDA`
   publicada antes de planificar ya hay dos mensajes donde había uno, verificado sobre bag. **Pero
   los dos salen con el mismo sello:** `/clock` late a 10 Hz y la asignación tarda ~0,14 ms. Dejó de
   ser un defecto de código y pasó a ser un **límite del instrumento**, que se resuelve reportando
   una cota en campaña y caracterizando en banco (§3.2 del protocolo, enmendado). **El banco no
   existe todavía y tiene que estar antes de S24.**
2. **El bag no puede dar el RTF.** Con `--use-sim-time` todos los sellos son de simulación,
   incluidos los del `metadata.yaml`. Se corrigió el mismo día: `grabar_mision.sh` mide `/clock`
   alrededor de la ventana grabada y escribe `rtf.json` junto al bag.
3. **Repetibilidad no es calibración.** La odometría láser del vehículo cierra un circuito de 18,4 m
   con 0,18 % de deriva y aun así estima **+2,9 % largo** contra flexómetro: el error de escala se
   cancela entre ida y vuelta. Sin la referencia externa, el dato habría pasado por una validación
   exitosa.

**Arrastre declarado a S21:** la verificación del mapa físico (decisión de protocolo), ~~la corrección
del tiempo de asignación~~ —hecha el mismo sábado, y sustituida por **construir el banco de
caracterización**—, `coordinacion_msgs` en la tarjeta Jazzy, y la comparación de cúspides que cierra
R12. El bloqueo de dominios sigue siendo el que manda. *(Anotado el 2026-08-30: dejó de mandar al domingo siguiente. El arrastre real que queda de esta lista es `coordinacion_msgs` en Jazzy, las cúspides de R12 y la verificación del mapa físico.)*

**Un arrastre nuevo que no estaba previsto: G2 no se capturó.** La salida al laboratorio del 28-ago
dejó cinco bags (`mapas/bag_mapa_*`, 16 MB, grabados en la tarjeta del carro —`storage_identifier:
mcap`, `ros_distro: jazzy`—) que **solo contienen `/rplidar_ros/scan`**: sin `/odom`, sin `/tf` y sin
`/amcl_pose`. La recta de ≥ 20 m que exige G2 —M1 ≥ 0,90 de desplazamiento registrado sobre real, M2
≤ 0,50 m de `amcl_pose` contra cinta— **no quedó registrada de forma que se pueda evaluar**, y uno de
los bags (`bag_mapa_1456`) ni siquiera tiene `metadata.yaml`. Se anota aquí, y no se disimula: G2 es
lo que impide que el GO/NO-GO de S21 devuelva un GO falso.

*(Anotado el 2026-09-01, tres días antes del G2 del viernes: el arrastre es peor de lo que decía este
párrafo, y ya está caracterizado en [`S21_preparacion_G2.md`](Documentos/Evidencia/S21_preparacion_G2.md).
Los bags del carro **tampoco se abren en este PC** por dos bloqueos independientes —la metadata de
Jazzy en versión 9 contra la versión 5 que Humble lee, ya resuelta con
[`herramientas/adaptar_bag_jazzy.py`](herramientas/adaptar_bag_jazzy.py), y el plugin
`ros-humble-rosbag2-storage-mcap` que falta y pide `sudo`—. Y el **mapa del pasillo que M2 necesita no
existe ni se puede construir** desde lo que hay. Consecuencia: el viernes son **dos pasadas**.)*

*(**Corregido el mismo 2026-09-01, más tarde:** la razón que este párrafo daba —que falta la TF
`odom → base_link` y los cinco bags traen un único tópico— es cierta pero **no es la que manda**, y
creérsela habría hecho volver del pasillo con el mismo problema. La TF ausente **se puede generar**
reproduciendo los barridos contra `rf2o`, que es exactamente lo que hace el carro en vivo; la cadena
fuera de línea se montó y funciona. Lo que no se puede generar es el movimiento: **682,6 s de los
812,8 grabados son el sensor quieto**, el 84,0 %. Un LiDAR que no se traslada no da de dónde
triangular estructura, así que el mapa no saldría **ni con la TF grabada**. Medido sin usar odometría
para no razonar en círculo; detalle en la bitácora del 01-sep y en el §3 de
[`S21_preparacion_G2.md`](Documentos/Evidencia/S21_preparacion_G2.md).)*

### S21 · 31 ago – 6 sep — la semana que decide si hay aporte

**Replanificada el domingo 2026-08-30, antes de abrir la semana.** El plan que estaba escrito aquí
—prioridad 1 «romper el bloqueo de dominios DDS», prioridad 2 «banco del tiempo de asignación»—
**se cumplió el domingo, adelantado**. Se deja constancia de que se replanificó y por qué, en vez de
sustituirlo en silencio.

Lo que eso cambia es el tipo de riesgo de la semana. Ya no es *«¿existirá el aporte?»* —existe, corrió
y está medido— sino *«¿se podrá reportar?»*. Y ahí el cuello de botella es que hoy hay **una** misión
con relevo, sin sortear y sin registro compuesto, cuando el protocolo pide **N = 30 con sorteo por
semilla**.

| Día | Actividad | Entregable verificable | Por qué va ahí |
|---|---|---|---|
| **Lun 31** | ✅ **Poner el registro al día.** Barrer los archivos que todavía instruyen `ROS_DOMAIN_ID=2` (`README.md`, `GUIA_EJECUCION.md`, `CONTRATO_INTERFACES.md`, `medir_paredes.py`) y actualizar los porcentajes de OE1 y OE4 en este tablero | `verificar_repositorio.sh` en 12/12 y ni un archivo de instrucciones con el dominio 2 | Cinco documentos afirman hoy lo contrario de lo que pasó. Es el riesgo **R6** —discrepancias informe ↔ repositorio— materializado sobre el resultado central. Va primero porque nada de lo demás se sostiene si el registro miente |
| **Mar 1** | ✅ **`herramientas/sortear_misiones.py`** (§9.5 del protocolo): sortea N pares origen–destino del catálogo con semilla anotada, equilibrando condición A y condición B | El guion, su prueba pura, y las 30 misiones de la campaña sorteadas y versionadas | Sin él no hay campaña, solo corridas sueltas. Es la pieza más barata que separa n = 1 de N = 30, y no necesita simulador |
| **Mié 2** | ✅ **Analizador de campaña** (§9.6): agrega los registros JSON y produce las **cuatro métricas de OE4** con su dispersión y la contabilidad de descartes contra el techo del 20 % | El analizador, probado contra los cuatro registros que ya existen | Sin él, 30 corridas de S24 son 30 archivos sueltos. Se puede escribir ya porque los registros existen |
| **Jue 3** | 🟡 **Adelantado a medias el martes 1.** **Componer el registro de la corrida del 30-ago y repetir la misión ya sorteada.** Ejecutar de paso el `clock_topic` de `slam_toolbox.launch.py`, que está escrito, comiteado y nunca corrido | Un registro validado contra el esquema, y la primera misión de campaña con su semilla | Convierte la demostración en el primer dato agregable. Y una ruta de código nunca ejecutada es una ruta que no existe. **Hecho:** el registro de la corrida del 30-ago está compuesto y validado, y de paso destapó dos defectos del compositor y que los dos productores de registro no producen el mismo documento. **Sigue abierto:** repetir la misión sorteada y ejercitar el `clock_topic`. **Y se añadió, con prioridad sobre todo lo anterior, la segunda pasada del G2** —la que produce M1 y M2—, que no estaba escrita: existía el criterio y no el procedimiento. **Escrita el miércoles 2** en [`GUIA_PASADA_LOCALIZACION.md`](Documentos/GUIA_PASADA_LOCALIZACION.md), antes de salir al pasillo y a propósito. Contesta las cuatro que estaban abiertas: **cruces y no rayas** en los dos extremos, porque M2 mide en 2D y hace falta un punto repetible; **tres pasadas por sentido**, con un mínimo duro de una por sentido para no volver con nada; **los dos sentidos con cifras separadas**, porque el 26-ago hubo un factor de cuatro entre este y oeste y promediar lo borra; y la pose de llegada se lee de una **parada declarada de 5 s** dentro de la grabación, que el análisis detecta con la misma heurística de `comprobar_movimiento_bag.py`. **El orden contraintuitivo se confirmó al escribirla: escribir primero destapó dos cosas que medir primero habría enterrado.** (1) El punto de referencia del carro tenía que ser **el LiDAR y no el eje trasero** —11,1 cm de sesgo sistemático según el URDF, el 22 % del presupuesto de M2—, y eso cambia el Paso 3.1 de la **primera** pasada, no de la segunda. (2) Arrancar el mapeo **sobre la marca de 0 m mirando al fondo** hace que el `(0,0,0)` del mapa sea esa marca, y entonces el `set_initial_pose` que AMCL ya trae sale correcto sin tocar un parámetro. Las dos son marcas en el suelo que no se pueden añadir con el pasillo ocupado. **Y se escribieron el mismo miércoles 2 las dos herramientas que faltaban**, `localizar_desde_bag.sh` y `medir_g2.py`, un día antes de lo previsto. `medir_g2.py` se hizo con la prueba primero —21 comprobaciones en 8 casos, sin ROS, sin bag y sin pasillo— y **reproduce exactamente la predicción del §4**: M1 = 1,029 y M2 = 0,58 m sobre 20 m. Eso significa que el viernes el resultado se podrá **contrastar contra un número anticipado**, no solo registrarse. **Correr la cadena entera sobre un bag real destapó cuatro cosas que la revisión de escritorio no vio:** (1) el guion no admitía más tópico que `/scan` y los bags del 28-ago publican en `/rplidar_ros/scan`, así que el Paso 1.2 habría fallado; (2) el volcador moría al arrancar porque `rclpy` ya declara `use_sim_time` por su cuenta y volver a declararlo aborta el nodo; (3) el `Waiting for laser_scans` que rf2o repite miles de veces **no es una avería** sino su temporizador de 20 Hz adelantando a un LiDAR de 10 Hz; y (4), la más grave, **la guarda de saltos de reloj marcaba como inválida cualquier corrida sana**. Un salto hacia atrás al arrancar la reproducción es inevitable —hasta ese instante los nodos van con la hora de pared y el primer `/clock` del bag los manda a la hora de grabación, anterior— mientras que la avería del 1-sep son saltos **repetidos**. La guarda buscaba la cadena en vez de contarla, así que el viernes habría mandado descartar las seis pasadas buenas. Ahora cuenta, y solo protesta a partir de dos por log. Las tres primeras están corregidas o documentadas en la tabla de la Parte 7; ninguna se habría visto sin ejecutar. **Bloqueada por una decisión del director de tesis y del tesista, no por código:** el §4 de [`S21_preparacion_G2.md`](Documentos/Evidencia/S21_preparacion_G2.md) demuestra que **M1 no puede fallar** tal como está escrito —umbral de un solo lado, tolera el 10 % y el peor error jamás medido es 5,7 %— y que **el resultado de M2 ya está calculado**: 0,029 × 20 m = 0,58 m > 0,50 m. Una puerta cuyo veredicto se conoce antes de correrla no gatea nada. Hay que decidir **antes de salir** si M1 pasa a dos lados (`\|registrado ÷ real − 1\| ≤ 0,10`) y sobre qué ramifica el GO/NO-GO si M2 falla, porque el §6.3 del protocolo prohíbe fijar umbrales después de ver los datos. **Corregido el jueves 3:** la misión sorteada **no hay que repetirla**, porque ya se corrió el 30-ago y es válida —`S21_piloto_A_02`, RTF 0,9988, error 0,215 m—, igual que el piloto de condición B con relevo —`S21_piloto_B_01`, RTF 0,9915, error 0,120 m—. Lo que queda de esa actividad es **componer los dos registros**, no ejecutar corridas nuevas. Sigue abierto el `clock_topic` |
| **Vie 4** | **G2 por la mañana** (recta de ≥ 20 m sobre el vehículo; **el carro graba solo `/scan`**, y `/odom`, `/tf` y `/amcl_pose` se generan después en el portátil) · **corte semanal por la noche**: `ESTADO.md`, entregable y commit | Bag evaluable contra M1 ≥ 0,90 y M2 ≤ 0,50 m, y el GO/NO-GO resuelto | G2 es lo único que impide que el GO/NO-GO de S21 devuelva un **GO falso**. Los bags del 28-ago no sirven: solo tienen `/rplidar_ros/scan` y uno ni siquiera tiene `metadata.yaml`. **Preparado el martes 1** ([`S21_preparacion_G2.md`](Documentos/Evidencia/S21_preparacion_G2.md)): la mañana **no habría salido**. Van **dos pasadas**, no una —mapa con SLAM y después localización con AMCL—, falta `sudo apt install ros-humble-rosbag2-storage-mcap` **antes del jueves**, y los umbrales M1 y M2 hay que decidirlos **antes** de salir al pasillo. **Las dos pasadas ya están escritas paso a paso** —[`GUIA_PASADA_MAPEO.md`](Documentos/GUIA_PASADA_MAPEO.md) y [`GUIA_PASADA_LOCALIZACION.md`](Documentos/GUIA_PASADA_LOCALIZACION.md)—, con la cadena fuera de línea verificada; lo que añade el hallazgo del 84 % es que **la grabación se comprueba en sitio y no al volver** —`comprobar_movimiento_bag.py` antes de recoger—, porque las tres causas del 28-ago eran todas detectables allí mismo. **Y hay una comprobación anterior a todas que nadie ha hecho: que la recta de ≥ 20 m exista.** Se hace con flexómetro, sin carro, y si no existe invalida G2 tal como está escrito |

**Al cierre del lunes 31 los tres primeros días están hechos.** El sorteo y el analizador cierran los
dos últimos pendientes del §9 del protocolo, y con eso **la campaña deja de estar solo definida y pasa
a ser ejecutable**: existe el camino completo sorteo → corrida → bag → registro → métricas con
intervalo de confianza, y cada pieza tiene prueba propia que corre sin simulador. Lo que compró
adelantarse **no fue tiempo, fue información**: escribir el analizador destapó que **RF-24 no tenía
campo en el registro** y que el intervalo de confianza del §6.1 no se reproducía (ver la bitácora del
31-ago). El primero de los dos habría costado las 30 corridas de S24 enteras, y **no se habría notado
ejecutándolas, solo agregándolas** — o sea que la única forma de encontrarlo a tiempo era escribir el
consumidor antes que los datos.

**Lo que eso libera para martes y miércoles, en este orden:** subir la corrida del jueves —componer el
registro del 30-ago y correr la primera misión sorteada— y, con el tiempo restante, el arrastre. **No
se adelanta G2 al martes:** necesita el vehículo montado y la mañana del viernes ya está reservada.

**Arrastre que entra si sobra tiempo, en este orden:** `coordinacion_msgs` compilado en la tarjeta
Jazzy (bloquea toda demostración física de la coordinación) · la comparación de cúspides que cierra
**R12** (toca directamente la métrica de tasa de éxito) · reescribir el criterio de verificación de
mapa para el banco físico.

**Lo que NO entra en S21, y conviene decirlo por escrito:** la HRI es S22 y no se adelanta pese a que
OE3 está en 0 %; los nombres definitivos de las salas del piso 2 hacen falta en S24; y **no se repite
ninguna corrida «para tener más datos» antes de que exista el sorteo**, porque una corrida no sorteada
no es un dato de campaña y acumularlas da sensación de avance sin producirlo.

**El criterio de éxito de la semana**, para que el viernes no se discuta: que exista una campaña
*ejecutable* —sorteo, analizador y un registro de ejemplo que atraviese los dos— y que el tablero
diga la verdad. **No** es haber corrido las 30 misiones: eso es S24 y adelantarlo con el analizador a
medias obligaría a repetirlas.
